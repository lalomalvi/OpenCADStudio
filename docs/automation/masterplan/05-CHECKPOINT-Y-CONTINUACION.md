# Checkpoint y continuación
Corte inicial: 2026-09-23. Este documento describe el estado antes del commit documental; consultar historial Git y remotos para el SHA final de publicación.

## Estado conservado
- Raíz del usuario: C:/Users/Luis Martinez/Desktop/PROYECTOS DE CODIGO/02. EN DESARROLLO/OPEN CAD.
- Rama raíz: feat/audited-mcp-save, 0fa6f008; original FIBRA OPITA PLANETARIO.dwg sin seguimiento. No añadirlo a Git.
- Worktree del plan: C:/Users/Luis Martinez/.codex/worktrees/mcp-robustness-masterplan/OPEN CAD.
- Rama del plan: codex/mcp-robustness-masterplan, base origin/main 133bfddb.
- Endurecimiento: C:/Users/Luis Martinez/.codex/worktrees/image-to-cad-hardening/OPEN CAD, rama codex/image-to-cad-hardening, HEAD d002b08a4c70a88de5af5df103b4ecbfb6890b26.
- Worktree Luna: C:/Users/Luis Martinez/.codex/worktrees/a4be/OPEN CAD, detached d002b08a.
- Binario: C:/Users/Luis Martinez/.codex/worktrees/image-to-cad-hardening/OPEN CAD/target/release/OpenCADStudio.exe.
- Evidencia privada: C:/Users/Luis Martinez/.codex/acceptance/open-cad/2026-09-23-luna-floorplan-01.
- Tarea Luna: 01a0cef6-c939-7d30-929b-5c649b33449e.

La raíz no está en main y no recibe automáticamente los documentos de la otra rama; abrir este worktree o fetch/checkout controlado. El ensayo permanece partial. No hay revisión externa ni visual del usuario aceptada. La próxima sesión no necesita abrir el DWG para M0/M1.

## Publicación verificada de esta sesión
- Documentación: commit 9814cd17a1ad95b0565cd1454da82d695a62b111 (incluye normalización de finales de archivo).
- Merge documental publicado en origin/main: ed8dbaf2a87494f23d3960232fda4a212fc05a01.
- Rama experimental respaldada en origin/codex/image-to-cad-hardening: d002b08a4c70a88de5af5df103b4ecbfb6890b26; no integrada a main.
- El worktree del plan quedó en codex/mcp-masterplan-integration siguiendo origin/main.
- Se comprobaron enlaces Markdown locales y git diff --check del diff integrado. No se ejecutó suite CAD porque este cambio solo contiene documentación.
- El cierre se integra con commit posterior al merge indicado; obtener su SHA con git log y contrastar git ls-remote origin refs/heads/main.
- No se publicaron PNG, DWG, JSONL privados ni cambios en upstream. No se creó PR: merge local documental y push directo autorizado al fork.

## Primera entrega siguiente
M0 completo y un cambio acotado M1 con pruebas de protocolo. No prometer completar todos los hitos en un turno. Antes de editar comprobar AGENTS.md en rutas aplicables, HEAD/remotos, manifest del binario y cambios ajenos.
Revisar especialmente la diferencia entre capacidades del programa ya disponibles y capacidades no usadas por Luna. Revisar asistencia supervisora y telemetría para no declarar ejecución autónoma total.
Mantener todos los ensayos nuevos en directorios nuevos; no reescribir el corte 2026-09-23.

## Prompt listo para pegar
Continúa el masterplan de robustecimiento de OpenCADStudio MCP. Empieza leyendo por completo docs/automation/masterplan/00-INDICE.md y sus cinco documentos vinculados en el worktree C:/Users/Luis Martinez/.codex/worktrees/mcp-robustness-masterplan/OPEN CAD. Verifica Git/remotos/AGENTS.md y el estado real antes de confiar en este checkpoint.

Ejecuta M0 y después el primer alcance verificable de M1: base de integración revisada, cliente MCP persistente reutilizando los harnesses existentes, handshake, readiness, correlación, timeouts y recuperación sin duplicar mutaciones. Implementa y prueba ese alcance; actualiza backlog y checkpoint. El código previo está en codex/image-to-cad-hardening (d002b08a) y todavía requiere revisión para integrarlo a main. No supongas que la publicación documental lo integró.

Preserva originales y evidencia del ensayo 2026-09-23-luna-floorplan-01. Usa fixtures sintéticos para pruebas técnicas. El ensayo de imagen permanece parcial: 273 entidades todas en capa 0, cotas como texto, sin aceptación de fidelidad externa. No atribuyas las diferencias de cadenas al plano original sin verificar extremos/caras/ejes. No abras ni modifiques el DWG del usuario para este hito.

Mantén controles críticos en backend y contratos. Guarda evidencia automática y sanitizada. No transcribas hashes manualmente ni deduzcas modelo efectivo de config.toml. Respeta negativas de política sin eludirlas. No publiques planos, logs crudos ni credenciales. La reconstrucción visual de futuros benchmarks usa exclusivamente GPT-6 Luna medium salvo protocolo nuevo; el desarrollo del software no tiene esa restricción. No lances evaluaciones de modelos adicionales por iniciativa propia.

La autorización vigente permite implementar el alcance, pruebas, documentación, commits, merge y push al fork lalomalvi/OpenCADStudio cuando el diff y los checks estén conformes. Inspecciona siempre origin/upstream; no publiques al repositorio del autor. Si es apropiado crear PR en el fork, adjúntalo a la tarea. No uses force push ni incluyas archivos ajenos. Cierra con resultados passed/failed/partial/pending, SHAs local/remoto, pruebas ejecutadas, riesgos reales y el siguiente prompt.

## Corte M0/M1 — 2026-09-23 (vigente)

Worktree: `C:/Users/Luis Martinez/.codex/worktrees/mcp-robustness-masterplan/OPEN CAD`. Rama: `codex/mcp-persistent-client`. `origin` es `lalomalvi/OpenCADStudio`; `upstream` es `HakanSeven12/OpenCADStudio`. Base comprobada `origin/main` = `a069d7f146c27a18255f2a47d733bf678385feae`. El commit `55fc9948` documentó la decisión de integración antes del merge `c449853c` de la rama experimental `d002b08a`. Véanse `M0-INTEGRACION.md` y `BASELINE.json`; el generador no leyó el DWG original. El hash del Release previo coincide con el documentado. No se han modificado el original ni el ensayo histórico.

El primer corte M1 está en `docs/automation/mcp_client.py`: proceso stdio persistente, negociación moderna, correlación, cola limitada, drenado de stderr, timeout/deadline monotónico, sondeo de arranque sin doble launch, selección explícita y consulta de operación tras respuesta de mutación perdida. Los tres harnesses de evaluación importan el cliente compartido. `docs/automation/test_mcp_client.py` y `tests/fixtures/mcp_stdio_fake.py` dan ocho pruebas L1. `mcp_client.md` describe el contrato y sus límites.

Verificaciones observadas: ocho pruebas L1 pasaron; `py_compile` pasó; `git diff --check` pasó; `mcp_smoke.py` contra el Release anterior y contra el binario debug de este worktree pasó; el cliente nuevo negoció `2026-07-28` y enumeró cuatro herramientas reales. `cargo test --lib mcp::tests` pasó 13/13, y `cargo test --lib` pasó 1665 con 24 ignored y cero fallos; hubo cuatro warnings preexistentes. `cargo build --bin OpenCADStudio` pasó. El hash del binario debug lo genera `BASELINE.json`. No se lanzó la evaluación GUI sobre una sesión de usuario ni se abrió el DWG. L2 CAD sintético y L3–L5 siguen pending. El formato formal de readiness del servidor, precondiciones cliente obligatorias de documento/revisión, recuperación de lotes `run_script` tras pérdida de transporte, journal durable, propiedad/cierre y telemetría quedan pendientes.

### Publicación verificada del corte

- Código y evidencia M0/M1: `6db1b790eeb0609f5efdf46d7e20d6f4a09a4322`.
- `origin/codex/mcp-persistent-client` y `origin/main` devolvieron ese SHA en `git ls-remote` después de sendos push no forzados. `main` avanzó desde `a069d7f1`; incluye el merge explícito `c449853c` de la rama experimental y el commit de implementación.
- Destino exclusivo: `https://github.com/lalomalvi/OpenCADStudio.git`. No hubo push a `upstream` ni PR al repositorio del autor. No se creó PR en el fork: el usuario autorizó merge/push y ambos refs se publicaron directamente.
- El commit de este último registro documental tendrá un SHA posterior al de código. Verificarlo en la próxima sesión mediante `git log -1` y `git ls-remote origin`; el propio archivo no puede contener su SHA final sin crear otro commit.
- Revisión de publicación: 13 archivos de implementación/checkpoint, cero DWG/PNG/JPG/JSONL añadidos, cero coincidencias de patrones de secretos en el diff staged, enlaces Markdown locales completos, `git diff --check` aprobado. El caso histórico y el DWG del usuario permanecen intactos.

### Prompt de continuación tras este corte

Lee `docs/automation/masterplan/00-INDICE.md`, los cinco documentos vinculados y este corte vigente. Empieza con `git status --short --branch`, `git remote -v`, `git log -3 --oneline` y `git ls-remote origin refs/heads/main refs/heads/codex/mcp-persistent-client`; contrasta los SHAs publicados con el checkpoint. Reproduce `python -m unittest discover -s docs/automation -p test_mcp_client.py -v` y los tests Rust focalizados. Continúa M1: añade el contrato formal `starting/ready`, selecciona sesión por identidad y documento, exige revisión antes de mutaciones, y resuelve recuperación de `run_script` por request_id sin repetir efectos. Implementa pruebas de fallos y un L2 sintético aislado antes de afirmar aceptación CAD. Preserva el DWG y el ensayo privado; no publiques secretos ni envíes nada a `upstream`.

## Corte en progreso M1/M2 — 2026-09-23

La sesión siguiente al corte publicado trabaja en `codex/mcp-lifecycle-and-recovery`, basada en `f785e15600bbedfc06f255aa1f40dc603820e1ed`. Al iniciar este corte, `origin/main` y `origin/codex/mcp-persistent-client` devolvieron ese mismo SHA. `origin` sigue siendo el fork `lalomalvi/OpenCADStudio`; `upstream` sigue siendo el repositorio del autor. Verificar de nuevo antes de publicar: este apartado se redactó antes del commit y push del corte.

Cambios: `ocs_sessions` expone estados `absent/starting/ready/failed` y arranque sin doble lanzamiento; descriptor con PID, ejecutable e instante de inicio; descubrimiento paralelo acotado y conexión directa al descriptor elegido; precondición de documento/revisión; progreso de `run_script` consultable por `request_id` dentro del mismo proceso MCP; cliente que no reenvía mutaciones inciertas y bloquea al perder la prueba de resultado. El journal entre reinicios, ACL de descriptor en Windows, heartbeat, cuarentena y cierre formal de sesión propia siguen pendientes. `mcp_client.md` delimita el contrato.

Ensayos L2 bajo `target/mcp-isolated`:

- `20260923-184721-770a24ab`: **failed** por modales de inicio; `report.json` original preservado.
- `20260923-185016-a0cf7e3e`: **failed** por sondeo transitorio de sesión; `report.json` original preservado.
- `20260923-185109-3e55f0f9`: **passed** con binario anterior a la última corrección; 3 entidades sintéticas, audit/save_verified/hash y salida.
- `20260923-185832-eab9a934`: **failed** porque `close_modal` reveló otro modal y el cliente trató `waiting_input` como fallo incierto; `report.json` preservado.
- `20260923-190435-10a7ca1c`: **passed**; `report.json` generado contiene hashes del binario y DWG sintético, tamaño, PID y sesión. Tres entidades, `save_verified` y salida del PID propio. Los PID de los tres ensayos fallidos también salieron tras cierre de ventana con ruta de ejecutable comprobada; no se forzó terminación.
- `20260923-191629-18cb7573`: **passed** con el binario reconstruido tras el límite de descriptor y la marca de inicio obtenida del SO; `report.json` local contiene hashes y `gui_exited: true`. Es el L2 final del corte.

Los artefactos L2 no se añaden a Git. No se abrió ni modificó el DWG del usuario, y no se cambió el ensayo histórico. El L2 no acredita L3 (interpretación de imagen), L4 (motor externo) ni L5 (revisión humana). Las pruebas L1 Python más recientes dieron 15/15; Rust focalizado dio 15/15. `cargo test --lib` dio 1667 passed, 24 ignored y cero fallos. La medición directa del binario de test en diez repeticiones con 100 descriptores sintéticos muertos dio 1851.7–1909.1 ms, p50 1869.7 ms y p95 conservador 1909.1 ms. La revisión final del diff estaba pendiente al redactar.

### Prompt de continuidad desde este corte

En el worktree `C:/Users/Luis Martinez/.codex/worktrees/mcp-robustness-masterplan/OPEN CAD`, lee el índice y los cinco documentos. Verifica `AGENTS.md`, Git/remotos, estado local, SHA remoto y pruebas: no des por publicado este corte sin `git ls-remote`. Conserva los fallos L2 originales. Completa M1.6 con recuperación durable o declara claramente la brecha; después M2 con identidad de proceso, política de documentos sucios y cierre de sesión propia, midiendo p50/p95 de descubrimiento. Prosigue M3–M8 por gates y fixtures sintéticos sin abrir el DWG del usuario ni publicar artefactos privados. Si un gate exige oracle, motor externo o revisión del usuario, registra exactamente qué evidencia falta. Mantén `upstream` intacto.

## Segundo corte M2 — 2026-09-23, en progreso

La rama `codex/mcp-lifecycle-and-recovery` publicó el primer corte en `origin/codex/mcp-lifecycle-and-recovery` con SHA `94967c302711a427d759c1ebe6784f1228cb95a9`; `origin/main` permaneció en `f785e15600bbedfc06f255aa1f40dc603820e1ed`. Se comprobó con `git ls-remote`. No hubo push a `upstream`. El segundo corte sigue sin commit al redactar; verificar SHA después de publicarlo.

M2 añadió `shutdown_owned_session` en el backend. Requiere que la GUI sea hija lanzada por ese mismo proceso MCP, compara PID, creación del proceso y ejecutable, canonicaliza `owned_root` y rechaza modal, comando activo, documento sucio o documento fuera de la raíz. Tras pedir salida espera el `Child` y guarda el resultado para la misma `request_id`. El primer L2 de este control quedó interrumpido con una pestaña sintética sucia; el cliente bloqueó al cerrar streams y ese hallazgo se corrigió con join acotado. El segundo L2 `20260923-193344-owned-8032995c` falló por comparar una ruta canonicalizada con otra sin canonicalizar; el documento sintético se guardó en `recovered-synthetic.dwg` dentro de ese run y su GUI salió. No se alteró ningún ensayo anterior.

El L2 `target/mcp-isolated/20260923-194235-owned-f6c67219/owned-report.json` pasó: rechazo `dirty_document`, rechazo `foreign_document`, cierre del PID propio, repetición del mismo ID con idéntico resultado y descubrimiento posterior `absent`. Después se duplicó el control en la cola de la GUI para cerrar la carrera entre lectura y salida. El L2 final `target/mcp-isolated/20260923-195314-owned-53014dbb/owned-report.json` repitió todos esos resultados. Ambos artefactos son locales, sintéticos y fuera de Git. `cargo test --lib mcp::tests` pasó 16/16 después de la corrección de identidad; la prueba GUI nueva también pasó. Las pruebas Python pasaron 15/15 sin ResourceWarning tras el cierre acotado. El binario Rust de ese build ejecutó la suite completa: 1669 passed, 24 ignored, cero fallos. La revisión y publicación de este segundo corte siguen pendientes al redactar.

Al segundo corte, M1.6 seguía parcial: el lote conservaba progreso solo dentro del proceso MCP. M2.2 y cierre explícito de pestañas seguían pendientes. M3–M8 no se promovieron por aquel L2.

## Tercer corte M1.6 — 2026-09-23, en progreso

Desde `41350691202f8326f8963e5333e967effdcb2217`, publicado y verificado en `origin/codex/mcp-lifecycle-and-recovery`, se abrió la rama `codex/mcp-durable-journal`. El servidor escribe un journal local por sesión y lote con archivo temporal, `sync_all` y reemplazo atómico antes de despachar un paso. La ruta se deriva de SHA-256 de IDs; el contenido conserva solicitud/progreso/estado pero ningún token. Al consultar la operación desde un nuevo proceso MCP, carga exactamente ese journal y consulta el ID de paso activo de la GUI. Precondiciones de documento y revisión se envían explícitamente en cada paso. Un journal corrupto falla cerrado. Los archivos están en el perfil local y no se publican.

Ensayos L2 aislados, todos en directorios nuevos:

- `20260923-200635-journal-5b09fc82`: **failed**, el reemplazo nativo no aceptó la ruta larga; quedó un `.tmp` sin `.json`. GUI cerrada limpiamente.
- `20260923-201129-journal-e5f259f0`: **failed de harness**, `wait_seconds:0` devolvió `running` con cero comandos completados, estado válido. La pestaña sintética se guardó en el mismo run y la GUI salió; el reporte original no se alteró.
- `20260923-201223-journal-ba19ea11`: **passed**; el primer MCP se cerró con el paso activo, el segundo recuperó por el ID original, terminó tres comandos con tres entidades, verificó el DWG sintético por hash y cerró la GUI. `journal-report.json` generado contiene identidad y hashes; no transcribirlos a mano.

La prueba Rust de journal en ruta Windows deliberadamente larga y de corrupción pasó. Pruebas focalizadas M1/MCP: 17/17 antes del ajuste de ruta larga; la prueba específica del ajuste pasó. Pruebas Python: 16/16. El binario Rust de ese corte ejecutó la suite completa: 1670 passed, 24 ignored, cero fallos. Revisión y publicación aún pendientes al redactar. M1.6 puede marcarse passed **solo para reinicio de MCP con la misma GUI**; reinicio de GUI, ACL Windows, retención de journal y conciliación externa continúan pendientes.

## Cuarto corte M2.3 — 2026-09-23, en progreso

El tercer corte se publicó en `origin/codex/mcp-durable-journal` con SHA `739f14cef397b58cd821f4873821e9cb1c0b207e`, verificado con `git ls-remote`; `origin/main` seguía en `f785e156`. La rama actual `codex/mcp-owned-document-lifecycle` deriva de ese SHA. No hubo push al repositorio del autor. Verificar remoto después de publicar este cuarto corte.

Se añadió `close_document` con política `require_saved` al servidor MCP y a la cola de la GUI. Exige que el mismo proceso MCP haya lanzado la GUI, identifica PID/inicio/binario, exige `document_id`/`revision`, rechaza pestaña sucia, modal, comando activo y ruta no guardada o fuera de `owned_root`. No habilita descarte automático. El L2 final `target/mcp-isolated/20260923-202242-owned-e9de5b27/owned-report.json` reportó rechazo de cierre sucio y externo, cierre del documento sintético ID 2, cierre del proceso propio y descubrimiento posterior `absent`. El reporte fue generado por el harness; no se añade a Git. Dos pruebas Rust focalizadas pasaron y el binario de test ejecutó la suite completa: 1672 passed, 24 ignored, cero fallos. M2.2 heartbeat/descriptor obsoleto/cuarentena y escenarios adicionales de M2.4 siguen pendientes; M3–M8 también.

## Quinto corte M2.2 — 2026-09-23, en progreso

El cuarto corte se publicó en `origin/codex/mcp-owned-document-lifecycle` con SHA `3b90ef81288deaa773df89e940e8b5f8f3d2473f`; `origin/main` permanecía en `f785e156`. La rama de este quinto corte es `codex/mcp-descriptor-liveness`. Verificar su SHA remoto después del commit y push; no enviar nada a `upstream`.

La GUI escribe un heartbeat local cada dos segundos sin token. Descubrimiento contrasta PID y creación del proceso con Win32 antes del handshake; un proceso terminado o PID reutilizado provoca cuarentena del descriptor en el mismo perfil, tras validar nombre, archivo regular y contención de rutas. Un proceso inaccesible o un timeout no autoriza cuarentena. El primer L2 `target/mcp-isolated/20260923-203703-owned-9a8cb961/owned-report.json` confirmó heartbeat e identidad, pero dejó el descriptor activo: faltaba `SYNCHRONIZE` para distinguir un proceso terminado con handle retenido. La prueba Win32 reprodujo `WAIT_FAILED` y el permiso denegado; se corrigió la máscara de `OpenProcess`. El L2 final `target/mcp-isolated/20260923-204627-owned-2626c01d/owned-report.json` pasó con heartbeat de 1345 ms, identidad `matched`, rechazo de pestañas sucias/ajenas, cierre del PID propio, descubrimiento `absent` y un descriptor en cuarentena. Ambos reportes originales se conservan. El heartbeat residual queda local en el perfil del ensayo. `cargo build --bin OpenCADStudio`, 16 pruebas Python y `cargo test --lib` (1674 passed, 24 ignored, 0 failed) pasaron. Cuatro warnings Rust preexistentes no son gates de este cambio. La protección ACL del descriptor en Windows todavía no está demostrada; M2.2 queda **partial**. M2.4 también permanece partial, al igual que M3–M8.

### Prompt de continuidad desde el quinto corte

Desde el worktree del masterplan, verifica Git, `AGENTS.md`, remotos y SHA remoto de `codex/mcp-descriptor-liveness` antes de asumir su publicación. Lee los seis documentos del masterplan y conserva todos los ensayos anteriores. Completa M2.2 verificando ACL de descriptores y cuarentena en Windows y midiendo p50/p95 tras el cambio; amplía M2.4 en modalidades/`waiting_user` con fixtures. Continúa M3–M8 por gates, con datos y planos sintéticos: telemetría y sellos, PlanSpec, semántica CAD, QA, evaluación controlada y entrega reproducible. No abras ni modifiques el DWG del usuario; no publiques archivos privados ni envíes nada a `upstream`. Si una aceptación requiere caso reservado, motor externo o revisión humana, registra el gate pendiente sin promoverlo a passed.

## Sexto corte M3 inicial — 2026-09-23, en progreso

`codex/mcp-descriptor-liveness` fue publicado en `origin` con commit `67231fc8` (verificar SHA completo remoto al retomar). Se añadió `usage_evidence.py`: recorre el rollout local sin exportar mensajes, IDs de respuesta ni rutas y deduplica por `response_id`. El ensayo histórico privado contenía 84 registros y 84 respuestas únicas; la salida sanitizada reprodujo entrada 9,727,404, entrada cacheada 9,513,472, salida 70,548 y total 9,797,952. El coste facturado queda `null`, la identidad efectiva del modelo y el uso del supervisor quedan `unknown`: el resumen numérico por sí solo no los acredita. Los cuatro tests sintéticos prueban duplicado idéntico, duplicado contradictorio, suma incorrecta e identidad ausente.

`artifact_evidence.py` introduce referencias locales PNG con hash, tamaño, dimensiones, documento y revisiones de geometría/cámara. Un hash cambiado o revisión obsoleta falla; el sello determinista excluye binario y restringe campos para evitar exportar el rollout o credenciales. Tres tests usan solo bytes sintéticos. Esto cubre el primer alcance verificable de M3.1, M3.3 y M3.5; **M3 sigue partial**: faltan instrumentación de tiempos por fase en el cliente/CAD, render-fence real, captura negociada, bitácora append-only, AUDIT/SAVE/VERDICT desde ejecución y presupuestos con reanudación. El PNG de fixture solo comprueba encabezado y hash, no decodificación completa. El caso histórico no se reescribe ni se promueve.

### Prompt de continuidad desde el sexto corte

Verifica Git y remoto del fork, ejecuta las pruebas M3 con `python -m unittest discover -s docs/automation/masterplan -p 'test_*.py'` y reproduce el agregado privado local mediante `python docs/automation/masterplan/usage_evidence.py <rollout-local> --expected-total 9797952` sin publicar la ruta ni contenido del rollout. Completa M2.2 ACL/latencia y M2.4; luego instrumenta M3 en el flujo real con timestamps, artefactos, sellos y presupuesto. Prosigue M4–M8 en fixtures sintéticos. Mantén intactos el DWG y el caso histórico; nada a `upstream`.

## Séptimo corte M4 inicial — 2026-09-23, en progreso

`codex/mcp-evidence-contract` publicó el sexto corte con commit `66a895f0` en el fork; obtener SHA completo remoto al retomar. En este corte se añadió `planspec.schema.json` y validador/compilador puro `planspec.py`. El contrato exige unidad `m`, origen, nodos con IDs únicos y procedencia, referencias existentes, cifras finitas y región/confianza. Comprueba texto y valor de cota contra la geometría referenciada con tolerancia de 0.001 m. Dos cotas de referencias distintas no producen falso conflicto; el fixture 2.50 sobre distancia 2.54 falla antes de emitir comandos. El dry-run ordena líneas/círculos por ID, da SHA-256 estable y mapa ID→comando. Cotas nativas y capas distintas de 0 se declaran `unsupported` y `executable=false`; no se ha conectado a GUI ni abierto un DWG. Cinco pruebas sintéticas de PlanSpec y siete de evidencia pasaron. M4 permanece **partial**: faltan grafo general de cotas, compilación de muros/aperturas/símbolos, manifest de capacidades por build, ejecución con trazabilidad de handles y L2 sintético de esa ejecución. El JSON Schema describe el subconjunto actual, no el contrato arquitectónico completo de `02-ARQUITECTURA-Y-CONTRATOS.md`.

### Prompt de continuidad desde el séptimo corte

Reverifica `git status`, `git remote -v`, SHA local/remoto y AGENTS. Mantén los cortes anteriores intactos. Mide M2 discovery con 100 entradas tras el heartbeat y resuelve ACL; completa trazas M3 y evidencia real. Extiende M4 por manifiesto/compilador/handles y prueba L2 usando únicamente un plano sintético. Después censa M5, valida M6 y prepara M7/M8 según sus gates. El DWG y evidencias privadas históricos no se abren ni se reescriben; no publiques en `upstream`.

## Octavo corte ACL Windows M2 — 2026-09-23, en progreso

El séptimo corte se publicó en `origin/codex/mcp-planspec-contract` con commit `5ff1af94` (verificar SHA completo remoto). La rama actual `codex/mcp-windows-descriptor-acl` añade `automation_security.rs`. En Windows el descriptor con token se crea directamente con `CreateFileW` y DACL protegido `owner rights + SYSTEM`, sin ventana de ACL heredado entre creación y escritura. El lector consulta el DACL y rechaza uno ampliado. Los journals nuevos también se escriben primero a un archivo privado y luego se reemplazan atómicamente. Esto mantiene la recuperación de lotes bajo el perfil sintético. Un journal antiguo con ACL heredado amplio, como el ensayo anterior que incluía otro SID con lectura, falla cerrado; no se migra automáticamente ni se reenvía el comando. Ese cambio de compatibilidad está identificado.

El L2 `target/mcp-isolated/20260923-210821-owned-f10b9292/owned-report.json` pasó: identidad/heartbeat, rechazo dirty/foreign, cierre propio, un descriptor en cuarentena. `Get-Acl` sobre ese descriptor mostró DACL protegido de propietario y SYSTEM sin ACE heredada. Una prueba Rust cambia deliberadamente el DACL de un archivo de fixture a Everyone y confirma rechazo. El L2 `target/mcp-isolated/20260923-211201-journal-d93ec728/journal-report.json` recuperó el lote tras reinicio de MCP, verificó tres entidades y cerró GUI. Diez ejecuciones de descubrimiento con 100 descriptores privados dieron 1874.5–1904.3 ms, p50 1890.85 ms y p95 conservador 1904.3 ms. `cargo build --bin OpenCADStudio`, 16 pruebas Python de cliente, 12 pruebas Python de masterplan y `cargo test --lib` (1675 passed, 24 ignored, 0 failed) pasaron. Los artefactos permanecen en `target` sin añadir a Git. M2.1 y M2.2 pasan **solo para este alcance Windows/sintético**; M2.4 y M3–M8 aún no están aceptados globalmente.

Una revisión final corrigió el caso de un descriptor legado con nombre de sesión válido y ACL amplio: ahora `descriptors_in` devuelve error en vez de ignorarlo y presentar `absent`, lo que podría haber lanzado una segunda GUI. El test focalizado de 100 descriptores privados pasó tras ese cambio. Se remidió el binario final en 10 ejecuciones: 1890.6–1907.2 ms, p50 1900.45 ms y p95 conservador 1907.2 ms. La medición anterior se conserva como corte previo.

### Prompt de continuidad desde el octavo corte

Verifica commit y SHA remoto de `codex/mcp-windows-descriptor-acl` antes de afirmar publicación. Preserva todos los runs previos, incluidos los fallos. Completa M2.4 y M3 con trazas reales por fase, artefactos y sellos; integra PlanSpec en L2 sintético y después aborda M5–M8 según los gates. No reutilices automáticamente journals viejos de ACL heredado: concilia su estado antes de cualquier edición. Mantén el DWG y la evidencia histórica intactos, y publica exclusivamente en el fork.

## Noveno corte M3 transporte — 2026-09-23, en progreso

El octavo corte se publicó en `origin/codex/mcp-windows-descriptor-acl` con commit `2b8ff754` (verificar SHA completo remoto). La rama actual `codex/mcp-transport-trace` añade un `TraceSink` opcional al cliente persistente: un JSONL nuevo por run, append-only, con ID de run, secuencia, método MCP de lista blanca, estado, timestamps UTC y duración monotónica por RPC. Nunca serializa argumentos, resultados, tokens ni texto de error. Un fallo de escritura queda indicado por `trace.failed`, sin reinterpretar ni repetir una mutación. El test L1 envió valores privados de fixture en dos RPC y confirmó que no aparecen en el archivo. La suite Python de cliente pasó 17/17 tras la instrumentación.

El L2 `target/mcp-isolated/20260923-212002-owned-185c1b47/owned-report.json` pasó cierre/propiedad y registró 27 eventos RPC; `write_failed=false` y `contains_token_field=false`. El JSONL permanece en `target`, fuera de Git. Esto acredita tiempos de transporte del cliente, **no** separación de tiempo de modelo, cola GUI, ejecución CAD o render, ni cobertura de toda la ejecución del benchmark. M3.2 sigue partial. M3 requiere todavía bitácora integral, ArtifactRef conectado a captura real, render-fence, AUDIT/SAVE/VERDICT automáticos y presupuestos/reanudación. El DWG del usuario y el ensayo histórico siguen sin abrirse ni alterarse.

### Prompt de continuidad desde el noveno corte

Reverifica remotos/SHAs y pruebas. Completa trazas M3 desde GUI/CAD hasta evidencia final con cobertura; conecta capturas reales mediante referencias y revisiones sin Base64 en JSONL. Ejecuta PlanSpec sintético por el cliente persistente, manteniendo `unsupported` como fallo de gate. Después censa e implementa M5/M6, y evalúa M7/M8 solo con cohorte y revisiones autorizadas. No abra el DWG del usuario; preserve evidencia previa y no publique en upstream.

## Décimo corte PlanSpec L2 — 2026-09-23, en progreso

El noveno corte se publicó en `origin/codex/mcp-transport-trace` con commit `3ca13bd1` (verificar SHA completo remoto). En `codex/mcp-planspec-l2`, el fixture versionado `fixtures/synthetic-room.planspec.json` se valida y compila antes de lanzar GUI; si el dry-run declara `unsupported`, no hay ejecución CAD. `mcp_isolated_smoke.py` envía los tres comandos del compilador al cliente persistente, comprueba tres entidades y mapea cada ID PlanSpec al handle `Added` del paso correspondiente. El primer L2 `target/mcp-isolated/20260923-212219-f47e037a/report.json` pasó ejecución, auditoría, guardado verificado y salida; el segundo `target/mcp-isolated/20260923-212302-cda4b60e/report.json` repitió y añadió trazabilidad ID→handle (`line-1`→`64`, `line-2`→`65`, `circle-1`→`66`). Ambos DWG son sintéticos, en `target`, fuera de Git. El SHA del fixture y de los comandos se generó por código en los reportes.

El L2 confirma ese subconjunto geométrico en el mismo motor y formato DWG; no demuestra interpretación de imagen, arcos/cotas/bloques/HATCH, dimensiones asociativas, interoperabilidad externa ni QA visual. M4 permanece partial. El DWG del usuario y el ensayo histórico continúan intactos.

### Prompt de continuidad desde el décimo corte

Verifica SHA publicado del corte PlanSpec y árbol limpio. Continúa M3 con telemetría por fase y evidencia automática real, M4 con grafo de cotas y capacidades por build; censa M5 antes de implementar semántica. Luego M6 QA geométrica/persistencia y M7/M8 con gates de motor externo/casos reservados/revisión humana. Usa solo fixtures sintéticos hasta nueva autorización; no publiques datos privados ni envíes nada a `upstream`.

## Undécimo corte censo M5 — 2026-09-23, en progreso

El décimo corte se publicó en `origin/codex/mcp-planspec-l2` con commit `c20eb1ea` (verificar SHA completo remoto). La rama `codex/mcp-cad-capability-census` añadió consulta del catálogo en un perfil aislado, sin editar el DWG del usuario. El L2 final `target/mcp-isolated/20260923-212518-owned-3061d305/owned-report.json` pasó: catálogo completo de 634 nombres, SHA `19a7dd4115908496fe8d2d2f56859fadb7ddd3128de79cfa4be6cb20e4940243`, diez candidatos M5 registrados, cierre propio y 28 eventos RPC sanitizados. El primer L2 `20260923-212453-owned-f4a97cc4` también pasó pero consultó `PSETUP`, nombre inexistente; la corrección es `PSETUPIN`, importación de page setup. Ambos reportes se conservan.

`M5-CENSO-CAPACIDADES.md` separa registro, compilación PlanSpec, ejecución MCP y persistencia/semántica. ARC, PLINE, DIMLINEAR/DIMALIGNED, BLOCK/INSERT, HATCH, LAYER/CLAYER y PSETUPIN están registrados; **ninguna** de sus semánticas M5 se acepta por ese hecho. Los fixtures de capacidad, reapertura y asociatividad siguen pendientes. M5 censo pasó solo para ese build.

### Prompt de continuidad desde el undécimo corte

Reverifica SHA remoto de `codex/mcp-cad-capability-census` y árbol limpio. Aplica la matriz del censo para probar capacidades M5 con fixtures sintéticos independientes, sin duplicar comandos existentes. Cierra M3/M4 y M6 con evidencia de ejecución, guardado y QA; prepara M7/M8 conforme a sus gates. Preserva el DWG y ensayos históricos; nada de archivos privados en Git ni push a upstream.

## Duodécimo corte ARC/PLINE nativos — 2026-09-23, en progreso

El undécimo corte se publicó en `origin/codex/mcp-cad-capability-census` con commit `fd9b0060` (verificar SHA completo remoto). La rama `codex/mcp-native-primitives-l2` añadió `mcp_native_primitives_smoke.py`, que reutiliza el perfil aislado del smoke PlanSpec y ejecuta `ARC 0,0 5,5 10,0` y `PLINE 20,0 24,0 24,4 C` tras los tres comandos de base. El primer L2 `target/mcp-isolated/20260923-212827-86ae5b24/report.json` pasó y mostró Arc radio 5 y Polyline de tres vértices con `is_closed=true`; el reporte inicial conservó demasiadas propiedades de la polilínea sintética. Se redujo a campos de aceptación concretos. El L2 final `target/mcp-isolated/20260923-212856-14636786/report.json` pasó con handles `67`/`68`, tipos nativos, auditoría, DWG sintético guardado y tipos Arc/Polyline presentes en el manifiesto de reapertura interna. El PID salió. Ambos reportes siguen en `target` y no se añaden a Git.

M5.2 se acepta **solo** para creación nativa y tipo persistido internamente en este fixture. No se verificaron espesor, uniones, exactitud completa de ángulos, asociatividad ni motor externo. El compilador PlanSpec aún no emite ARC/PLINE. El DWG del usuario permanece intacto.

### Prompt de continuidad desde el duodécimo corte

Verifica SHA remoto, Git y pruebas. Extiende PlanSpec para producir ARC/PLINE mediante capacidades demostradas; prueba espesores/uniones y persistencia geométrica real. Sigue con cotas, bloques, HATCH, capas y plantilla métrica en fixtures sintéticos. Completa M3/M6 antes de evaluar M7; mantén gates de motor externo y revisión humana. Nada de DWG privado ni upstream.

## Decimotercer corte QA geométrica interna M6.4 — 2026-09-23, en progreso

El duodécimo corte se publicó en `origin/codex/mcp-native-primitives-l2` con commit `34bccbf3` (verificar SHA completo remoto). En `codex/mcp-geometry-roundtrip-qa` se añadió una comparación antes/después del DWG sintético, abriéndolo de nuevo como pestaña de OpenCADStudio. Para ARC compara handle, capa, centro, radio y ángulos. Para PLINE compara handle, capa, vértices, cierre, ancho constante y grosor. La tolerancia numérica es 1e-6 unidades CAD; igualdad de campos no numéricos es exacta. El L2 `target/mcp-isolated/20260923-213106-0b44cc68/report.json` pasó con `roundtrip_geometry=matched_1e-6_internal`, auditoría, salida de GUI y SHA del DWG generado por código. Dos pruebas L1 sembraron cambios en radio, ángulo, cierre, vértices y NaN y verificaron rechazo; la suite Python de automatización pasó 19/19.

M6.4 queda **partial**: solo cubre dos tipos nativos y reapertura en el mismo motor. No se probó otro motor, atributos de las otras entidades ni QA visual/regional. El original del usuario y el ensayo histórico no se abrieron ni alteraron.

### Prompt de continuidad desde el decimotercer corte

Verifica SHA remoto y árbol Git; conserva el fixture y reportes. Completa QA para más tipos y motor externo identificado, sin llamar a una reapertura interna interoperabilidad. Cierra M3 y M4, y prueba M5 cotas/bloques/HATCH/capas con semántica verificable. Prepara protocolo M7 y entrega M8 solo cuando los gates faltantes tengan evidencia. No uses el DWG del usuario ni publiques artefactos privados/upstream.

## Decimocuarto corte sello CAD M3 — 2026-09-23, en progreso

El decimotercer corte se publicó en `origin/codex/mcp-geometry-roundtrip-qa` con commit `a4e7b406` (verificar SHA completo remoto). La rama `codex/mcp-cad-evidence-seal` registra en el reporte L2 un resumen de auditoría real (estado, errores, target, manifest, bounds, entidades desconocidas) y el manifest obtenido por `save_verified` tras reapertura interna. `seal_cad_run.py` usa ese reporte, el fixture y binario identificados, verifica hashes/tamaño y crea una sola vez cuatro archivos sanitizados. Rechaza auditoría fallida, manifest cambiado, DWG alterado, binario distinto, escape de ruta y GUI aún viva. El verificador coteja los archivos con las fuentes y sus hashes. Dos tests sintéticos prueban alteración y no exportación de campo privado.

El L2 `target/mcp-isolated/20260923-213433-3a2932b3/report.json` pasó con cinco entidades sintéticas, auditoría sin errores/advertencias, manifest interno igual antes/después y salida de GUI. En ese run se generaron `AUDIT.json`, `SAVE.json`, `VERDICT.json` y `SEAL.json`; `--verify` pasó. SHA-256 del DWG `A72E74F624B83F22583A34116D00806EE1823F7A5B4D0BF0552FBBD01A582E64`, SHA-256 del binario `0F9F4E42870D4AC5EDBD497986AD56DC1A21E779ADE98E3373B4021AB6597410`. El veredicto es **partial**, con motor externo, interpretación de imagen y revisión humana pendientes. Los artefactos se conservan en `target`, sin Git. Pruebas de masterplan 14/14 y `--verify` pasaron. `M3-SELLO-SINTETICO.md` explica reproducción y límites.

### Prompt de continuidad desde el decimocuarto corte

Verifica SHA remoto y árbol limpio. Completa M3 con artefactos PNG reales/revisiones/render-fence y tiempos por fase más presupuesto; no mezcles uso del supervisor con el generador. Extiende PlanSpec y M5 sobre capacidades probadas, M6 con QA espacial/external, y M7/M8 con cohortes y gates pendientes. Mantén el DWG del usuario, los runs fallidos y el caso histórico intactos; nunca publiques contenido privado ni upstream.

## Decimoquinto corte captura MCP y ArtifactRef — 2026-09-23, en progreso

El decimocuarto corte se publicó en `origin/codex/mcp-cad-evidence-seal` con commit `b83b8595` (verificar SHA completo remoto). La rama `codex/mcp-capture-artifact-fence` extiende `ocs_capture` con argumentos opcionales de documento y revisiones de geometría/cámara. La GUI devuelve las revisiones al terminar el screenshot; el servidor rechaza discrepancias, conserva `content` MCP de imagen para clientes remotos y agrega `structuredContent` sin Base64. El cliente persistente guarda un solo PNG acotado a ruta nueva y genera `ArtifactRef` con SHA, bytes, dimensiones y revisiones; el trace no registra contenido. Un test Python demuestra que una revisión obsoleta no escribe archivo. Las 20 pruebas Python de automatización y 20 pruebas Rust MCP focalizadas pasaron antes del L2.

El L2 `target/mcp-isolated/20260923-214220-27b215c6/report.json` pasó con PNG 1024×506, 37,292 bytes, hash `b568159ea1b80abaad2e4658e4b60c1d4eebb66d49d542077f2dfc9a8bb06c12`, documento 2/geometry 13/camera 0. La inspección visual mostró las tres entidades sintéticas, junto con UI y texto de ruta local; el PNG permanece privado en `target`. El L2 semántico `target/mcp-isolated/20260923-214352-af7e4c77/report.json` pasó con cinco entidades y captura ligada a documento 3/geometry 24/camera 1. Allí se creó `ARTIFACTS.json` y `SEAL.json` v2; `--verify` pasó. El test de sello detectó cambio de PNG. Los sellos v1 anteriores se preservan. Falta un render-fence explícito que asegure que el frame contiene la revisión observada; el recorte viewport aún incluye UI. M3.3 sigue partial, y el veredicto general sigue partial.

### Prompt de continuidad desde el decimoquinto corte

Verifica SHA remoto del corte y árbol Git. Implementa render-fence real en GUI o declara formalmente la brecha; mejora recorte para eliminar UI sin perder geometría y mide bytes/tokens observados. Completa presupuesto/reanudación y telemetría por fase M3, PlanSpec/M5/M6, protocolo M7 y M8 según gates. Mantén capturas y DWG sintéticos dentro de `target`, sin publicar archivos privados ni tocar el DWG del usuario; nunca push a upstream.

## Decimosexto corte capas PlanSpec/M5.1 — 2026-09-23, en progreso

El decimoquinto corte se publicó en `origin/codex/mcp-capture-artifact-fence` con commit `69d13250` (verificar SHA completo remoto). Desde `codex/mcp-layer-semantics-l2`, el primer L2 `target/mcp-isolated/20260923-214751-c6f6dcbf/report.json` ejecutó `LAYER NEW A-WALL`, `CLAYER A-WALL` y una línea sintética: la consulta por handle devolvió `A-WALL`, y el manifest tras reapertura interna contó una entidad en esa capa. El censo del binario debug actual (SHA `D51B92533028E011CEE15DD1D39D3710E8340C018A763E0DA34518475958FB55`) devolvió catálogo de 634 comandos con SHA `19a7dd4115908496fe8d2d2f56859fadb7ddd3128de79cfa4be6cb20e4940243`; L2 `20260923-214838-owned-86d583d1`.

El compilador PlanSpec ahora produce pasos de creación/cambio de capa deterministas cuando recibe `layer_assignment` verificada; sin esa capacidad, mantiene `unsupported`. El manifiesto `fixtures/capabilities-d51b9253.json` liga la capacidad al hash exacto de binario y catálogo; el harness valida ambos antes de mutar. El L2 PlanSpec final `target/mcp-isolated/20260923-215103-a5cdfc7f/report.json` pasó con seis pasos, tres handles, mapa ID→capa (line-1 A-WALL; line-2/circle-1 en 0), auditoría, guardado/reapertura y PNG local. `SEAL.json` v2 se generó y `--verify` pasó. El primer L2 PlanSpec `20260923-215037-18e6084e` también pasó antes de añadir comprobación de capa por handle. El fixture base se repitió en `20260923-215128-3d7534cf` y pasó. M5.1 es **partial**: estilos, anchos de línea, escalas y motor externo pendientes. El manifiesto es evidencia L2 local para este build; no es una autoridad de capacidades para binarios futuros. El DWG del usuario no se abrió ni modificó.

### Prompt de continuidad desde el decimosexto corte

Verifica SHA remoto, Git y AGENTS; conserva todos los L2. Completa capacidades M5 mediante fixtures sintéticos de cotas, bloques, HATCH, estilos y perfil métrico, con comparación geométrica/propiedades tras guardado. Resuelve M3 render-fence, presupuestos, fases; M4 grafo de cotas; M6 QA espacial y motor externo. M7/M8 mantienen cohortes reservadas, revisión humana y publicación solo al fork. No uses el DWG privado ni reescribas ensayos históricos.

## Decimoséptimo corte cota nativa y materialización DWG — 2026-09-23, en progreso

El decimosexto corte se publicó en `origin/codex/mcp-layer-semantics-l2` con commit local `1c1cdc5b` (verificar SHA completo remoto). Desde `codex/mcp-native-dimension-l2`, el primer L2 `target/mcp-isolated/20260923-215337-2f177641/report.json` creó una `DIMLINEAR` de 2.50, pero `save_verified` falló con `semantic_mismatch`: antes del guardado había siete entidades y tras reapertura había quince. No se reescribió ese reporte. La GUI aislada PID 12012 se cerró mediante `mcp_recover_isolated.py`, que guardó una copia `recovered-synthetic.dwg`; luego se verificó que el PID ya no existía.

El escritor genera para la cota un bloque gráfico anónimo `*D` de ocho entidades auxiliares. El gate de `save_verified` ahora clona el documento y materializa dicho bloque antes de comparar, conservando la igualdad estricta del manifiesto. Entrega manifiestos separados de origen, materializado y reabierto; el sello exige `audit=origen` y `materializado=reabierto`. Los sellos anteriores sin esos campos siguen verificables mediante su comparación original. El test Rust de cota comprobó medida 2.50 y bloque `*D` tras reapertura; las 40 pruebas `app::automation::tests`, 20 pruebas Python MCP y 16 del masterplan pasaron. El L2 final `target/mcp-isolated/20260923-220512-3ce4691c/report.json` pasó con cota nativa en `A-DIMS`, medida 2.50 antes/después de reapertura interna, origen de siete entidades y manifest materializado/reabierto de quince. Binario SHA-256 `93CB34814B68236C1E3E7BC4A7B6EF67158D34E390B4F4A952B21411CEE1B9B8`; DWG sintético SHA-256 `9F4734928A6FF35056EB9F5D69B3F667907E95DC15A0947D402206277EDAA582`. `SEAL.json` v2 se creó y `--verify` pasó. La captura PNG se inspeccionó localmente y muestra la cota separada del arco; permanece en `target` por contener UI y texto local. El L2 intermedio `20260923-220009-6cd45bb7` pasó con la cota superpuesta al arco, y el `20260923-220408-2e615a54` pasó con el contrato de tres manifiestos antes del desplazamiento visual; ambos se conservan.

M5.3 queda **partial**: se probó creación, medida, capa y persistencia interna de `DIMLINEAR`, sin demostrar vínculo asociativo a una referencia editada, `DIMALIGNED` ni interoperabilidad externa. M3/M4/M6 restantes y M7–M8 siguen abiertos. El DWG del usuario y el ensayo histórico permanecen intactos.

### Prompt de continuidad desde el decimoséptimo corte

Desde el mismo worktree, verifica `git status`, remotos, AGENTS, commit y SHA remoto de `codex/mcp-native-dimension-l2`; conserva todos los reportes, incluido el fallo inicial. Prueba asociatividad con una referencia sintética editable antes de declararla soportada; amplía `DIMALIGNED`, bloques/INSERT, HATCH, estilos, perfiles métricos y PlanSpec solo con capacidades medidas. Completa M3 con render-fence, tiempos por fase y presupuesto/reanudación; M4 con grafo de cotas; M6 con QA espacial y un motor externo identificado. Prepara M7 con cohortes reservadas y comparación controlada, y M8 con paquete, checks, PR/merge solo al fork y gates humanos pendientes. No abras ni modifiques el DWG del usuario, no incluyas artefactos privados y no publiques a `upstream`.

## Decimoctavo corte grafo dimensional acotado M4.3 — 2026-09-23, en progreso

El decimoséptimo corte `codex/mcp-native-dimension-l2` se publicó en el fork y `git ls-remote origin` coincidió con el SHA local `911bdcf8f13bfa0f8cb18917aae09a52a85891f3`; el árbol quedó limpio. La rama `codex/mcp-dimension-constraint-graph` añade a `planspec.py` un análisis determinista de cadenas de cotas x/y por eje y tipo de referencia. Usa el sentido geométrico para componer potenciales por nodo y reporta el ID de la cota y residual de un ciclo incompatible. Cada cota sigue validándose contra sus extremos antes del análisis; un conflicto de cadena aborta `dry_run` antes de CAD. El fixture de tres cotas acumula un residual de 0.0027 m aunque cada diferencia individual sea 0.0009 m; se detecta el conflicto `d-bc`. Al cambiar una cota a referencia `axis`, ya no se crea el falso conflicto con `face`. Las cotas `aligned` siguen verificadas individualmente, pero el grafo las informa `indeterminate`, sin adjudicarles una solución de cadena. Las 18 pruebas Python del masterplan pasaron; el código no abre GUI ni DWG.

M4.3 queda **partial L1**: el contrato actual identifica nodos y `reference_type` pero no caras de muros, ejes derivados, cadenas alineadas generales ni indeterminación por coordenadas ausentes. No se promueve a aceptación general. M3, M5 asociatividad, M6 externa y M7–M8 conservan los gates del corte anterior.

### Prompt de continuidad desde el decimoctavo corte

Verifica Git, AGENTS y SHA remoto de `codex/mcp-dimension-constraint-graph` antes de afirmar publicación. Preserva reportes sintéticos, sellos y el fallo de cota. Amplía el modelo PlanSpec con identidad topológica real para caras/ejes y relaciones de muros/aperturas, sin mezclar referencias. Ejecuta M5 asociatividad, DIMALIGNED, bloques/HATCH y propiedades; completa M3 render-fence, tiempos y presupuesto, M6 QA espacial e interoperabilidad externa, y M7/M8 solo con cohortes, motor y revisión adecuados. No abras el DWG del usuario ni publiques a upstream o archivos privados.

## Decimonoveno corte DIMALIGNED nativa — 2026-09-23, en progreso

El corte de grafo `codex/mcp-dimension-constraint-graph` quedó publicado en `origin` con SHA local/remoto `b38613d44092ecea6a322509584474ea0338a85a` y árbol limpio. La rama `codex/mcp-aligned-dimension-l2` amplió el fixture semántico aislado con `DIMALIGNED 46,0 49,4 48.5,3`, junto a la cota lineal anterior. El L2 `target/mcp-isolated/20260923-221050-30bf736d/report.json` pasó: dos handles `Dimension` distintos en `A-DIMS`, medidas 2.50/5.00 antes y después de reapertura interna, auditoría sin errores, salida de GUI y manifiestos de origen (8 entidades), materializado y reabierto (22 cada uno) iguales. La captura se inspeccionó localmente y el `SEAL.json` v2 se creó y verificó. SHA-256 del DWG sintético `F13CF7FB08491536ECD7BA732B659622B58A1C66260676073459707DABACBE8B`; binario `93CB34814B68236C1E3E7BC4A7B6EF67158D34E390B4F4A952B21411CEE1B9B8` (el mismo del corte 17, porque solo cambió el harness).

M5.3 continúa **partial**: ambas cotas nativas se crearon y persistieron internamente, pero no se ha movido una referencia asociada ni comparado toda su geometría/estilo en otro motor. No se tocó el DWG del usuario; PNG/DWG/reportes de prueba siguen ignorados en `target`.

### Prompt de continuidad desde el decimonoveno corte

Verifica Git, AGENTS, remoto y SHA de `codex/mcp-aligned-dimension-l2`. Sigue con la asociación real de cotas al editar referencias sintéticas; no infieras asociatividad de una medida estable. Prueba BLOCK/INSERT, HATCH y perfiles métricos con persistencia/propiedades, y amplía M4 con identidad topológica. Cierra M3 render-fence, fases y presupuesto, M6 QA y motor externo; prepara M7/M8 con cohortes reservadas, revisión humana y publicación solo al fork. Preserva todos los fallos y originales; no abras ni alteres el DWG del usuario.

## Vigésimo corte BLOCK/INSERT sintético — 2026-09-23, en progreso

La rama `codex/mcp-aligned-dimension-l2` se publicó en el fork con SHA local/remoto `d645c6a3cdf20c1ca623c99fd337802a0c55e625`, árbol limpio. Desde `codex/mcp-block-insert-l2` se creó una línea sintética, se seleccionó por handle y `-BLOCK MCP-SYMBOL 55,0` produjo la definición y su primera referencia. Dos comandos `INSERT` añadieron referencias en x=62 y x=66; la primera conservó escala 1 y rotación 30° (0.5235987756 rad), la segunda escala uniforme 2 y rotación 0. El L2 final `target/mcp-isolated/20260923-221502-2e129678/report.json` comparó nombre, posición, x/y scale y rotación de ambas por handle antes y después de reapertura interna; `matched_1e-6_internal`. El manifiesto contó tres Block Reference, de 12 entidades de origen a 26 materializadas/reabiertas por los bloques gráficos de las dos cotas. Auditoría y salida de GUI pasaron; PNG inspeccionado localmente y `SEAL.json` v2 verificado. DWG sintético SHA-256 `B8EBD7D77B7BE4FC0E23AED7B9290F900A5A1629A7C99F7692267FD8B3E9B8D7`; binario SHA-256 `93CB34814B68236C1E3E7BC4A7B6EF67158D34E390B4F4A952B21411CEE1B9B8`. Los L2 intermedios `20260923-221318-842aa91f` y `20260923-221357-295fe1f3` también pasaron, primero solo con definición y después con INSERT sin extracción correcta de propiedades; se conservan.

M5.4 queda **partial L2**: no se verificaron unidades de inserción, contenido geométrico de la definición ni motor externo. El DWG del usuario no se abrió y ningún artefacto de `target` entra a Git.

### Prompt de continuidad desde el vigésimo corte

Verifica Git, AGENTS y SHA remoto de `codex/mcp-block-insert-l2` antes de afirmar publicación. Completa unidades/definición de BLOCK, HATCH, estilos y perfil métrico en fixtures; prueba cotas asociadas mediante edición de referencias. Resuelve M3 render-fence, telemetría por fase y presupuesto/reanudación, M4 identidad topológica, M6 QA espacial e interoperabilidad. Reserva M7/M8 para cohortes y motor identificados; publica solo en el fork y mantiene intactos el DWG del usuario y ensayos históricos.

## Vigesimoprimer corte HATCH nativo — 2026-09-23, en progreso

`codex/mcp-block-insert-l2` quedó publicado en `origin` con SHA local/remoto `ca5fab46dd290bca452ab8fd1cebf511b7679fe1` y árbol limpio. Desde `codex/mcp-hatch-l2`, un batch MCP lanzó `HATCH`, seleccionó el punto interno `[23,1,0]` de la polilínea triangular cerrada y aplicó Enter. El L2 final `target/mcp-isolated/20260923-221923-92ebdb10/report.json` pasó: una entidad `Hatch` por handle en `A-HATCH`, una ruta de contorno, patrón no sólido con escala 1 y ángulo 0, flag `is_associative=true`; ruta, capa y propiedades coincidieron a 1e-6 tras reapertura interna. Auditoría sin errores, salida de GUI y `SEAL.json` v2 verificado. DWG sintético SHA-256 `8D69B7228D3F9360380B8F55689F8593DD07AE3C4FC8D04856E29C59A72A661E`; binario SHA-256 `93CB34814B68236C1E3E7BC4A7B6EF67158D34E390B4F4A952B21411CEE1B9B8`. Los L2 `20260923-221728-475df30e` y `20260923-221847-d8c87b21` pasaron antes de asignar la capa dedicada; se conservan. La captura final se inspeccionó localmente, pero su escala no permite acreditar legibilidad de patrón.

M5.5 queda **partial L2**: el flag asociativo no demuestra actualización al editar el contorno; faltan escala visual, grosores e interoperabilidad externa. M3/M4/M6 restantes y M7–M8 siguen abiertos. Los runs, PNG y DWG sintéticos permanecen en `target`; el DWG del usuario no se abrió.

### Prompt de continuidad desde el vigesimoprimer corte

Verifica SHA remoto de `codex/mcp-hatch-l2`, Git, AGENTS y estado del worktree. Prueba edición de contorno para HATCH y referencias para cotas antes de aceptar asociatividad; completa unidades de BLOCK, perfil métrico y estilos. Aborda M3 render-fence, telemetría por fase y presupuesto; M4 topología; M6 QA espacial/motor externo; M7/M8 cohortes y entrega con revisión humana. Conserva evidencia pasada y publica únicamente al fork; no abras ni modifiques el DWG del usuario.

## Vigesimosegundo corte actualización de cota asociada — 2026-09-23, en progreso

`codex/mcp-hatch-l2` quedó publicado en el fork con SHA local/remoto `44c99dbe1b285caaf422c60f879c34ed0a8aedd4` y árbol limpio. En `codex/mcp-dimension-association-l2`, un fixture sintético creó la línea de 70 a 72.5 y una `DIMLINEAR` coincidente de 2.50. La primera ejecución `target/mcp-isolated/20260923-222220-c05bb897/report.json` comprobó que `set_properties` con compare-and-set `/end/x:72.5→73.5` actualizó la cota a 3.50, pero el harness falló después porque esperaba el manifiesto anterior de dos cotas. Ese `failed` quedó intacto. La GUI aislada PID 15544 se recuperó con guardado separado y QUIT; el PID dejó de existir. La expectativa del fixture se corrigió a tres cotas.

El L2 final `target/mcp-isolated/20260923-222339-b12b6cdc/report.json` pasó con medida inicial 2.50, medida observada 3.50 tras editar la línea, y 3.50 tras guardar y reabrir internamente el DWG. Auditoría sin errores, quince entidades de origen y 37 materializadas/reabiertas, GUI salida, captura local inspeccionada y `SEAL.json` v2 verificado. DWG sintético SHA-256 `8E001D4F1411223DE175B9C11F87D2D2990F011ACC657966428B1517C39CA6FC`; binario SHA-256 `93CB34814B68236C1E3E7BC4A7B6EF67158D34E390B4F4A952B21411CEE1B9B8`. El L2 intermedio `20260923-222301-01bdc4ac` también pasó antes de registrar explícitamente la medida inicial.

M5.3 es **partial L2**: actualización asociativa demostrada en esa sesión sintética y medida persistida, pero no se ha editado la referencia después de reabrir para probar persistencia del vínculo; tampoco se ha probado motor externo o un caso topológico más complejo. HATCH asociativo y M3/M4/M6 restantes, M7–M8 siguen pendientes. El DWG del usuario y ensayos históricos permanecen intactos.

### Prompt de continuidad desde el vigesimosegundo corte

Verifica Git, AGENTS, remoto y SHA de `codex/mcp-dimension-association-l2`. En un nuevo fixture sintético reabre el DWG, edita otra vez la línea y verifica actualización de cota, guardado y cierre, sin tocar el sello anterior. Prueba también edición de contorno para HATCH y unidades de BLOCK. Continúa M3 render-fence/tiempos/presupuesto, M4 topología, M6 QA/external, M7 cohortes y M8 entrega con gates; publica solo en el fork y preserva todo fallo original.

## Vigesimotercer corte asociación tras reapertura y poda de `*D` — 2026-09-23, en progreso

`codex/mcp-dimension-association-l2` se publicó en `origin` con SHA local/remoto `142922a8ef2aa7db4ccb5226ab8177f8753e6770`, árbol limpio. Se añadió `mcp_dimension_reopen_smoke.py`, que lanza perfil y GUI sintéticos, crea línea+cota de 2.50, guarda DWG, reabre, cambia `/end/x` de 72.5 a 73.5 con compare-and-set, verifica medida 3.50, guarda otro DWG y cierra la GUI. El primer L2 `target/mcp-isolated/20260923-222625-assoc-205e7173/report.json` pasó la asociación pero mostró crecimiento de 10 a 18 entidades: el segundo DWG contenía un bloque gráfico `*D` antiguo además del nuevo. Se conserva como evidencia de defecto; no se promueve como cierre. Un intento de eliminar el bloque del documento vivo falló la prueba `viewport_dimension_paper_centerline_tracks_each_owning_space` por alterar el historial de undo; ese cambio se revirtió antes de publicar.

La corrección poda en la copia de guardado únicamente bloques anónimos de nombre `*D` seguido de dígitos que ningún `Dimension`/`Insert` referencie, con sus entidades propietarias. El documento vivo y el historial no se alteran. La nueva prueba Rust de regeneración verifica que no crecen bloques ni entidades y que un bloque anónimo ajeno `*DUSER` permanece. La prueba de undo pasó y `cargo test --lib` terminó con **1677 passed, 24 ignored, 0 failed**. Python masterplan 18/18 y automatización MCP 20/20 pasaron. El L2 final con el binario exacto `target/mcp-isolated/20260923-223855-assoc-07e4a0e0/report.json` pasó: antes y después de reabrir la cota midió 2.50, tras editar midió 3.50, ambos DWG verificaron el mismo manifiesto de diez entidades y la GUI salió. SHA-256 del binario `EE6493921D93E733500DEE850E71398467C588DFF2D577A37399EBAE99384557`; primer DWG `F596FCDAC44A04FF81F259AB7B34405270E6654E9A779C9BA73833493E25D98F`, segundo `F72949A9BC1F1CC2F782D33C57DB4B9E4832ADC7CD8AA0E17551DF0E158EF91E`. El L2 `20260923-223428-assoc-865c403c` también pasó antes de restringir la poda a nombres numéricos. Los runs quedan en `target` y no entran a Git.

M5.3 queda **partial scoped L2**: el vínculo asociativo sobrevivió a una reapertura interna en un caso lineal sintético y el guardado ya no acumula su imagen anterior. No se probaron relaciones a caras/ejes de muros ni motor externo. M3 render-fence/tiempos/presupuesto, M4 topología, M5 unidades/estilos, M6 QA externa y M7–M8 siguen abiertos. El DWG del usuario no se abrió ni alteró.

### Prompt de continuidad desde el vigesimotercer corte

Verifica Git, remotos, AGENTS y SHA remoto de `codex/mcp-dimension-association-reopen`; conserva el L2 con bloque huérfano y el fallo de prueba de undo como evidencia. Sigue con actualización asociativa de HATCH al editar contorno, unidades de BLOCK/INSERT y perfil métrico. Completa M3 render-fence, fases y presupuestos con checkpoint/reanudación, M4 topología y semántica de muros/aperturas, M6 QA e interoperabilidad externa y M7/M8 con cohortes reservadas, revisión humana y publicación solo al fork. Nunca abras el DWG del usuario ni incluyas artefactos privados.

## Vigesimocuarto corte HATCH asociado tras reapertura — 2026-09-23, en progreso

El corte 23 `codex/mcp-dimension-association-reopen` quedó publicado en el fork con SHA local/remoto `429b36900746d8b232e1d062bcf75827bb245e8b` y árbol limpio. Desde `codex/mcp-hatch-association-reopen` se añadió `mcp_hatch_reopen_smoke.py`: lanza GUI/perfil aislados, crea una PLINE triangular cerrada y un HATCH nativo por punto interior; comprueba que su ruta asociativa referencia el handle real de la PLINE. Guarda DWG sintético, reabre, edita mediante compare-and-set `/vertices/1/location/x` de 24 a 25, comprueba que los bordes del HATCH cambian con el vértice, guarda otro DWG, vuelve a abrir y compara la ruta y el contorno. La GUI sale y ambos `save_verified` reportan dos entidades (Polyline y Hatch), sin crecimiento de manifiesto.

El primer L2 `target/mcp-isolated/20260923-224352-hatch-528e4718/report.json` pasó la actualización; se conserva. El L2 ampliado `target/mcp-isolated/20260923-224419-hatch-1bf27e3a/report.json` pasó también la segunda reapertura: HATCH `65` referencia PLINE `64`, el borde inferior termina en (25,0) después de editar y ya no en (24,0), `is_associative=true` persiste, `gui_exited=true`. Binario SHA-256 `EE6493921D93E733500DEE850E71398467C588DFF2D577A37399EBAE99384557`; primer DWG SHA-256 `E2A84DD74F7D968B076E2D2A97B40996E4E5AC4917227EFA4065060A449B3008`; segundo `16B7F0936D18BA4AEC2D698AACBACC092907C102D12C2AEA772477E4A9F1C56F`. Los reportes y DWG sintéticos permanecen ignorados en `target`; el DWG del usuario no se abrió ni alteró.

M5.5 queda **partial scoped L2**: asociación geométrica y persistencia interna probadas para un contorno simple. No se acreditan múltiples islas/agujeros, grosor/legibilidad visual por escala ni interoperabilidad externa. M3 render-fence/tiempos/presupuesto, M4 topología, M5 unidades/estilos, M6 QA externa y M7–M8 siguen abiertos.

### Prompt de continuidad desde el vigesimocuarto corte

Verifica Git, AGENTS y SHA remoto de `codex/mcp-hatch-association-reopen`; conserva ambos L2. Continúa M3 con render-fence real, tiempos por fase y presupuesto/reanudación; investiga unidades de BLOCK/INSERT y perfil métrico, M4 topología y M6 QA espacial/interoperabilidad externa. M7 requiere cohortes de reserva y baseline comparable; M8 solo con gates cumplidos, revisión y publicación al fork. No abras ni modifiques el DWG del usuario, no toques el ensayo histórico y no publiques archivos privados ni upstream.

## Vigesimoquinto corte presupuesto y checkpoint MCP — 2026-09-23, en progreso

El corte HATCH `codex/mcp-hatch-association-reopen` se publicó en el fork y `git ls-remote origin` coincidió con SHA local `1e289b64bbf46b5a547c7be663d5da68fe02f602`; árbol limpio. Desde `codex/mcp-budget-checkpoint` se añadió `BudgetedRun`: recibe secuencia inmutable de comandos, sesión y `run_id`; guarda un diario JSONL append-only de encabezado, intención previa a cada mutación, finalización con revisión y pausa por límite de pasos o tiempo. Cada línea lleva SHA-256 y referencia al hash de la anterior. El diario no guarda texto CAD. Al reanudar, verifica plan, sesión, documento, revisión y cadena; omite pasos finalizados y reconcilia una intención pendiente consultando el mismo `request_id`, sin reenviarla. Una operación no reconciliable o una revisión externa detiene la secuencia. Cinco pruebas L1 cubrieron pausa/reanudación, intención pendiente, imposibilidad de conciliar, plan/revisión distinta y alteración del diario; `python -m unittest discover -s docs/automation -p 'test_*.py'` pasó 25/25.

El L2 `target/mcp-isolated/20260923-224853-budget-47fb3b93/report.json` pausó tras crear una línea, cerró el proceso MCP, inició otro con la misma GUI aislada, reanudó y creó solo la segunda línea. La consulta devolvió exactamente dos líneas; `save_verified` reabrió un DWG sintético con `Line:2`; la GUI salió. Hash del checkpoint `A77BE05A16F1F49C8C6F5B43565740CFCD531A0809052DD2DB8B0CF7D40F6A33`; del DWG `17B259FD6139418537135B09996D348EA3EC7056088D66B8BCA8BC6744CA1501`; del binario `EE6493921D93E733500DEE850E71398467C588DFF2D577A37399EBAE99384557`. Se verificaron los hashes en disco y que `target` está ignorado. No se abrió el DWG del usuario ni se reescribió evidencia previa.

M3.6 queda **partial scoped L2**: límites de pasos/tiempo y reanudación de comandos con cliente MCP reiniciado. Faltan presupuestos de tokens, capturas, bytes/reintentos y checkpoint del generador de PlanSpec; no se afirma recuperación de GUI perdida. M3 render-fence/tiempos CAD separados, M4 topología, M5 perfil/unidades, M6 motor externo y M7–M8 permanecen abiertos.

### Prompt de continuidad desde el vigesimoquinto corte

Verifica Git/AGENTS/SHA remoto de `codex/mcp-budget-checkpoint` y preserva el L2. Extiende presupuesto a tokens/capturas/bytes con identidad y cobertura reales; mide tiempos por fase y añade render-fence que corresponda al frame dibujado. Continúa M4 topología y M5 perfil métrico/unidades, M6 espacial y validación externa identificada. Reserva cohortes inéditas y revisión humana para M7; integra al fork en M8 solo tras checks y gates. No abras ni modifiques el DWG privado, no reescribas ensayos históricos y nunca publiques a upstream.

## Vigesimosexto corte fence de frame para captura — 2026-09-23, en progreso

El corte de presupuesto `codex/mcp-budget-checkpoint` quedó publicado en el fork con SHA local/remoto `8ecf8423be0e8ab8af2af3ec79bc451d462bef46` y árbol limpio. Desde `codex/mcp-render-fence`, el shader registra en la `Scene` la pareja geometría/cámara y el instante del viewport que realmente codificó. Una solicitud `capture` espera un frame posterior a esa solicitud y con la pareja de revisiones activa; después solicita el screenshot. Si cambia el documento/revisión o no aparece el frame en ocho segundos, falla; el temporizador sigue activo incluso con ventana minimizada. Al terminar, GUI, servidor MCP y cliente comprueban la marca `shader_encoded_frame` y las revisiones renderizadas. El cliente rechaza metadatos sin marca sin escribir PNG; la prueba L1 correspondiente pasó. El renderer no promete readback GPU independiente ni resuelve el recorte de UI.

`cargo check --lib -q` y `cargo build --bin OpenCADStudio -q` pasaron. `cargo test --lib -q` terminó **1677 passed, 24 ignored, 0 failed** antes del ajuste temporal; después de ese ajuste, `cargo check --lib -q`, build y L2 volvieron a pasar. Las 26 pruebas Python de automatización pasaron. El L2 inicial `target/mcp-isolated/20260923-230051-18e07852/report.json` pasó con el nuevo binario y el segundo `20260923-230130-20e33108` registró la marca explícita. El L2 final `target/mcp-isolated/20260923-230839-48d5f587/report.json` pasó tras exigir frame posterior a la solicitud: `rendered_geometry_revision=13`, `rendered_camera_revision=0`, iguales al PNG de 1024×506; tres entidades sintéticas visibles en el PNG local, auditoría y save_verified pasaron, GUI salió. SHA-256 del binario final `9590C13580CDA085AA9835EC986B03366A838B02B5B3F1B3C178A7311ACF292B`; PNG `b5cfa92540f65314e75ed5c7c52f44d921349b75d469bb53daa0d38cd1cf1953`; DWG sintético `C03A563CC680C63B55219B0041E0937966FDFC92D52F8E22C0E4C409C4A92BC5`. La imagen contiene UI/texto local y permanece ignorada en `target`. El chequeo global `cargo fmt --check` mostró diferencias preexistentes masivas y se detuvo sin formatear el repositorio; `git diff --check` pasó.

M3.3 queda **partial scoped L2**: existe un fence explícito de frame codificado y captura sintética con geometría visible. Faltan recorte limpio, prueba de cambios visuales por revisión, tiempos CAD/render separados y cuotas de tokens/capturas/bytes. M4–M8 siguen según el corte previo.

### Prompt de continuidad desde el vigesimosexto corte

Verifica remoto, Git y AGENTS; conserva ambos L2 de captura. Continúa M3 recorte/tiempos/presupuesto completo y M4 topología; mide unidades/estilos de M5 y QA externa de M6 con motor identificado. M7 necesita cohortes reservadas y revisión humana; M8 requiere checks e integración al fork, nunca a upstream. No abras el DWG privado ni alteres reportes históricos.

## Vigesimoséptimo corte sonda AutoCAD y corrección de cota — 2026-09-23, en progreso

El corte 26 `codex/mcp-render-fence` quedó publicado en el fork con SHA local/remoto `23414ac5091d042632abdd029f34c25e6b8233b4`. En `codex/mcp-autocad-independent-qa` se identificó AutoCAD Core Console 2025, versión `25.0.162.0.0`, SHA-256 `C2908EBE9EC5F0152C3E49E1DD30CF6AF093BB785276DCDC68AA3F1AFDF7D187`, como motor DWG externo. `mcp_autocad_probe.ps1` solo admite un DWG bajo `target/mcp-isolated` de este worktree, usa `/readonly` y perfil hijo aislado, toma hashes antes/después, censa entidades y referencias de cota, ejecuta `AUDIT _N` y conserva log/reporte en una carpeta nueva de `target/mcp-external`. Requiere PowerShell 7. Ningún DWG del usuario se abrió.

La primera prueba externa del DWG rico `20260923-222339-b12b6cdc` detectó el defecto que `save_verified` interno omitía: cota `79` esperada 3.5, pero AutoCAD leyó DXF 42 = 0.0 y los dos puntos DXF 13/14 en (73.5,0). `AUDIT` había informado 0 errores/0 correcciones. El reporte `target/mcp-external/20260923-232151-autocad-45245276/report.json` conserva el fallo; el caso mínimo `20260923-232505-autocad-d860fcf0` reprodujo los puntos colapsados. La inspección del `DIMASSOC` mediante reactor mostró osnap END=1 para ambos extremos, marcadores 0/1 correctos y puntos de pista distintos. En el cliente MCP, `mcp_dimension_reopen_smoke.py` registró puntos 70/72.5 antes y después de la primera reapertura y 70/73.5 tras editar; el problema era de interpretación externa, no de los puntos que la GUI exponía. Se conservaron además los intentos diagnósticos intermedios y los logs.

La asociación de un `LINE` con marcador 0 ahora usa osnap `START_POINT`=13; el marcador 1 conserva `END`=1. El fixture mínimo L2 final `target/mcp-isolated/20260923-233108-assoc-2fc162a9/report.json` pasó con GUI salida y binario SHA-256 `A8AF80D48B6AE4C01C5D6514FD0522A025C9C38A4014E07A6927A42D90707508`; su segundo DWG tiene SHA-256 `1B3C9B21939D55056147F60D711EFAE78D5A35F0FC3C2ECCDEBB02EF62C563C6`. AutoCAD confirmó DXF 13=(70,0), 14=(73.5,0) y 42=3.5, con comparación exacta a 1e-6 en `target/mcp-external/20260923-233126-autocad-8da59c03/report.json`.

Referencias de formato: Autodesk [DIMASSOC DXF](https://help.autodesk.com/cloudhelp/2016/ENU/AutoCAD-DXF/files/GUID-C0B96256-A911-4B4D-85E6-EB4AF2C91E27.htm) enumera `START_POINT=13`, `END=1` y el marcador 91; Autodesk [Common Dimension Group Codes](https://help.autodesk.com/cloudhelp/2016/ENU/AutoCAD-DXF/files/GUID-EDD54EAC-A339-4EBA-AEA6-EC8066505E2B.htm) define 42 como medida real. La hipótesis de que AutoCAD seleccionaría el extremo equivocado con `END` y marcador 0 quedó verificada por el censo previo/posterior, no se dedujo solo de la documentación.

El fixture semántico completo L2 `target/mcp-isolated/20260923-233257-d4367988/report.json` pasó con el mismo binario; DWG sintético SHA-256 `EABCDCA9EE15B99BDF52DC974D05634A401A28A7882F6D45160BD5ADFFFB3456`. La sonda L4 `target/mcp-external/20260923-233345-autocad-10a904aa/report.json` registró en AutoCAD medidas 2.5/5.0/3.5 de las tres cotas, las dos referencias de la cota asociada correctas, tres INSERT, un HATCH, 14 entidades en Model, `AUDIT` 0/0 y SHA del DWG sin cambios. El censo interno de 15 incluye una línea en definición de bloque, por eso no se exige igualdad directa con las 14 entidades de Model. La sonda devolvió `partial_abnormal_exit`: después de `QUIT`, Core Console no terminó dentro de 40 s y se detuvo únicamente su proceso propio. Esto deja M6.6 **partial L4**, aun con la comparación semántica acotada correcta. Dos intentos de usar ActiveX en Core Console no devolvieron medida; DXF 42 es el oráculo observado en estas ejecuciones.

Las 26 pruebas Python de automatización pasaron; `cargo build --bin OpenCADStudio`, `cargo test --lib` (1677 passed, 24 ignored, 0 failed) y `git diff --check` pasaron. El SHA de publicación se completa al cerrar este corte. Los reportes DWG/PNG/perfiles/logs siguen ignorados en `target`; los anteriores no se reescribieron. M3 cuotas y fases, M4 topología, M5 unidades/estilos, M6 QA espacial y externa completa, M7 cohortes reservadas/L3/L5 y M8 integración final siguen abiertos.

### Prompt de continuidad desde el vigesimoséptimo corte

Reverifica Git, `AGENTS.md`, remotos y SHA local/remoto de `codex/mcp-autocad-independent-qa`; conserva todos los reportes externos, en especial el fallo semántico inicial y la terminación anómala. Investiga el cierre de AutoCAD Core Console con fixture sintético y amplía M6.6 a geometría, HATCH/INSERT y propiedades, sin promover el gate por `AUDIT` solamente. Continúa M3 con tiempos de CAD/render y presupuestos completos; M4 con identidades de caras/ejes y relaciones de apertura; M5 con unidades de INSERT, perfil métrico y estilos; M6.1–M6.5 con QA espacial y visual. Prepara M7 con cohortes inéditas y evaluación comparativa, y M8 con paquete, checks y revisión humana. No abras ni alteres el DWG del usuario o los ensayos históricos, no publiques archivos privados ni envíes a upstream.

## Vigesimoctavo corte QA geométrica previa M6.1 — 2026-09-23, en progreso

El corte 27 `codex/mcp-autocad-independent-qa` quedó publicado en `origin` con SHA local/remoto `2dfede68440c991654789f50c77f494f0ba0b114` y árbol limpio. En `codex/mcp-planspec-geometry-qa`, `dry_run` añade un `geometry_qa` determinista calculado antes de CAD con aritmética decimal sobre las líneas y círculos de PlanSpec v1. Informa líneas idénticas aunque inviertan extremos, círculos de igual centro/radio, tramos colineales solapados, cruces interiores, uniones T y extremos de línea con grado uno. El estado `review_required` señala candidatos; no infiere si un cruce es un error arquitectónico ni si un extremo abierto pertenece a un contorno, porque el contrato todavía no incluye identidad de muros/contornos. La detección de línea degenerada y radio no positivo sigue en el validador básico. El compilador conserva la misma secuencia y hash de comandos.

Los fixtures L0 sembraron duplicado inverso, solape, círculo duplicado, cruce y unión T y pasaron; se verificó que invertir el orden de nodos y líneas no cambia el informe. `python -m unittest discover -s docs/automation/masterplan -p 'test_*.py'` pasó 20/20 y `python -m unittest discover -s docs/automation -p 'test_*.py'` pasó 26/26. `git diff --check` pasó. M6.1 queda **partial L0**: falta identidad de contorno, intersecciones con círculos y entidades CAD reales, y reglas específicas de colisión; el reporte no sustituye QA de persistencia/visual. M3/M4/M5/M6 restante y M7–M8 conservan sus gates. El DWG privado y las evidencias anteriores permanecen intactos.

### Prompt de continuidad desde el vigesimoctavo corte

Verifica SHA remoto, Git y `AGENTS.md` antes de asumir publicación de `codex/mcp-planspec-geometry-qa`. Extiende PlanSpec con identidad topológica explícita de muros, contornos y aperturas antes de clasificar cruces o extremos como errores; incorpora QA por categoría y separa bounds arquitectónicos de anotaciones. Reproduce las validaciones en un DWG sintético y amplía M6.6 en AutoCAD con cierre limpio o límite declarado. Completa M3 fases/recorte/cuotas, M5 unidades/estilos, M7 cohortes reservadas y M8 entrega/revisión por gates. Conserva todos los resultados fallidos, no abras el DWG del usuario ni publiques archivos privados o upstream.

## Vigesimonoveno corte cierre limpio y atributos L4 — 2026-09-23, en progreso

El corte 28 `codex/mcp-planspec-geometry-qa` quedó publicado en el fork con SHA local/remoto `563675f560cc3ff4e8ca405e55abdb3664c60c0a` y árbol limpio. Desde `codex/mcp-autocad-console-exit`, la sonda AutoCAD añade `_N` después de `_.QUIT` para responder el descarte pendiente del documento abierto en lectura. Con el DWG rico sintético del corte 27, `target/mcp-external/20260923-234134-autocad-f9c866d3/report.json` registró `forced_termination=false`, `exit_code=0`, `AUDIT` 0/0, las tres cotas correctas y SHA de entrada sin cambio; duración de la ejecución de consola observada aproximadamente 2.4 s. El caso mínimo `target/mcp-external/20260923-234145-autocad-70196080/report.json` repitió cierre limpio, medida 3.5 y puntos 70/73.5. Los reportes previos con terminación forzada no se reescribieron.

La sonda comparó después propiedades externas de HATCH y dos INSERT por handle con el reporte L2 cuyo hash de DWG coincide. El primer intento `target/mcp-external/20260923-234308-autocad-438f2d54/report.json` dio `semantic_mismatch` exclusivamente en ángulo de HATCH porque la sonda leyó DXF 50, que corresponde a la rotación de INSERT; el ángulo de patrón HATCH es DXF 52 según Autodesk. Ese reporte fallido se conserva. Tras corregir el lector, `target/mcp-external/20260923-234328-autocad-5febff9a/report.json` pasó: 23/23 propiedades HATCH/INSERT y 3/3 medidas de cotas coinciden a 1e-6, 14 entidades de Model censadas, `AUDIT` 0/0, `forced_termination=false`, `exit_code=0`, SHA-256 del DWG antes/después `EABCDCA9EE15B99BDF52DC974D05634A401A28A7882F6D45160BD5ADFFFB3456`. El archivo era exclusivamente sintético y permaneció intacto.

M6.6 sigue **partial scoped L4**: la sonda no compara aún el contenido geométrico de la definición de BLOCK ni los bordes de HATCH, tampoco edita en AutoCAD una fuente para comprobar vínculo asociativo, ni cubre estilos, unidades de inserción, visual, versiones múltiples o planos diversos. M3/M4/M5/M6 restantes y M7–M8 mantienen sus gates. Verificar SHA de publicación al cerrar este corte.

### Prompt de continuidad desde el vigesimonoveno corte

Verifica SHA local/remoto de `codex/mcp-autocad-console-exit`, Git y `AGENTS.md`; conserva los reportes fallidos de la sonda y los previos con timeout. Amplía M6 con cotejo externo de geometría de bloque/HATCH y edición asociativa sintética si puede hacerse sin alterar la entrada; aborda M6.2–M6.5 y topología M4. Completa M3 tiempos por fase, recorte y cuotas, M5 perfil métrico/unidades/estilos, prepara M7 con reserva inédita y revisión L3/L5, y M8 con checks/PR/merge solo al fork cuando los gates se cumplan. El DWG del usuario y ensayos históricos siguen fuera de alcance; nada a upstream ni archivos privados en Git.

## Trigésimo corte tiempos internos de guardado y captura — 2026-09-23, en progreso

El corte 29 `codex/mcp-autocad-console-exit` se publicó en `origin` con SHA local/remoto `1b6ca19abd3e82257eef56712195c2969b1252ed` y árbol limpio. Desde `codex/mcp-save-phase-timing`, `save_verified` devuelve duraciones medidas con `Instant` dentro de la GUI: auditoría, preparación/materialización, escritura, hash/estructura, reapertura, comparación y total. La captura devuelve tiempo de espera del frame codificado, desde frame hasta callback de screenshot, codificación/escritura PNG y total. El servidor MCP transporta esos campos de metadatos y el cliente los registra sin Base64. El harness mide además, con reloj monótono del cliente, la latencia completa de script, guardado y captura; no confunde esas latencias con trabajo del motor CAD.

La prueba Rust focalizada `save_verified_writes_reopens_hashes_and_matches_manifest` pasó 1/1 y las 26 pruebas Python de automatización pasaron. `cargo build --bin OpenCADStudio` pasó. El L2 `target/mcp-isolated/20260923-235229-2be32518/report.json` pasó con GUI salida, auditoría y DWG sintético reabierto. Binario SHA-256 `B72F46C2C57FC68E6F73990383D0329C9FA3BFEF0FD4C9B1BA5E1A545348A40D`; DWG `C03A563CC680C63B55219B0041E0937966FDFC92D52F8E22C0E4C409C4A92BC5`; PNG `8AA337130EDB284729F788F24952C7C4E610DA19B438D345C1EC911E31040B4D`. En esta sola muestra, script RPC 362.224 ms; save RPC 60.245 ms frente a total GUI 31.1538 ms (escritura 14.4203, reapertura 13.8597); captura RPC y artefacto 1249.533 ms frente a total GUI 1217.7799 ms (espera de frame 129.6046, callback 6.7715, codificación/escritura 1081.4038). Son observaciones de un fixture, no p50/p95 ni tiempo de ejecución de cada comando CAD. La captura sigue incluyendo UI y permanece ignorada en `target`.

La primera suite Rust completa se ejecutó con `TEMP` dentro de `target`; dos pruebas de ciclo de vida fallaron porque su fixture define como externo todo archivo temporal fuera del repositorio. No se interpretaron como fallos del código de temporización. Con `TEMP` en una carpeta nueva de `Downloads`, las 14 pruebas de `app::control::tests` pasaron. Una suite completa posterior dio 1676 passed/1 failed/24 ignored por `io::pdf_export::tests::atomic_write_replaces_an_existing_pdf`, error transitorio de reemplazo Windows 1175; la prueba focalizada pasó al repetirla. La última suite completa, bajo el mismo `TEMP` externo, pasó **1677 passed, 24 ignored, 0 failed**. No se borraron las carpetas temporales ni se modificó AppData. Estas tres ejecuciones se declaran por separado, no como un único run limpio.

M3 tiempos queda **partial scoped L2**: ahora se distingue tiempo interno de guardado/captura y latencia RPC, pero faltan tiempos internos de comandos CAD, distribución multirun, recorte limpio, cuotas de tokens/capturas/bytes y costo facturado. M4–M8 mantienen sus gates; el DWG privado y la evidencia histórica no se tocaron. Verificar SHA de publicación al cerrar este corte.

### Prompt de continuidad desde el trigésimo corte

Verifica Git, `AGENTS.md` y SHA local/remoto de `codex/mcp-save-phase-timing`. Mide fases internas de ejecución CAD, repite fixtures para p50/p95 si el gate de rendimiento lo exige y optimiza la codificación PNG solo con calidad equivalente. Continúa M3 recorte/cuotas y M4 topología, M5 perfil/unidades, M6 QA externa geométrica. El usuario confirmó que puede aportar seis imágenes reservadas autorizadas para M7; esperarlas antes del run L3/L5, manteniendo el DWG privado fuera de alcance. M8 requiere checks, PR/merge al fork y revisión humana apropiada. No publiques datos privados ni envíes a upstream.

## Trigésimo primer corte presupuesto durable de capturas — 2026-09-24, en progreso

El corte 30 `codex/mcp-save-phase-timing` quedó publicado en el fork con SHA local/remoto `d0468536885fbc353e5547841c0d9639d3b8749e` y árbol limpio. Desde `codex/mcp-capture-budget`, `CaptureBudget` escribe un diario JSONL append-only con cadena SHA-256, identidad de run/sesión/documento, límites de número de capturas y bytes, revisión de geometría/cámara e intención durable antes de invocar `capture_artifact`. Al reanudar, devuelve un artefacto finalizado únicamente si tamaño y hash siguen coincidiendo; no repite la captura. Una intención sin finalización, incluso cuando apareció el PNG tras perderse la respuesta, bloquea toda captura posterior de ese run. El diario solo guarda metadatos y nombre local del PNG.

Cuatro tests L1 cubrieron agotamiento y reinicio, exceso de bytes, respuesta perdida y alteración de diario/revisión. La suite Python de automatización pasó **30/30** y `git diff --check` pasó. El primer L2 `target/mcp-isolated/20260924-000642-32c6245d/report.json` pasó, pero la nueva envoltura usó 1600 px por defecto mientras el cliente histórico usaba 1024 px; se preservó esa ejecución para comparación y se corrigió el valor por defecto a 1024. El L2 final `target/mcp-isolated/20260924-000857-cbe44315/report.json` pasó con 3 entidades sintéticas, auditoría 0/0, guardado verificado, captura 1024×506 de 37,403 bytes, segunda captura rechazada por cuota, mismo artefacto conciliado sin nuevo RPC y GUI salida. SHA-256 binario `B72F46C2C57FC68E6F73990383D0329C9FA3BFEF0FD4C9B1BA5E1A545348A40D`; DWG sintético `C03A563CC680C63B55219B0041E0937966FDFC92D52F8E22C0E4C409C4A92BC5`; PNG `F7BDC7BC75D052C82D09C6678E90406CC0D96399774CB06441A1129DEC2CFD05`; diario `675FB32ECFC3D6DD32D1412EB41B1AF1B40B81A3996E38BEA141625FA74C8731`.

El límite de bytes se comprueba después de recibir un PNG de tamaño no conocido de antemano: una captura individual puede superar el máximo, se conserva como evidencia, queda registrada como excedida y bloquea nuevos intentos; no se afirma un tope estricto previo a la codificación. M3.6 queda **partial scoped L1/L2**: falta presupuesto de tokens con cobertura real y su conciliación; el recorte de UI y la distribución de latencias también siguen pendientes. M4–M8 no se promueven por este corte. El DWG privado, los reportes anteriores y `upstream` no se tocaron. Verificar SHA de publicación al cerrar este corte.

### Prompt de continuidad desde el trigésimo primer corte

Verifica `AGENTS.md`, Git/remotos y SHA local/remoto de `codex/mcp-capture-budget`; conserva también el L2 de 1600 px. Continúa M3 con tiempo interno de comandos CAD, presupuesto de tokens medidos y recorte de UI; desarrolla M4 con identidades topológicas explícitas y M5 con perfil de unidades/estilos. Amplía M6 QA geométrica y externa sobre fixtures sintéticos. El usuario aportará aquí seis imágenes inéditas autorizadas para M7: espera esas entradas para L3/L5 y evita el DWG privado. M8 exige checks, integración al fork y revisión humana por gate; nunca publiques datos privados ni envíes a `upstream`.

## Trigésimo segundo corte duración de operaciones CAD — 2026-09-24, en progreso

El corte 31 `codex/mcp-capture-budget` se publicó en el fork con SHA local/remoto `ba92fdeb8dabfc1f667685daa38863a32dd762b3` y árbol limpio. Desde `codex/mcp-command-operation-timing`, la GUI incorpora `timings.scope=gui_operation_elapsed` y `total_ms` en la respuesta terminal de cada operación, medidos desde aceptación hasta asentamiento con reloj monótono del mismo proceso. El journal de `run_script` conserva ese dato por paso y el resultado compacto devuelve la lista `command_timings_ms` y su alcance. Una respuesta reconciliada por `request_id` conserva el valor original, no vuelve a ejecutar ni vuelve a medir la operación.

`cargo build --bin OpenCADStudio -q` pasó; `python -m unittest discover -s docs/automation -p 'test_*.py'` pasó 30/30 y `git diff --check` pasó. El L2 `target/mcp-isolated/20260924-001352-a4e2031a/report.json` pasó con tres entidades, AUDIT 0/0, DWG reabierto, captura 1024×506 y salida de GUI. Binario SHA-256 `450C6BE26C4B00119A829BAF015D0F16E6065D453E2DF1D1DC8246E43142FF5C`; DWG sintético `C03A563CC680C63B55219B0041E0937966FDFC92D52F8E22C0E4C409C4A92BC5`. En esa única muestra, las tres operaciones de PlanSpec duraron 3.5309, 1.3123 y 1.8836 ms dentro de GUI (suma 6.727 ms), y la llamada RPC del script duró 499.644 ms. La duración GUI incluye el ciclo de eventos y ejecución; no representa CPU puro del kernel ni separa el costo restante en transporte, journal y polling. La captura demoró 1463.7856 ms en GUI y 1543.4 ms extremo a extremo. No se deriva p50/p95 de un caso.

La primera suite Rust completa dio **1676 passed, 1 failed, 24 ignored**: `saving_over_an_existing_drawing_succeeds` recibió error Windows 1175 al reemplazar un DXF temporal. La prueba focalizada pasó 1/1 al repetirla; una segunda suite completa dio **1677 passed, 0 failed, 24 ignored** con el mismo `TEMP` externo en `Downloads`. El fallo original no se borra ni se presenta como parte del run limpio. Los cuatro warnings de compilación preexistentes siguen fuera del alcance. M3.2 sigue **partial scoped L2** hasta separar espera del modelo, transporte, GUI/kernel y render con más muestras. M3 presupuesto de tokens y recorte de UI, M4–M8 siguen abiertos. No se tocaron DWG privado ni evidencias anteriores. Verificar SHA de publicación al cerrar el corte.

### Prompt de continuidad desde el trigésimo segundo corte

Verifica `AGENTS.md`, Git/remotos, suite y SHA local/remoto de `codex/mcp-command-operation-timing`; preserva los L2 previos. Profundiza M3 en presupuesto de tokens con identidad y cobertura, transporte/polling por fase, recorte limpio de UI y latencias con muestras suficientes. Define relaciones topológicas de M4, perfil métrico/estilos/unidades de M5 y QA externa M6 sobre fixtures sintéticos. Para M7 espera las seis imágenes inéditas autorizadas que el usuario aportará aquí y su revisión L3/L5; no uses el DWG privado. Integra M8 solo tras gates, checks y destino `origin` del fork, nunca `upstream`.

## Trigésimo tercer corte contornos explícitos de PlanSpec — 2026-09-24, en progreso

El corte 32 `codex/mcp-command-operation-timing` quedó publicado en el fork con SHA local/remoto `128e382bfd7f1f865cb1e271f7411fd110bd41d3` y árbol limpio. Desde `codex/mcp-planspec-contours`, `planspec-2.schema.json` introduce `topology.contours`: cada contorno tiene ID propio, rol `exterior` o `room`, procedencia y secuencia de IDs de líneas existentes. El validador Python comprueba cierre en el orden declarado aunque una línea esté invertida, vértices únicos, área no nula y autointersección, antes de emitir comandos CAD. PlanSpec v1 sigue admitido sin ese campo; los comandos existentes mantienen la compilación determinista. El reporte topológico v1 dice `unavailable` en vez de inventar habitaciones.

Los 22 tests del masterplan y 30 de automatización pasaron. El fixture versionado `fixtures/synthetic-contour.planspec.json` representa un rectángulo de 4×3 m, área explícita 12 m²; el L2 `target/mcp-isolated/20260924-002607-024d1afa/report.json` pasó con cuatro comandos, handles `64`–`67`, capa 0, AUDIT 0/0, manifiesto reabierto `Line:4`, captura con fence y cierre de GUI. SHA-256 fixture `27FE320197AAD899A8E7E69BCB2CE77E59036740C046926F90081AE1324BA81B`, comandos `2F58606E8C48DE58A2C17B36DF8091E3969464239207EE61F594BCF12B48BF03`, binario `450C6BE26C4B00119A829BAF015D0F16E6065D453E2DF1D1DC8246E43142FF5C`, DWG sintético `F5E37C00A6CFB68177FF9A04C7A150537CAB31CB5474944009D9AD3DBFB4B33A`. El ejecutor no modifica ni lee el DWG del usuario.

M4 queda **partial L1/L2**: contorno cerrado es identidad geométrica explícita, pero no equivale aún a muro con espesor, unión, puerta/ventana, recinto clasificado por uso ni relación de cotas con caras/ejes; tampoco hay cotejo externo de la geometría del contorno. La geometría de salida de este fixture fue verificada por manifiesto y handles internos, no por AutoCAD. M3 restante, M5/M6 restante y M7–M8 conservan sus gates. Verificar SHA remoto al cerrar el corte.

### Prompt de continuidad desde el trigésimo tercer corte

Verifica `AGENTS.md`, Git/remotos, árbol y SHA local/remoto de `codex/mcp-planspec-contours`; conserva todos los ensayos. Extiende v2 con muros/espesor, aperturas y referencias topológicas de cotas con procedencia, sin inferirlos de líneas abiertas. Comprueba geometría y propiedades externamente con fixtures sintéticos. Completa M3 presupuesto de tokens, recorte y tiempos por fase con distribuciones; M5 unidades/estilos y M6 QA espacial y visual. Espera las seis imágenes inéditas autorizadas que el usuario aportará para M7 L3/L5; no uses el DWG privado. Integra M8 al fork solo tras gates y revisión; nada a `upstream`.

## Trigésimo cuarto corte perfil métrico DWG — 2026-09-24, en progreso

El corte 33 `codex/mcp-planspec-contours` quedó publicado en el fork con SHA local/remoto `4de7642b99c120d56b16c8a0488192ba07df5e07` y árbol limpio. En `codex/mcp-metric-dwg-profile`, la sonda externa de AutoCAD se amplió para registrar `INSUNITS`. Abrió el DWG sintético v2 del corte 33, cuyo PlanSpec declara metros, y leyó **INSUNITS=4 (milímetros)** en `target/mcp-external/20260924-002847-autocad-f103250c/report.json`; AUDIT 0/0 y SHA intacto no habían detectado esa divergencia. Según [Autodesk INSUNITS](https://help.autodesk.com/cloudhelp/2025/ENU/AutoCAD-Core/files/GUID-A58A87BB-482B-4042-A00A-EEF55A2B4FD8.htm), 4 es milímetros y 6 metros; la variable determina el escalado automático al insertar bloques/imágenes/referencias. El reporte original no se modifica.

El compilador de PlanSpec v1/v2 ahora antepone `SETVAR INSUNITS 6` a la geometría y reporta `dwg_unit_profile`; el hash de comandos nuevos cambia deliberadamente. El contrato de planos sigue expresado en metros. La sonda AutoCAD acepta una expectativa `ExpectedInsunits` y falla si no coincide. El L2 v2 `target/mcp-isolated/20260924-003000-cb8938d5/report.json` pasó con cinco pasos, cuatro entidades, AUDIT 0/0, DWG reabierto y GUI salida. SHA del DWG nuevo `303A692C2F8EDE79100A2245D1B0033C62AFF1DE037677BF2D6C69EC13F05A5A`, comandos `11D56BAC78AD46554AD8DCB41F6954CBD8F369530B6C5624113B45C52E568B61`, binario `450C6BE26C4B00119A829BAF015D0F16E6065D453E2DF1D1DC8246E43142FF5C`. AutoCAD confirmó `INSUNITS=6`, cuatro LINE, AUDIT 0/0, salida código 0, DWG intacto en `target/mcp-external/20260924-003015-autocad-a17c3a6f/report.json`. La sonda con expectativa 6 contra el DWG anterior devolvió exit 1, `unit_match=false` y reporte preservado `target/mcp-external/20260924-003031-autocad-371b771b/report.json`. El L2 v1 `target/mcp-isolated/20260924-003047-5f383192/report.json` también pasó con el nuevo prefijo, tres entidades y GUI salida.

Las suites Python pasaron: masterplan 22/22, automatización 30/30; `git diff --check` pasó. M5 unidades queda **partial L2/L4**: se acreditó la variable de inserción métrica en este fixture y motor, no escala de bloques externos, formato de cotas, estilos, impresión ni variedad de archivos. El DWG privado, ensayos anteriores y `upstream` permanecen intactos. Verificar SHA remoto al cerrar el corte.

### Prompt de continuidad desde el trigésimo cuarto corte

Verifica `AGENTS.md`, Git/remotos y SHA local/remoto de `codex/mcp-metric-dwg-profile`; conserva el fallo de unidad del DWG antiguo y el run negativo. Continúa M5 con escala de inserción de bloques, estilo de cota, texto y ancho de línea medidos externamente, y M6 con QA geométrica/visual. Completa M3 presupuestos de tokens, recorte y telemetría de fases; extiende M4 muros, aperturas y cotas por referencias explícitas. Espera las seis imágenes inéditas autorizadas para M7 y la revisión L3/L5 del usuario; no uses su DWG privado. Publica exclusivamente en el fork, con gates y SHA verificado, nunca en `upstream`.

## Trigésimo quinto corte contrato estricto de captura viewport — 2026-09-24, en progreso

El corte 34 `codex/mcp-metric-dwg-profile` quedó publicado en el fork con SHA local/remoto `0c388e14ec5fe3262027d104632e72d4c54cdb67` y árbol limpio. Desde `codex/mcp-viewport-capture-contract` se cerró la sustitución silenciosa: cuando se pide `scope=viewport`, la GUI falla si faltan bounds o el rectángulo está vacío; el servidor MCP compara el alcance efectivo con la solicitud antes de entregar el PNG; el cliente exige `viewport` antes de escribir el archivo. Un test Python simula una respuesta `window` y confirma que no aparece el PNG.

`cargo check --lib -q` y `cargo build --bin OpenCADStudio -q` pasaron. La suite Python de automatización pasó 31/31. El L2 `target/mcp-isolated/20260924-003828-89a426c4/report.json` pasó con GUI salida, DWG sintético reabierto y PNG `region=viewport`, 1024×506, fence de revisión coincidente. SHA-256 binario `84926694CD1F7EEC400269393F1DEB2C6EA2D8A06012A59F2F0C881560456851`; DWG `2722DAD74688FE6E9CC415278FAB31B3AA48465A3451843EC9A46611EA954D2E`; PNG `7D9D6111CDBE15BEE93F88202B8E70A349ED96933E31082093A51431F5A00746`. Se inspeccionó visualmente un PNG L2 anterior del mismo binario: incluye geometría y también barra superior, navegación y línea de comandos superpuestas dentro del viewport. Por ello M3.3 queda **partial scoped L2**; el alcance efectivo se garantiza, pero todavía no hay imagen limpia de overlays ni recorte por coordenadas CAD. Las capturas permanecen ignoradas en `target`. El DWG privado no se abrió.

### Prompt de continuidad desde el trigésimo quinto corte

Verifica `AGENTS.md`, Git/remotos y SHA local/remoto de `codex/mcp-viewport-capture-contract`; conserva los PNG y reportes anteriores. Resuelve el recorte limpio de overlays o un render offscreen reproducible, con fence de revisión y evidencia visual L2. Avanza M3 tokens medidos/latencias; M4 muros/aperturas/cotas; M5 estilos/escala; M6 QA externa completa. Espera las seis imágenes inéditas autorizadas para M7 L3/L5 y revisión humana. No abras el DWG privado ni publiques archivos privados o `upstream`.

## Trigésimo sexto corte geometría básica externa — 2026-09-24, en progreso

El corte 35 `codex/mcp-viewport-capture-contract` quedó publicado en el fork con SHA local/remoto `f85513c1a9878fea5403e6b70825857a059a2ede` y árbol limpio. En `codex/mcp-autocad-line-geometry`, la sonda AutoCAD registra DXF 10/11 de cada LINE y DXF 10/40 de CIRCLE. Solo toma como oráculo esperado un fixture sintético de nombre permitido cuyo SHA coincida con el reporte L2 y cuyo DWG tenga el SHA observado; compara ID PlanSpec→handle, tipo, capa, coordenadas y radio con tolerancia 1e-6, aceptando una LINE invertida. Una diferencia produce `semantic_mismatch` y exit 1. No se usa el DWG privado ni se ejecutan comandos de edición sobre la entrada.

El primer intento de la sonda falló antes de generar reporte por una suma de arreglos mal parentizada en PowerShell; su directorio/log local se conservó. El siguiente reporte `target/mcp-external/20260924-004204-autocad-7d231c6a/report.json` produjo un falso `semantic_mismatch`: PowerShell evaluó la combinación `-and`/`-or` de izquierda a derecha. Se preservó, se agruparon explícitamente las dos orientaciones y `target/mcp-external/20260924-004332-autocad-ec9dbcfe/report.json` pasó 4/4 líneas del contorno, con `INSUNITS=6`, AUDIT 0/0, salida código 0 y SHA de DWG sin cambio. Tras añadir círculo, `target/mcp-external/20260924-004412-autocad-0e7e5f95/report.json` pasó 2/2 líneas y 1/1 círculo del fixture simple; `target/mcp-external/20260924-004423-autocad-f1c8cafe/report.json` repitió 4/4 líneas del contorno. Una copia sintética bajo `target/mcp-isolated/20260924-line-negative-8b6b6d32` alteró solo el mapa de handle `wall-a` en su reporte copiado; `target/mcp-external/20260924-004447-autocad-0f7231ad/report.json` rechazó 1/4 comparaciones con `semantic_mismatch`, exit 1, conservando el DWG original y la copia.

M6.6 sigue **partial scoped L4**: ahora hay cotejo externo de primitivas LINE/CIRCLE del PlanSpec, cotas y propiedades HATCH/INSERT en fixtures distintos; no se ha cotejado aún el contenido geométrico de BLOCK, bordes HATCH ni editado una asociación dentro de AutoCAD. Faltan estilos, escala, visual y más versiones. Los reportes L4 quedan ignorados en `target`; `git diff --check` y verificación de publicación se completan antes de cerrar este corte.

### Prompt de continuidad desde el trigésimo sexto corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-autocad-line-geometry`; conserva los falsos fallos y el caso negativo. Amplía M6.6 al contenido geométrico BLOCK/HATCH y asociación editada externamente en copia sintética; aborda M6.1–M6.5 espacial/visual. Continúa M3 tokens/recorte, M4 muros/aperturas/cotas y M5 estilos/escala con gates separados. Espera las seis imágenes inéditas autorizadas para M7 L3/L5 y revisión humana; no abras el DWG privado, no publiques artefactos privados ni uses `upstream`.

## Trigésimo séptimo corte diario de presupuesto de tokens — 2026-09-24, en progreso

El corte 36 `codex/mcp-autocad-line-geometry` quedó publicado en el fork con SHA local/remoto `da20837da3ff19a9ccebf587ad3bddd55fa4f7b0` y árbol limpio. En `codex/mcp-token-budget-journal`, `TokenBudget` reserva un ID local antes de invocar un modelo y escribe intención fsync en JSONL append-only con cadena SHA-256. Después acepta `token_usage_record` validado de una respuesta identificada, deduplica y suma `input + output` una sola vez; `cached_input` y `reasoning_output` siguen siendo subconjuntos, no se suman otra vez. Solo se persisten hashes de IDs y cifras de uso, no texto ni credenciales. Tras reiniciar, una intención sin respuesta bloquea otra invocación hasta conciliar el registro original; un exceso observado se registra y bloquea solicitudes posteriores. El límite es posterior a la respuesta porque su tamaño no puede conocerse antes de generarla.

Cuatro tests L1 cubrieron reanudación/deduplicación, intención incierta, exceso y alteración del diario/límites. La suite masterplan pasó **26/26**, automatización **31/31** y `git diff --check` pasó. El ensayo sintético durable `target/mcp-token-budget/synthetic-edad628d/report.json` registró 2 respuestas, 20/20 tokens, tercer pedido bloqueado y SHA-256 del diario `824048839A25EC0CD0DC3C9EE34BE01D44DC19A426F462E0FEFD4C0D6AC17CAE`. No se llamó a un modelo ni se leyó un rollout privado en este corte.

M3.6 queda **partial L1/L2**: el presupuesto de tokens está verificado solo con registros sintéticos y todavía no hay adaptador confiable al runtime Luna ni cobertura de tokens del supervisor; los presupuestos de pasos/tiempo y capturas/bytes sí tienen L2 sintético. No se promueve G10. El DWG privado y evidencias históricas permanecen intactos. Verificar SHA remoto al cerrar el corte.

### Prompt de continuidad desde el trigésimo séptimo corte

Verifica `AGENTS.md`, Git/remotos y SHA local/remoto de `codex/mcp-token-budget-journal`; preserva el L1 y todos los cortes previos. Conecta el diario de tokens solo a telemetría confiable del modelo efectivo y supervisor, con cobertura por respuesta y conciliación de intención incierta; no inventes uso del motor CAD. Completa M3 recorte limpio y latencias, M4 muros/aperturas/cotas, M5 estilos/escala, M6 QA espacial/visual y externa. Espera las seis imágenes inéditas autorizadas para M7 L3/L5 y revisión del usuario; no uses su DWG privado ni publiques datos privados o `upstream`. M8 requiere checks y merge solo al fork al cumplir los gates.

## Trigésimo octavo corte preflight de cohorte reservada — 2026-09-24, en progreso

El corte 37 `codex/mcp-token-budget-journal` quedó publicado en el fork con SHA local/remoto `db56f6b3863db9a2c62b92c38be072ff165236e8` y árbol limpio. Desde `codex/mcp-reserved-cohort-manifest`, `reserved_cohort.py` congela antes del primer run seis imágenes PNG/JPEG decodificables y únicas, bajo una raíz de entrada explícita, con exactamente dos casos simples, dos medios y dos adversariales. Guarda hash SHA-256, bytes, dimensiones, formato, tres IDs de repetición por caso y `oracle_status=pending`; no guarda rutas locales ni contenido de imagen en el manifiesto. Rechaza el hash de la fuente histórica conocida y los hashes de desarrollo que proporcione el operador, duplicados, symlinks que escapen la raíz y sobrescritura del manifiesto. `verify_sources` detecta cambios después de congelar. Se utilizó Pillow 12.1.0 presente en el entorno; para reproducir este helper hace falta Pillow.

Tres tests L0 con seis PNG generados pasaron dentro de la suite masterplan **29/29**. El preflight durable `target/m7-cohort-preflight/synthetic-7f5c0d2f/manifest.json` registró seis casos y 18 repeticiones; SHA-256 del manifiesto `09416E6F2A5DEF1BCE3AE92397B495C0AC18E5297CA8116CAC4A99B050D579EE`. Son imágenes de color sintéticas, no una cohorte de evaluación ni planes inéditos. El usuario confirmó que aportará seis imágenes autorizadas aquí; aún no han llegado. Debe prepararse y congelarse también el conjunto completo de hashes de desarrollo antes de usar el freezer con esos archivos. M7 queda **partial L0/preflight**; G0–G10, Luna L3, oracle externo por caso, tres repeticiones reales y revisión L5 siguen pendientes. El DWG privado no se abrió ni modificó. Verificar SHA remoto al cerrar el corte.

### Prompt de continuidad desde el trigésimo octavo corte

Verifica `AGENTS.md`, Git/remotos y SHA local/remoto de `codex/mcp-reserved-cohort-manifest`; conserva el preflight sintético y no lo confundas con reserva. Continúa M3 telemetría real/recorte, M4 semántica de muros/aperturas/cotas, M5 estilo/escala y M6 QA externa/visual con fixtures sintéticos. Cuando el usuario adjunte las seis imágenes inéditas, confirma derechos y ausencia en desarrollo, congela todos sus hashes y el oracle antes de Luna L3; ejecuta tres repeticiones por caso y solicita revisión L5. No uses el DWG privado ni envíes archivos privados a Git. M8 requiere gates, checks y merge/push solo al fork, nunca `upstream`.

## Trigésimo noveno corte muros y aperturas tipadas — 2026-09-24, en progreso

El corte 38 `codex/mcp-reserved-cohort-manifest` quedó publicado en el fork con SHA local/remoto `116be1cad6b9a77418f21b1bcb5a77fe91e7725c` y árbol limpio. Desde `codex/mcp-planspec-walls-openings`, `planspec-3.schema.json` añade colecciones explícitas `walls` y `openings` a los contornos v2. Un muro recto referencia nodos de eje, espesor de 0.01–2 m, capa y procedencia. Una apertura referencia el muro, offset desde su inicio, ancho positivo, tipo puerta/ventana y procedencia. El validador exige IDs únicos, referencias existentes, extremos dentro del muro y al menos 0.001 m entre intervalos de apertura; no deduce huecos a partir de líneas sueltas.

El fixture versionado `fixtures/synthetic-wall-opening.planspec.json` contiene un contorno de 12 m², muro sur de 4 m/0.2 m y una puerta de ancho 0.9 m a offset 1 m. Su dry-run `target/m4-wall-opening-preflight/synthetic-wall-opening-report.json` produjo arquitectura `validated_uncompiled`, hash de fixture `8CE023ADBF70EC87457C9E599676E2D5272C1D6239C57816DB97A3908274DC73`, hash de comandos base `ABDDB501E84271C3684274031C110D883328642A6371F379D5C607E15FE71ABA` y `executable=false` con capacidades faltantes `wall_compilation` y `opening_compilation`. No se invocó CAD para este caso. Dos tests nuevos ejercieron caso válido y rechazo de solape, exceso y muro inexistente. Las suites masterplan **31/31**, automatización **31/31**, parseo JSON del schema y `git diff --check` pasaron.

M4 queda **partial L0** para muros/aperturas: el contrato de interpretación está tipado, pero faltan compilador geométrico, referencias de cotas a caras/ejes, uniones, puerta con bisagra/giro y ventanas con altura. El contorno v2 sí tiene L2, pero v3 no. No se abrió ni modificó el DWG privado. Verificar SHA remoto al cerrar el corte.

### Prompt de continuidad desde el trigésimo noveno corte

Verifica `AGENTS.md`, Git/remotos y SHA local/remoto de `codex/mcp-planspec-walls-openings`; conserva el dry-run v3 y ensayos previos. Implementa un compilador determinista de muro/apertura rectos con IDs→handles múltiples, valida dimensiones por caras/ejes y verifica un DWG sintético con AutoCAD antes de promover M4. Continúa M3 uso real/recorte, M5 estilos/escala, M6 QA espacial/visual. Para M7 espera las seis imágenes inéditas autorizadas, congela cohortes y oracle, luego ejecuta L3/L5. No uses el DWG privado ni publiques archivos privados o `upstream`; M8 integra solo tras gates.

## Cuadragésimo corte compilación de un muro recto — 2026-09-24, en progreso

El corte 39 `codex/mcp-planspec-walls-openings` quedó publicado en el fork con SHA local/remoto `013c71cb200161fca02348e401681ca8b9da6d76` y árbol limpio. En `codex/mcp-wall-geometry-compiler`, un único muro v3 sin apertura compila determinísticamente su eje/espesor a cuatro comandos LINE que cierran el contorno; cada borde tiene ID derivado, `source_id` del muro, capa y comando trazables. Un muro con apertura o varios muros conserva `unsupported` y no puede llegar a CAD. El fixture anterior de puerta sigue siendo un dry-run no ejecutable; no se dibuja un muro continuo a través de esa puerta. Los comandos de unidades métricas siguen al inicio.

El fixture versionado `fixtures/synthetic-wall.planspec.json` declara un eje de 4 m y espesor 0.2 m. L2 `target/mcp-isolated/20260924-005927-72061d5b/report.json` pasó con cuatro bordes, handles `64,65,66,67`, AUDIT 0/0, DWG reabierto, captura fenced y GUI salida. SHA-256 fixture `DDA61A032A2608F173C9869A03DFC9A3B2B5083318BE9B00DBDBE6114050EC26`, comandos `EBC8D327B69B8346A3E05A4900CAD0B090044DAF2A6728C498C2C331E5A58E8C`, binario `84926694CD1F7EEC400269393F1DEB2C6EA2D8A06012A59F2F0C881560456851`, DWG `174CBBAA892F641309F846BCB33B46C079DA9079EA23519D38F1F7273C07A940`. La sonda AutoCAD calcula de forma separada las cuatro esquinas desde nodos y espesor del fixture; `target/mcp-external/20260924-010049-autocad-839e881c/report.json` pasó 4/4 bordes, capa, `INSUNITS=6`, AUDIT 0/0, salida código 0 y SHA de entrada sin cambios.

La sonda ahora falla si existe reporte L2 al lado del DWG y sus hashes no coinciden. Una copia sintética con el hash del reporte adulterado bajo `target/mcp-isolated/20260924-source-hash-negative-2aa19f3d` produjo `semantic_mismatch`, exit 1, en `target/mcp-external/20260924-010111-autocad-4df5efc6/report.json`; originales intactos. Las suites masterplan **32/32** y automatización **31/31** pasaron; `git diff --check` pasó. M4 muro queda **partial scoped L2/L4**: solo segmento recto aislado, sin uniones, huecos, puertas, ventanas o dimensiones a caras. M6.6 también partial; no hay representación completa del plano ni calidad visual L3/L5. Verificar SHA remoto al cerrar el corte.

### Prompt de continuidad desde el cuadragésimo corte

Verifica `AGENTS.md`, Git/remotos y SHA local/remoto de `codex/mcp-wall-geometry-compiler`; preserva el L2/L4 del muro y el negativo de hash. Compila aperturas rectas como gaps y jambas con IDs→handles y QA espacial antes de incorporar símbolos de puerta/ventana; luego añade uniones y cotas vinculadas a caras/ejes. Sigue M3 uso real/recorte, M5 estilos/escala y M6 visual/interoperabilidad restante. Para M7 espera seis imágenes inéditas autorizadas, congela cohorte/oracle y ejecuta L3/L5 con tres repeticiones. No uses el DWG privado ni publiques privados/upstream. M8 merge solo tras gates.

## Cuadragésimo primer corte hueco claro y jambas — 2026-09-24, en progreso

El corte 40 `codex/mcp-wall-geometry-compiler` quedó publicado en el fork con SHA local/remoto `ebf8958bd8ab83b8f501d1339b6a1ed97eb6eb7b` y árbol limpio. En `codex/mcp-clear-wall-openings`, PlanSpec v3 incorpora `opening.kind=clear`: hueco geométrico sin símbolo. Para un muro recto único con uno o más huecos claros, el compilador ordena intervalos, interrumpe ambas caras en cada hueco y emite tapas de extremos y jambas, con ID por parte y `source_id` del muro o hueco. Puerta/ventana continúan `unsupported` y no se ejecutan sin símbolos propios. La separación mínima entre intervalos sigue en 0.001 m y la compilación falla si un tramo colapsa a precisión CAD.

El fixture `fixtures/synthetic-wall-gap.planspec.json` declara eje de 4 m, espesor 0.2 m y hueco claro de 0.9 m a 1 m del inicio. L2 `target/mcp-isolated/20260924-010427-8507f503/report.json` pasó con ocho LINE, handles de muro `64`–`69`, jambas `6A`/`6B`, AUDIT 0/0, DWG reabierto y GUI salida. SHA-256 fixture `8B877FAF252579AA0F46DB18DD686D6E4A4EDC96A7E9D6BF604C7CC36056F38B`, comandos `B038D355FFAECC06617FE51AF72A0558CB1F8FD5FB406A04D72264B6A0FE371C`, binario `84926694CD1F7EEC400269393F1DEB2C6EA2D8A06012A59F2F0C881560456851`, DWG `3849C37BAAB472AEB093E147147E8F3B465CFAA10BBD95B2179B354AB579AF66`. AutoCAD calculó en PowerShell los spans/caps/jambas desde el fixture y `target/mcp-external/20260924-010649-autocad-faa7d4ec/report.json` pasó 8/8 coordenadas, total de ocho Model, capa, `INSUNITS=6`, AUDIT 0/0, salida código 0 y SHA de DWG intacto. Una copia sintética con el handle de jamba alterado, `target/mcp-isolated/20260924-gap-negative-6e88a10f`, produjo `semantic_mismatch`, exit 1, en `target/mcp-external/20260924-010611-autocad-2fa9f8cc/report.json`.

Las suites masterplan **33/33**, automatización **31/31** y `git diff --check` pasaron. El PNG L2 fue inspeccionado: el hueco es demasiado pequeño con el encuadre actual y aún hay overlays, por lo que **no pasa G6 visual** aunque G2/G5/G9 numéricos acotados pasan para este fixture. M4 sigue partial: no hay puerta/ventana con símbolo, uniones de muros, cotas a caras ni aperturas en muros curvos; M6 externo sigue parcial. No se tocó el DWG privado. Verificar SHA remoto al cerrar el corte.

### Prompt de continuidad desde el cuadragésimo primer corte

Verifica `AGENTS.md`, Git/remotos y SHA local/remoto de `codex/mcp-clear-wall-openings`; conserva L2/L4 y el caso negativo. Mejora encuadre y captura limpia para que el hueco sea legible, sin confundir G6 con la QA numérica. Después diseña símbolos nativos de puerta/ventana, uniones y cotas por cara/eje antes de promover M4. Avanza M3 uso real/recorte, M5 estilos/escala y M6 QA visual/externa. M7 espera las seis imágenes inéditas autorizadas, oracle y revisión L3/L5; no uses el DWG privado ni publiques artefactos privados o `upstream`. M8 solo tras gates.

## Cuadragésimo segundo corte serie de latencia L2 — 2026-09-24, en progreso

El corte 41 `codex/mcp-clear-wall-openings` quedó publicado en el fork con SHA local/remoto `43d97ac26364001bbe44b1623c62093dc6adcba0` y árbol limpio. En `codex/mcp-l2-latency-series`, `mcp_latency_series.py` ejecuta secuencialmente el mismo fixture `synthetic-room` diez veces con el mismo binario, conserva cada perfil, DWG, PNG y reporte L2 en `target/mcp-isolated`, fija hashes de binario/fixture/comandos y rechaza cambios de alcance de reloj, audit fallido o GUI sin salida. El agregado usa percentil nearest-rank (con n=10, p95 es el máximo) y se guarda en `target/mcp-latency-series/20260924-011150-364f3cd9/series.json`; un ensayo previo de dos corridas está en `target/mcp-latency-series/20260924-011118-01d2bd93/series.json`. Los dos conjuntos son sintéticos, locales e ignorados por Git.

La serie de diez pasó 10/10, con binario SHA-256 `84926694CD1F7EEC400269393F1DEB2C6EA2D8A06012A59F2F0C881560456851`, fixture `D38B92F2D3205EB16AECFF82CD75795BD11243188DB8E5505C1E312FFFCA3E84`, comandos `E4C78B0DC2D7D5EE8A16F865F658DD33C953BCCC964CA52DCC6024FADCD107EA` y un solo hash de DWG para las diez salidas: `2722DAD74688FE6E9CC415278FAB31B3AA48465A3451843EC9A46611EA954D2E`. En ms, p50/p95: ejecución completa 8235.376/8685.812; RPC script 2608.621/2846.615; RPC save 66.128/72.187; RPC captura+artefacto 1323.461/1426.274; operaciones GUI script sumadas 6.2/7.381; guardado GUI 31.4477/34.5554; captura GUI total 1239.671/1364.285, de los que espera de frame fue 140.7764/165.1521 y PNG encode/write 1090.5693/1216.2318. Los relojes internos GUI y cliente son distintos: no se atribuye su diferencia exclusivamente al transporte. El tiempo total incluye arranque, handshake, verificaciones, persistencia, captura y cierre. n=10 en una máquina es una observación exploratoria L2, sin intervalo de confianza ni extrapolación al DWG privado o M7. M3.2 continúa **partial L2**.

Se preservaron los 10 reportes individuales y el ensayo de dos corridas. No se abrió ni modificó el DWG privado. `git diff --check`, tests y SHA remoto se verifican antes de cerrar/publicar este corte.

### Prompt de continuidad desde el cuadragésimo segundo corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-l2-latency-series`; conserva los 12 reportes de latencia y el agregado. Repite una serie equivalente al cambiar el binario o fixture, sin comparar números de distinto perfil como si fueran el mismo tratamiento. Prosigue M3 con recorte limpio y telemetría real de modelo/supervisor; M4 con símbolos de puerta/ventana, uniones y cotas a caras/ejes; M5 estilos/escala; M6 QA espacial/visual y externa. Espera las seis imágenes inéditas autorizadas para M7, congela sus hashes/oracle y ejecuta L3/L5 solo con ellas. No uses el DWG privado ni publiques archivos privados o `upstream`; M8 requiere gates y merge/push solo al fork.

## Cuadragésimo tercer corte captura limpia y encuadre del hueco — 2026-09-24, en progreso

El corte 42 `codex/mcp-l2-latency-series` quedó publicado solo en el fork con SHA local/remoto `28f050e137d645c42ce251a9a47732995ec3e294` y árbol limpio. En `codex/mcp-clean-viewport-capture`, una captura MCP `viewport` activa temporalmente la vista limpia ya usada por las miniaturas, espera dos frames de compositor y conserva el fence de geometría/cámara antes del screenshot. Al resolver éxito o error, restaura los overlays. La GUI declara `overlay_policy=drawing_only`; el servidor MCP y el cliente Python rechazan otra política antes de entregar o escribir PNG. Un test negativo simula `interactive` y verifica ausencia de archivo. Las capturas `window` conservan su política `interactive`.

La primera L2 del hueco con `ZOOM EXTENTS` produjo el PNG limpio y legible, pero el zoom marcó el documento de sesión como dirty: `target/mcp-isolated/20260924-012044-b9c18034/report.json` quedó **partial**, con GUI viva. Se preservó el reporte y PNG; `cleanup.json` registra que se verificó PID 9320 y ruta del binario aislado antes de detener únicamente esa GUI. El harness ahora guarda de nuevo `synthetic-session.dwg` después del zoom. La siguiente L2 `target/mcp-isolated/20260924-012132-3adfee2b/report.json` pasó y cerró la GUI. Estos intentos no alteraron el DWG verificado ni el privado.

Con el binario final SHA-256 `1F05414C4314FC5B64DD54409C2AE936483F3CB79E2418109CB2F9A500FA142F`, L2 `target/mcp-isolated/20260924-012919-6e11922b/report.json` (room) y `target/mcp-isolated/20260924-012939-5139da8a/report.json` (wall-gap) pasaron, `overlay_policy=drawing_only`, revisiones renderizadas coincidentes y GUI salida. El PNG room SHA-256 es `59E7E2F4860A6D28BCB8BA1191BEEFC7FFB7AFAD5657ABE8E5E637C33E0C2B3B`; el PNG wall-gap `FFD2E153F52DAFBDF0E2865828A7D1BB89A5CB23290DDC8E7C1A73A24978180D`. Se inspeccionaron visualmente ambos: no aparecen barra de comandos, controles de navegación ni otros overlays; el hueco se ve entre dos tramos de muro tras `ZOOM EXTENTS`. El DWG de gap conservó SHA `3849C37BAAB472AEB093E147147E8F3B465CFAA10BBD95B2179B354AB579AF66`, mientras el PNG declara cámara revisión 1. `cargo check --lib`, `cargo build --bin OpenCADStudio`, suite Rust MCP 20/20, suites Python automatización 31/31 y masterplan 33/33, y `git diff --check` pasaron. `cargo fmt --check` global no es gate vigente: encontró diferencias masivas preexistentes por todo el repositorio; no se reformateó código ajeno.

M3.3 y M6 visual mejoran a **partial scoped L2**: existe captura limpia y fixture de hueco legible, pero aún faltan recortes por región CAD, revisión visual sistemática, variedad de casos y la cohorte M7 L3/L5. No se abrió ni modificó el DWG privado. Verificar SHA remoto al publicar este corte.

### Prompt de continuidad desde el cuadragésimo tercer corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-clean-viewport-capture`; conserva el intento parcial con su cleanup y los L2 finales. Extiende M3/M6 a recortes por coordenadas CAD, anotación de regiones y oracle visual reproducible sin confundir legibilidad del fixture con G6 completo; mide la latencia del nuevo binario con una serie comparable antes de afirmar mejoras de rendimiento. Completa M3 telemetría real de modelo/supervisor, M4 puertas/ventanas/uniones/cotas por referencias, M5 estilos/escala y M6 QA externa. Espera las seis imágenes inéditas autorizadas para M7 L3/L5 y su revisión humana. No uses el DWG privado ni publiques privados o `upstream`; M8 merge/push solo en el fork tras gates.

## Cuadragésimo cuarto corte segunda serie de latencia L2 — 2026-09-24, en progreso

El corte 43 `codex/mcp-clean-viewport-capture` quedó publicado en el fork con SHA local/remoto `be9822dbd0bf844dfc58dc30605ef8a69e26f13a` y árbol limpio. Desde `codex/mcp-clean-capture-latency`, se repitió la serie secuencial de diez L2 `synthetic-room` con el binario final del corte 43. Agregado: `target/mcp-latency-series/20260924-013400-4b502532/series.json`; pasó 10/10 con binario SHA-256 `1F05414C4314FC5B64DD54409C2AE936483F3CB79E2418109CB2F9A500FA142F`, fixture `D38B92F2D3205EB16AECFF82CD75795BD11243188DB8E5505C1E312FFFCA3E84`, comandos `E4C78B0DC2D7D5EE8A16F865F658DD33C953BCCC964CA52DCC6024FADCD107EA` y un solo SHA de DWG en las diez salidas. Se recalcularon y compararon los hashes de los diez reportes guardados: 10/10. Se preservó la serie anterior del binario viejo bajo `target/mcp-latency-series/20260924-011150-364f3cd9/series.json`.

En ms, p50/p95 nearest-rank del binario nuevo: ejecución completa 7721.139/8046.808; RPC script 492.52/2517.12; RPC save 60.23/70.72; RPC captura+artefacto 1120.901/1165.781; operaciones GUI script sumadas 6.32/7.67; guardado GUI 29.76/36.60; captura GUI total 1052.6094/1098.5909, con espera de frame 132.96/143.84 y PNG encode/write 916.01/951.90. Los números de la serie anterior fueron respectivamente 8235.376/8685.812 para ejecución y 1323.461/1426.274 para captura RPC+artefacto. En la serie nueva, nueve RPC script estuvieron entre ~460–540 ms y una en 2517 ms. No hay asignación causal de las diferencias: las series no se intercalaron, el sistema pudo variar entre ellas, el binario cambió y n=10 no da estimación robusta de cola. M3.2 continúa **partial L2**, sin gate de rendimiento promovido ni extrapolación a M7. No se tocó el DWG privado ni `upstream`; los 20 reportes de ambas series y los agregados siguen bajo `target` ignorado. Verificar publicación y SHA remoto al cerrar este corte.

### Prompt de continuidad desde el cuadragésimo cuarto corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-clean-capture-latency`; conserva ambas series L2. Si se quiere estimar efecto causal de la captura limpia, diseña un A/B intercalado con perfiles y binarios fijos antes de fijar umbrales; no llames mejora de rendimiento a estas dos muestras descriptivas. Prosigue M3 recortes CAD y uso real Luna/supervisor, M4 puertas/ventanas/uniones/cotas referenciadas, M5 estilos/escala y M6 QA visual/externa. M7 requiere las seis imágenes inéditas autorizadas, oracle previo, tres repeticiones y revisión humana L5; no uses el DWG privado. M8 solo se integra/publica al fork tras pasar gates; nunca empujes a `upstream`.

## Cuadragésimo quinto corte contrato de puerta y ventana v4 — 2026-09-24, en progreso

El corte 44 `codex/mcp-clean-capture-latency` quedó publicado en el fork con SHA local/remoto `baa33127910d9a8a0780d71d99eba0c522fc6f69` y árbol limpio. Desde `codex/mcp-door-swing-contract`, PlanSpec v4 agrega al contrato v3 un `swing` requerido únicamente para `kind=door`: `hinge=start/end` respecto al eje dirigido del muro, `side=left/right` y `angle_deg=90` entero. Para `kind=window`, requiere `elevation={sill_m,head_m}` con 0 ≤ sill < head. `kind=clear` no acepta ninguna de esas estructuras. El validador Python exige los campos propios del tipo, referencias y espaciado previos, y rechaza giro/hinge/lado/alturas inválidos; el esquema JSON v4 declara las mismas ramas `oneOf`. V3 sigue conservado sin reinterpretar sus puertas antiguas.

El fixture `fixtures/synthetic-door-swing.planspec.json` usa un muro sintético y una puerta con bisagra inicial/giro a la izquierda. El dry-run queda **`validated_uncompiled`**, `unsupported=[opening_compilation,wall_compilation]`, `executable=false`, y no emite entidad para la puerta. El preflight final `target/m4-door-swing-preflight/synthetic-92ee340c/report.json` conserva hash SHA-256 `5C4ED00A6BC37A8B4FDB06FCEE23C7AC92EF860CCB4CC908BE4953CDE6EC90C7`; fixture `47AC031F90BB53C2290450107D32A37FDBD14F8A02B397FD7E1792B37866F98C`, esquema `130FF06FF75C2F04E4FFFB163F4B235BA0136433BED29B8FBA5CAF59851000AD`, comandos base `ABDDB501E84271C3684274031C110D883328642A6371F379D5C607E15FE71ABA`. El ensayo anterior `target/m4-door-swing-preflight/synthetic-4f3b8a9e/report.json` queda conservado como preflight supersedido cuando se hizo estricto el tipo entero del ángulo. Ninguno invocó CAD. La suite masterplan pasó 35/35, automatización 31/31, schema parse JSON y `git diff --check` pasaron.

M4 puerta/ventana sigue **partial L0**. Faltan compilador geométrico, definición precisa de bisagra en la cara de muro, oráculo independiente para arco/hoja, ventanas con geometría 2D/altura, uniones y cotas a referencias. El DWG privado, los originales y `upstream` permanecen intactos. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el cuadragésimo quinto corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-door-swing-contract`; conserva ambos preflights v4 y los esquemas/fixtures v3. Implementa primero un único símbolo de puerta recta con bisagra/lado explícitos y gap en muro aislado, con IDs→handles, verificación de geometría/ángulo en L2 y AutoCAD L4, más negativo alterado; no promociones ventana o múltiples muros. Después aborda ventana con altura, uniones y cotas por cara/eje. Sigue M3 uso real/recortes, M5 estilos/escala y M6 QA externa/visual. Espera las seis imágenes inéditas autorizadas para M7 L3/L5 y revisión del usuario; no abras el DWG privado ni publiques privados/upstream. M8 integra solo tras gates.

## Cuadragésimo sexto corte puerta única con hoja y arco — 2026-09-24, en progreso

El corte 45 `codex/mcp-door-swing-contract` quedó publicado en el fork con SHA local/remoto `976fcec1cb5ac7141b339b36159d666cb134fd83` y árbol limpio. Desde `codex/mcp-single-door-compiler`, un único muro recto v4 con una puerta tipada compila los ocho bordes de dos tramos y sus jambas, una LINE desde la bisagra hasta el extremo abierto y un ARC de tres puntos para el giro de 90°. La bisagra se coloca en la cara del muro del lado declarado (`left/right` relativo al eje dirigido); el extremo cerrado está en la jamba opuesta y el abierto se proyecta normal al muro. Las piezas derivadas llevan ID, fuente, parte y capa. El compilador rechaza colapso a precisión CAD; dos puertas o ventanas aún mantienen `unsupported` y no se envían a CAD. Cuatro combinaciones de bisagra/lado se verificaron en seco. El preflight v4 `validated_uncompiled` del corte 45 se conserva como estado histórico anterior a este compilador.

L2 `target/mcp-isolated/20260924-014423-627e9b0a/report.json` pasó: 14 comandos geométricos del fixture (4 LINE de contorno, 8 LINE de muro/hueco, 1 LINE de hoja y 1 ARC), IDs→handles únicos; fuente `door-south` mapeó jambas `6E/6F`, hoja `70` y arco `71`. AUDIT 0 errores, DWG sintético guardado y reabierto, captura limpia `drawing_only` con cámara revisión 1, GUI salida. SHA-256 fixture `47AC031F90BB53C2290450107D32A37FDBD14F8A02B397FD7E1792B37866F98C`, comandos `FB5B575C2C627B1EF8056D89D4478AAA72F49F604925BC3E2FEACE619CF437E7`, binario `1F05414C4314FC5B64DD54409C2AE936483F3CB79E2418109CB2F9A500FA142F`, DWG `0958285A0CF70F7B24883383724DB2F6FDB3D9E674B4FB5F236D4001A1E9FD27`, PNG `23058B933DEC9409D0BF263EBEAAE790E7E5AF0247EA5AA5D89432C43436D906`. El PNG se inspeccionó visualmente: hueco, hoja y arco legibles, sin overlays.

La primera sonda AutoCAD `target/mcp-external/20260924-014551-autocad-8fd835d7/report.json` falló 13/14 porque comparó DXF 50/51 como grados; el censo mostraba `0` y ~`1.5708`, radianes. Se conservó ese fallo y se corrigió el oráculo, no el DWG. Un pase siguiente en `target/mcp-external/20260924-014621-autocad-f2f5c8b2/report.json` pasó con tolerancia angular 1e-5; se elevó la precisión de texto AutoLISP para sostener una tolerancia uniforme 1e-6. La sonda versionada `mcp-autocad-audit-l4-6` final `target/mcp-external/20260924-014811-autocad-2064309c/report.json` pasó 14/14 geometrías y conteo Model=14 (LINE=13, ARC=1), capa, INSUNITS=6, AUDIT 0/0, salida código 0 y SHA de entrada intacto. AutoCAD midió centro de ARC `(1,0.0999996)`, radio `0.9`, ángulos `0.000000390995` y `1.5707959358` rad; el oráculo independiente esperaba `(1,0.1)`, `0.9`, `0` y `π/2`. Una copia sintética `target/mcp-isolated/20260924-door-negative-73c5a491` cambió solo el handle del arco en su reporte; `target/mcp-external/20260924-014828-autocad-b0dab5e4/report.json` rechazó 1/14 con `semantic_mismatch`, exit 1, conservando ambos DWG. El fixture anterior de hueco pasó de nuevo en `target/mcp-external/20260924-014729-autocad-eb09cb6d/report.json`.

Suites Python masterplan 36/36 y automatización 31/31 pasaron; `git diff --check` pasó. M4 puerta única y M6.6 geometría de ARC quedan **partial scoped L2/L4**: son un muro recto, una bisagra a cara y giro fijo de 90° en un motor externo. Quedan múltiples puertas/muros, ventanas, cotas a caras/ejes, contenido BLOCK/HATCH y revisión de planos M7. El DWG privado no se abrió ni modificó; `upstream` no se tocó. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el cuadragésimo sexto corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-single-door-compiler`; preserva el primer falso fallo angular y el negativo de handle. Extiende M4 con ventana de altura explícita y su simbología 2D verificable, luego uniones de muros y cotas por caras/ejes; conserva el alcance de puerta única separado de nuevos casos. Completa M3 recortes CAD y telemetría real de modelo/supervisor, M5 estilos/escala, M6 QA espacial, visual y de interoperabilidad externa. Espera las seis imágenes inéditas autorizadas para M7, congela hashes/oracle antes de L3 y pide revisión L5; no uses el DWG privado. M8 integra y publica solo al fork tras gates, nunca a `upstream`.

## Cuadragésimo séptimo corte ventana con rieles 3D — 2026-09-24, en progreso

El corte 46 `codex/mcp-single-door-compiler` quedó publicado solo en el fork con SHA local/remoto `305bf8ddb5c4611b171731f747f36a44bf4031ca` y árbol limpio. Desde `codex/mcp-single-window-compiler`, un muro v4 con una ventana tipada compila el hueco/jambas como ocho LINE y dos rieles de marco como LINE 3D: el riel de antepecho a `z=sill_m` y +espesor/4 en planta, el riel de dintel a `z=head_m` y −espesor/4. La altura cambia el hash de comandos, evitando que dos contratos con distinta elevación produzcan el mismo DWG. Una ventana en un muro recto es el único alcance nuevo ejecutable; múltiples huecos simbólicos o varios muros siguen `unsupported`.

El fixture `fixtures/synthetic-window.planspec.json` declara antepecho 0.8 m y dintel 2.1 m; genera `LINE 1,0.05,0.8 1.9,0.05,0.8` y `LINE 1,-0.05,2.1 1.9,-0.05,2.1`. L2 `target/mcp-isolated/20260924-015216-39f5c449/report.json` pasó con 14 LINE (4 de contorno, 8 de muro/hueco y 2 rieles), handles por ID/fuente, AUDIT 0, DWG guardado/reabierto, captura fenced `drawing_only`, GUI salida. El PNG se inspeccionó y distingue abertura y dos líneas de ventana sin overlays. SHA-256 fixture `9D81F4F48144C957F5AD58F915BDDC61773D1414B3532E7BF732C86523BC5C7E`, comandos `D6EB202DF621897522E869B3D8772BCF1E219F8683EF34846D0F9E86E1C51998`, binario `1F05414C4314FC5B64DD54409C2AE936483F3CB79E2418109CB2F9A500FA142F`, DWG `A394BBA07E8B8516E57B7E9A9867FAED829B788D18D10E7A58CC3E9FA9D8D5BD`, PNG `E5912938D879863C73F9FAFC4AF4FB076557CCA1501D9278DE1DEB5998166FED`.

AutoCAD L4 versionó la sonda como `mcp-autocad-audit-l4-7`. El reporte final `target/mcp-external/20260924-015350-autocad-0fddeb79/report.json` pasó 14/14 coordenadas 3D con tolerancia 1e-6, incluido `z=0.8/2.1`, INSUNITS=6, AUDIT 0/0, Model=14 LINE, salida 0 y SHA DWG intacto. La copia sintética `target/mcp-isolated/20260924-window-negative-40d531e2` alteró solo el handle del riel de dintel en su reporte; `target/mcp-external/20260924-015352-autocad-296302bd/report.json` produjo `semantic_mismatch`, exit 1, con una sola comparación fallida y DWG sin cambio. La puerta anterior pasó de nuevo con esta sonda en `target/mcp-external/20260924-015420-autocad-6ad45534/report.json`. Se preservan los reportes preliminares de ventana de la misma sesión.

Suites Python masterplan 36/36 y automatización 31/31 pasaron; `git diff --check` pasó. M4 ventana queda **partial scoped L2/L4**: se acreditaron dos rieles 3D y alturas persistidas para este fixture. No se modelaron cristal/marco completo, jambas tridimensionales, grosor vertical, varias ventanas ni uniones. El DWG privado y `upstream` permanecen intactos. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el cuadragésimo séptimo corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-single-window-compiler`; conserva los L2/L4 positivos, el negativo y el DWG original. Continúa M4 con uniones de muros y cotas asociadas a caras/ejes en fixtures sintéticos, con oráculo externo por geometría y semántica. Completa M3 telemetría de modelo/supervisor y recortes por región CAD, M5 estilos/escala y M6 QA espacial/visual/interoperabilidad. Las seis imágenes inéditas autorizadas para M7 aún deben adjuntarse aquí: congela sus hashes/oracle antes de L3 y espera revisión L5 del usuario. No abras el DWG privado ni publiques privados o `upstream`; M8 merge/push solo al fork tras gates.

## Cuadragésimo octavo corte contrato explícito de unión ortogonal — 2026-09-24, en progreso

El corte 47 `codex/mcp-single-window-compiler` quedó publicado solo en el fork con SHA local/remoto `bf0dda1893fecfc59a8f33e8f0ce76e62455444f` y árbol limpio. Desde `codex/mcp-explicit-wall-join-contract`, PlanSpec v5 añade una colección `joins`: cada entrada declara ID propio, dos IDs de muro, extremo `start/end` de cada uno, `style=orthogonal_union` y procedencia. El validador exige que los extremos referenciados compartan exactamente el mismo nodo geométrico, ejes perpendiculares y espesores iguales; rechaza un extremo usado dos veces, referencias colgantes, estilo/terminal inválido y colisiones de ID. V1–v4 permanecen aceptados con sus contratos originales. El esquema JSON v5 documenta los campos, y `architecture.joins` devuelve la relación de forma ordenada.

El fixture `fixtures/synthetic-wall-join.planspec.json` declara dos ejes en L, uno horizontal de 4 m y otro vertical de 3 m, espesor común 0.2 m, unidos explícitamente en `(4,0)`. El dry-run retorna `validated`, `unsupported=[join_compilation,wall_compilation]`, `executable=false` y cero comandos de dibujo. Preflight L0 `target/m4-wall-join-preflight/synthetic-bd0f46a2/report.json`: SHA-256 `F614D761738CC2F1A85B3DB1C628DD7097594FEBFD3BE2352F3F5FAB25D914F4`; fixture `3859DDB2AA4E76DDB3E603A2161B588CD67C67B897F024F41557A15C16E26949`; esquema `54D6A159338085D8C62425A73F3D5BDA2715BAD9A86B16F150F1AF2B10D71751`; comandos `BB0FF67733E701552EF925A0B8C4610A5CE8450D9C8A0D98289B75FB9DF01424`; `cad_invoked=false`. Pruebas negativas rechazaron perpendicularidad y espesor incorrectos, extremo no coincidente y muro inexistente. La suite masterplan pasó 37/37; automatización 31/31 y `git diff --check` se verifican antes de publicar.

M4 uniones queda **partial L0**; no se aceptó aún geometría en L, unión editada, ortogonalidad bajo tolerancias medidas, ni DWG externo. El DWG privado, ensayos previos y `upstream` permanecen intactos. Verificar SHA remoto al cerrar el corte.

### Prompt de continuidad desde el cuadragésimo octavo corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-explicit-wall-join-contract`; conserva el preflight v5 no ejecutable. Implementa una unión ortogonal de dos muros iguales como contorno de unión sin superposición ni líneas interiores, con IDs→handles, QA topológica y L2; verifica independiente en AutoCAD L4 y siembra un negativo. Después amplía referencias de cotas a caras/ejes y evita inferir uniones por proximidad. Prosigue M3 uso real de modelo/supervisor y regiones CAD, M5 estilos/escala, M6 QA externa/visual. M7 espera seis imágenes inéditas autorizadas, oracle previo y revisión L5; no abras el DWG privado ni publiques privados/upstream. M8 solo merge/push al fork con gates completos.

## Cuadragésimo noveno corte contorno de unión ortogonal — 2026-09-24, en progreso

El corte 48 `codex/mcp-explicit-wall-join-contract` quedó publicado solo en el fork con SHA local/remoto `7c481fd88264f8436615c7407f7238d125f6da4c` y árbol limpio. Desde `codex/mcp-orthogonal-wall-union`, el compilador v5 acepta exactamente dos muros unidos en un extremo, perpendiculares, mismo espesor/capa, sin aperturas y con un join explícito. Calcula el contorno de la unión de ambos rectángulos en el sistema local de los dos ejes y emite ocho LINE encadenadas: tres aristas de cada muro y dos de la muesca de unión, sin líneas superpuestas dentro del sólido. Las piezas conservan ID derivado, fuente, parte y capa. Rechaza ejes demasiado cortos o vértices colapsados por la precisión de comandos. El test del fixture exige cierre, ocho vértices únicos, área 1.39 m² y mapa de fuentes 3/3/2. El preflight v5 no ejecutable del corte 48 permanece como historial previo al compilador.

L2 `target/mcp-isolated/20260924-020148-8d021220/report.json` pasó con ocho LINE, handles `64`–`6B`, AUDIT 0, DWG guardado/reabierto, PNG fenced `drawing_only` y GUI salida. SHA-256 fixture `3859DDB2AA4E76DDB3E603A2161B588CD67C67B897F024F41557A15C16E26949`, comandos `0FD942ED4ADC36742990B8FE0495D437948FBFB0B748E91D75A9D568CD1BD7EF`, binario `1F05414C4314FC5B64DD54409C2AE936483F3CB79E2418109CB2F9A500FA142F`, DWG `CE669954CF97275EA2D16312B1260D7C14C795AE51302A34C6B63956850F33D9`, PNG `A88E6F4F43743496B703209680A02BFFD990C1687B4EBB13663C261CE0E7267F`. Se inspeccionó el PNG: figura en L con muesca y sin overlays.

La primera sonda AutoCAD se interrumpió por la evaluación PowerShell de arrays/división antes de emitir reporte; se preserva el directorio `target/mcp-external/20260924-020249-autocad-e9220c84`. Se agrupó cada operación aritmética, y `target/mcp-external/20260924-020312-autocad-536fadb2/report.json` pasó. La sonda final `mcp-autocad-audit-l4-8` `target/mcp-external/20260924-020354-autocad-0ead9070/report.json` comparó de forma independiente los ocho segmentos calculados desde nodos/espesor del fixture: 8/8, Model=8 LINE, INSUNITS=6, AUDIT 0/0, salida código 0 y SHA de DWG intacto. Una copia sintética `target/mcp-isolated/20260924-join-negative-11b0d77c` cambió solo el handle de una arista en el reporte; `target/mcp-external/20260924-020356-autocad-1e7d805c/report.json` produjo `semantic_mismatch`, exit 1, con 1/8 discrepancias. Las regresiones de puerta/ventana pasaron con la misma sonda en `target/mcp-external/20260924-020358-autocad-27782887/report.json` y `target/mcp-external/20260924-020400-autocad-7f173589/report.json`.

Suites Python masterplan 37/37, automatización 31/31 y `git diff --check` pasaron. M4 unión queda **partial scoped L2/L4**: acreditado solo un par de muros perpendiculares sin aperturas. T/X, espesores/capas diferentes, joins encadenados, puertas/ventanas en muro unido, edición y cotas referenciadas permanecen pendientes. El DWG privado y `upstream` no se tocaron. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el cuadragésimo noveno corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-orthogonal-wall-union`; conserva preflight v5, falso fallo de PowerShell y negativos. Prioriza M4 cotas por caras/ejes con referencias tipadas a partes de muro/unión y QA de actualizaciones, luego amplía topologías de join de forma versionada. Continúa M3 uso real de modelo/supervisor y recortes CAD, M5 estilos/escala y M6 QA espacial/visual externa. Espera seis imágenes inéditas autorizadas para M7, congela hashes/oracle antes de L3 y revisión L5. No abras el DWG privado ni publiques privados o `upstream`; M8 merge/push solo en el fork tras gates completos.

## Quincuagésimo corte contrato v6 de cotas a muro — 2026-09-24, en progreso

El corte 49 `codex/mcp-orthogonal-wall-union` quedó publicado solo en el fork con SHA local/remoto `d0b687cc421f32a82e4bd066a6d3a868a1f0f77b` y árbol limpio. Desde `codex/mcp-wall-dimension-bindings`, PlanSpec v6 incorpora `dimension_bindings` obligatorio: cada cota tiene exactamente una pareja de referencias `{wall_id,side,station_m}`. `side` es `left/right` para una cota `face` o `axis` para una cota `axis`, respecto al eje dirigido del muro. El validador calcula la posición de cada referencia desde eje/espesor y exige que coincida con el nodo de la cota a 1e-6 m. Exige estación estrictamente interior, y rechaza muro inexistente, tipo/lado incompatible, vínculo faltante/duplicado, estación dentro de apertura y referencia sobre muro unido (la cara de la unión aún carece de resolución semántica). V1–v5 conservan sus contratos.

El fixture `fixtures/synthetic-wall-face-dimension.planspec.json` declara un muro horizontal de 4 m y espesor 0.2 m, caras en `(2,+0.1)` y `(2,-0.1)`, cota y texto `0.20 m`. El preflight L0 `target/m4-wall-dimension-preflight/synthetic-1618849d/report.json` confirmó `unsupported=[native_dimension]`, `executable=false`, cuatro bordes del muro en dry-run y **cero comandos DIM**; no invocó CAD. SHA-256 reporte `9C10FFA16EBDBE0B61ECE2717307134F366C650ACE129DD79C3B73ED6F34D23D`, fixture `B41DB1DDE8C43780DCB43660CA5088E365E7418B1F83A38A1E0A5EE75845D370`, esquema v6 `ECBE21888C029DD4673C2A9B15C90AB0F6D6B403C1CBAB83A8AFB81917E5FCB3`, comandos `3BE6A55C78C97640F8245C283F73AC3549B84DBF29B268FBF9AF6F887B3C2F6B`. Pruebas positivas de cara y eje y negativas del vínculo pasaron: masterplan 39/39, automatización 31/31, esquema JSON parseable y `git diff --check`.

M4.3 queda **partial L0**: las referencias son deterministas y verificadas contra nodos, pero no hay DIM nativa en este corte, vínculo a handle de borde, persistencia/edición asociativa ni resolución de cara en uniones. El DWG privado, ensayos previos y `upstream` no se tocaron. Verificar SHA local/remoto tras publicación.

### Prompt de continuidad desde el quincuagésimo corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-wall-dimension-bindings`; conserva el preflight L0 v6 y no lo reetiquetes como CAD. Compila una sola cota de espesor por caras de muro recto con offset y estilo explícitos, conservando IDs→handles; mide el valor nativo en L2 y AutoCAD L4, siembra negativos y ensaya edición de muro para acreditar o descartar asociatividad. Mantén ejes y muros unidos como gates separados. Continúa M3 telemetría real/recortes, M5 estilos/escala, M6 QA; las seis imágenes inéditas prometidas para M7 aún no se han adjuntado, y necesitan congelación de hashes/oracle y revisión L5. No abras el DWG privado ni publiques archivos privados o `upstream`; M8 solo merge/push en fork tras gates completos.

## Quincuagésimo primer corte asociación nativa a punto medio de caras — 2026-09-24, en progreso

El corte 50 `codex/mcp-wall-dimension-bindings` quedó publicado únicamente en el fork con SHA local/remoto `65c9abaf226d8e48db8b23640d23134e1492d0f2` y árbol limpio. Desde `codex/mcp-face-dimension-association` se comprobó por inspección de `dimension_assoc.rs` que la inferencia para LINE solo contemplaba extremos; por eso una DIMLINEAR en el centro de cada cara no podía afirmar asociatividad. Se añadió inferencia **solo del punto medio exacto** de LINE, y el vínculo se emite con osnap MID=2, marcador0 y parámetro0.5; la ruta de actualización existente resuelve ese feature después de editar la línea. El contrato PlanSpec v6 no cambió y sigue `unsupported=[native_dimension]` en dry-run. [Autodesk DIMASSOC DXF](https://help.autodesk.com/cloudhelp/2016/ENU/AutoCAD-DXF/files/GUID-C0B96256-A911-4B4D-85E6-EB4AF2C91E27.htm) documenta tipo 2 como punto medio, grupo40 como parámetro y 331 como objeto fuente; el gate efectivo es el censo AutoCAD descrito abajo.

Tests Rust verificaron inferencia de dos líneas de cara separadas, referencias a sus handles y medida 0.20→0.25 tras mover una cara. La prueba del comando tipado `DIMLINEAR 2,0.1 2,-0.1 2.5,0` pasó con el mismo vínculo. `cargo test --lib` pasó **1679, 24 ignored, 0 failed**; masterplan Python 39/39 y automatización Python 31/31 pasaron. Warnings Rust preexistentes no cambiaron el resultado.

El L2 sintético inicial `target/mcp-isolated/20260924-022536-face-76b83134/report.json` y el L2 final `target/mcp-isolated/20260924-022631-face-963b33b5/report.json` pasaron. En el final, GUI aislada PID5500 salió; handles LINE superior/inferior `64/65`, DIM `66`; 0.20 m inicial/reabierta, 0.25 m tras edición compare-and-set de ambos extremos de la LINE superior. Ambos `save_verified` conservaron manifiesto interno de 11 entidades sin residuos. Binario SHA-256 `BB4A7C8C97BEA93A48DF633B6F75143421EA5F738392E0CC3502EFDC84A506F8`, fixture `B41DB1DDE8C43780DCB43660CA5088E365E7418B1F83A38A1E0A5EE75845D370`, reporte final `37431FDE05E0FC3FE74673A349989A3623719E905B288A641F2F7DB9DE59E9CE`, DWG antes `3689E202B41F5F4EE8B4CF0D487299BEF85BF6766B56E7FEFA357CE48F36442C`, DWG después `63780E3165ABAA46870F12B29AC090AB766A6D265A1FD334451434D3FC30CC52`.

AutoCAD Core Console 2025, sonda versionada `mcp-autocad-audit-l4-9`: `target/mcp-external/20260924-022852-autocad-b5f046a9/report.json` cotejó antes 0.20, y `target/mcp-external/20260924-022825-autocad-7a1874fa/report.json` cotejó después 0.25 con DXF 42 y puntos 13/14 correctos. En ambos confirmó que DIM `66` tiene dos refs DIMASSOC en orden a handles `64/65`, cada una con osnap2; Model=2 LINE+1 DIM, INSUNITS6, AUDIT 0/0, exit0 y SHA de entrada intacto. El pase anterior de L4-8 sin comparación automática de referencias se conserva (`022649`/`022700`). Una copia sintética de reporte con medida esperada 0.30 fue rechazada `semantic_mismatch` en `target/mcp-external/20260924-022725-autocad-16691db4/report.json`; otra con handles `64/65` invertidos se rechazó pese a medida correcta en `target/mcp-external/20260924-022842-autocad-ab329c4d/report.json`. Originales y negativos permanecen en `target` ignorado.

Resultado **partial scoped L1/L2/L4**: se acreditó una cota nativa sobre los puntos medios de dos LINE paralelas, actualización en OpenCADStudio tras reabrir y lectura independiente de valor/referencias DWG. El muro PlanSpec v6 aún no crea esa cota; no hay estilo, escala, offset versionado, vínculo a cara de join, ni edición asociativa ejecutada por AutoCAD. El DWG privado y `upstream` no se tocaron. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el quincuagésimo primer corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-face-dimension-association`. Conserva ambos L2, los L4 previos y negativos. Diseña PlanSpec v7 con estilo/offset explícitos y compila **una** cota de espesor en el punto medio de dos caras de un muro recto, con IDs→handles y capa; conserva v6 inmutable. Repite L2 y AutoCAD L4-9, luego edita en AutoCAD solo una copia sintética si se requiere afirmar asociatividad externa de actualización, con oráculo y entrada original inalterada. Mantén cotas de eje/unión y otros perfiles como gates separados. Prosigue M3 telemetría real y recortes CAD, M5 estilos/escala, M6 QA espacial/visual, M7 con seis imágenes inéditas aún no adjuntas y revisión L5; M8 solo merge/push en fork tras gates completos. No abras el DWG privado ni publiques privados/upstream.

## Quincuagésimo segundo corte cota de espesor compilada por PlanSpec v7 — 2026-09-24, en progreso

El corte 51 `codex/mcp-face-dimension-association` quedó publicado solo en el fork con SHA local/remoto `5b8f34d697fd4740c133ca7a62b50757df66124c` y árbol limpio. Desde `codex/mcp-planspec-v7-face-dimension`, PlanSpec v7 añade `dimension_style` local con nombre reservado `OCS_`, alturas de texto/flecha, separación, escala1 y factor de medida1; cada cota declara `dimension_placement` con offset, capa y procedencia. El compilador acepta solo un muro horizontal dirigido +x, sin aperturas/join/otras figuras, una cota `face` de izquierda a derecha en el punto medio exacto, eje y. Genera cuatro LINE y una DIMLINEAR en ese orden; primero crea/fija/activa el estilo y `DIMASSOC=2`. El offset o un parámetro del estilo cambia el hash de pasos; estaciones distintas, orientación/eje/cantidad diferente siguen `native_dimension` unsupported, sin emitir DIM. V6 sigue sin ejecutar cotas y su esquema/fixture permanecen separados.

Fixture `fixtures/synthetic-wall-face-dimension-v7.planspec.json`: espesor0.20 m, offset0.5 m, estilo `OCS_WALL_METRIC` con `dimtxt=0.035`, `dimasz=0.02`, `dimgap=0.01`, `dimscale=1`, `dimlfac=1`. El dry-run devuelve cinco comandos geométricos, 14 pasos y `executable=true`; hash de pasos `2954016cfa6e96e79525458c19db6407ce8010c4a56fbd67820c4687bd9068d0`. El esquema v7 SHA-256 `A80CB57890A6CB72B564C11C3A3A16B61A2938E180DB856EE4F77E60D0CAE240`. Masterplan Python 40/40, automatización 31/31 y `git diff --check` pasaron; Rust completo 1679/24 ignored corresponde al corte 51 y no se repitió porque v7 cambió solo Python/sonda.

L2 `target/mcp-isolated/20260924-023458-face-22244130/report.json` pasó con el mismo binario SHA-256 `BB4A7C8C97BEA93A48DF633B6F75143421EA5F738392E0CC3502EFDC84A506F8`; fixture SHA-256 `1734556EF3E5DB55E57280287CDCFCFF65522EA8885AA628C7982E38FEF9ABC6`; reporte SHA-256 `76E65FF9138222AE974225DACE08CBCA1F689FBF9E76F26F300EDDDA073CC703`. GUI aislada PID29904 salió. Handle de cara superior `64`, inferior `66`, DIM `68`; medida 0.20 inicial/reabierta y 0.25 tras editar la cara superior; los dos guardados verificaron el mismo manifiesto interno de 11 entidades. DWG antes SHA-256 `611BADDDB9E8336C82AE243FD483F98F626BCB1612756F67FB1C517BD928FED7`, después `6BF6E85602D1983198F0F639FCD05C592BC3DD875DC372804B56DAFE1CE7E667`.

AutoCAD 2025 con sonda `mcp-autocad-audit-l4-11` pasó ambos DWG: `target/mcp-external/20260924-023653-autocad-ec5f0fbb/report.json` antes y `target/mcp-external/20260924-023655-autocad-91341aa9/report.json` después. Verificó DXF 42=0.20/0.25 m, referencias DIMASSOC a handles 64/66 con osnap MID=2, estilo de cota por DXF 3 y tabla DIMSTYLE con sus cinco parámetros numéricos, INSUNITS6, AUDIT 0/0, Model=4 LINE+1 DIM, salida0 y SHA de entrada intacto. [Autodesk DIMSTYLE DXF](https://help.autodesk.com/cloudhelp/2023/ENU/AutoCAD-DXF/files/GUID-F2FAD36F-0CE3-4943-9DAD-A9BCD2AE81DA.htm) documenta los códigos 140/41/147/40/144 empleados en la sonda; la comparación efectiva está en los dos reportes. Los L4-10 anteriores permanecen como pruebas previas sin hash de fixture. Copia sintética con `text_height_m` esperada 0.04 fue rechazada `semantic_mismatch` en `target/mcp-external/20260924-023638-autocad-e138cda7/report.json`, con DWG original intacto. Se preservan asimismo los L2/L4/negativos del corte 51.

Resultado **partial scoped L2/L4** de cota compilada para este único muro horizontal. No implica legibilidad visual ni capacidad de imprimir a escala, cota de eje, muro inclinado/vertical, varios muros, join, control de estilo de texto/fuentes, edición externa ni generalización a M7. El DWG privado, originales y `upstream` no se tocaron. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el quincuagésimo segundo corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-planspec-v7-face-dimension`; conserva v6 L0, v7 L2/L4 y negativos. Amplía M4/M5 solo con contratos explícitos: cotas de eje/u otras orientaciones, estilos/capas/escala y pruebas externas por valor/ref/estilo; no infieras calidad visual de los códigos DXF. Prioriza M6.2 bounds separados y M6.5 capturas por regiones/legibilidad, M3 medición del modelo real y recortes CAD, y la campaña M7 con las seis imágenes inéditas que el usuario prometió adjuntar aquí. Congela hashes/oracle antes de L3, tres repeticiones/caso y revisión L5 humana. M8 integra/push solo al fork tras todos los gates; no abras el DWG privado ni publiques privados/upstream.

## Quincuagésimo tercer corte QA visual de colocación de cota — 2026-09-24, en progreso

El corte 52 `codex/mcp-planspec-v7-face-dimension` quedó publicado solo en el fork con SHA local/remoto `bb97042d9a610b3e46a0f94b5f6cb4c0b2667ae3` y árbol limpio. Desde `codex/mcp-dimension-visual-qa` se añadió captura fenced `drawing_only` a `mcp_face_dimension_smoke.py` tras ZOOM EXTENTS, con hash/revisión/dimensiones y sin Base64 en el reporte. La primera captura del fixture v7 original quedó en `target/mcp-isolated/20260924-023923-face-34188589/dimension-before.png`, SHA-256 `B872318D5D75955BB58C276FD18A4885247DF5DCCF03815662BB378D76ADABA7`: en 1665×823 la línea de cota vertical de 0.20 m aparece **dentro** del muro horizontal. El L2 original de corte52 se conserva intacto; la captura expone un defecto de presentación no visible en DXF 42/estilo.

El dry-run ahora informa `dimension_placement_qa` separado del contorno arquitectónico: para este caso `wall_end_x_m=4`, `dimension_line_x_m=2.5`, `status=inside_wall_bounds`. Devuelve `quality_blockers=[dimension_line_inside_wall_bounds]` y `executable=false` para el fixture interior; conserva sus comandos y hash con fines diagnósticos. Preflight L0 sin CAD `target/m6-dimension-placement-preflight/inside-cb4fa1d8/report.json`, SHA-256 `434A50099984F73F3F3D32A4CA021DFFBC548EA811775E96A35E5266C7A08215`. Este bloqueo de calidad se introdujo después del ensayo histórico del corte52, que permanece como evidencia válida de semántica CAD, **no** de aceptación visual.

El fixture paralelo `fixtures/synthetic-wall-face-dimension-exterior-v7.planspec.json`, SHA-256 `0B4E76DD9119DDA453FD187F51B977EDA8372C9EDF9AD995C8A41B00F441EF30`, cambia solo `offset_m=2.5`: línea de cota `x=4.5` fuera del extremo del muro `x=4`, `quality_blockers=[]`, `executable=true`. Su primer L2/captura en `target/mcp-isolated/20260924-024030-face-7fc2f907` quedó conservado. El L2 final con regla activa `target/mcp-isolated/20260924-024310-face-e977807a/report.json`, SHA-256 `2E1C04C171B0E533BC074445C53F9EBA3AFACC8B0832E9C791BB7050B28731B4`, pasó: 0.20 m inicial/reabierta, 0.25 m tras editar cara superior, manifiesto estable de 11 entidades, GUI aislada salió, captura limpia `dimension-before.png` SHA-256 `DA98B6A6F887706DACB39DD9A6B72D9467E6FC1C0CEE75ACB12FF5AB16320301`, 1665×823, frame `shader_encoded_frame`, revisiones fijadas. La captura exterior se inspeccionó: la línea de cota quedó fuera del muro, aunque el texto sigue pequeño en la vista completa; no se declara legibilidad L5. DWG antes SHA-256 `D0BEDCE59CFBC9FDD734045E4B56DF9A6341040DDDB4FE7FFD5C83F300288C30`, después `A9A21E942EE4A348B32522B089C6278B2E8F0FC8644698C3A881BD6AA82EEF91`.

AutoCAD L4-11 final pasó ambos DWG en `target/mcp-external/20260924-024333-autocad-6e8db1f9/report.json` y `target/mcp-external/20260924-024337-autocad-8d23280a/report.json`: fixture hash válido, DXF 42=0.20/0.25, dos refs MID a handles 64/66, seis propiedades de estilo iguales, INSUNITS6, AUDIT 0/0, salida0 y SHA intacto. Pruebas Python masterplan 41/41, automatización 31/31 y `git diff --check` pasaron. El binario es el mismo SHA-256 `BB4A7C8C97BEA93A48DF633B6F75143421EA5F738392E0CC3502EFDC84A506F8`. M6.2/M6.5 queda **partial scoped**: solo detecta posición de línea de esta cota horizontal; no calcula extensión de texto, flechas, otras anotaciones, recortes por zonas ni legibilidad humana. DWG privado, ensayos históricos y `upstream` intactos. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el quincuagésimo tercer corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-dimension-visual-qa`; conserva captura interior fallida, preflight quality_blocked, exterior L2/L4 y sus hashes. Continúa M6.2 con bounds arquitectónicos/anotación general y M6.5 con recortes de regiones, métricas legibles y revisión humana; no conviertas inspección visual de un fixture en aceptación M7. Extiende PlanSpec por contratos versionados para otras cotas/orientaciones, mantiene estilos/escala por gate, y aborda M3 telemetría real de Luna/supervisor. Las seis imágenes inéditas prometidas para M7 aún no se han adjuntado: espera sus archivos, congela hashes/oracle, ejecuta tres repeticiones por caso y L5 con revisión del usuario. M8 solo merge/push al fork tras gates; no abras el DWG privado ni publiques privados/upstream.

## Quincuagésimo cuarto corte bounds de fuentes separados — 2026-09-24, en progreso

El corte 53 `codex/mcp-dimension-visual-qa` quedó publicado únicamente en el fork con SHA local/remoto `9aaefca5f2e6d75c6e6e5c1eaa73377378c8af65` y árbol limpio. Desde `codex/mcp-separated-bounds-qa`, el dry-run agrega `source_bounds` v1 en metros: `architecture_bounds_m` a partir de líneas, círculos y envolventes de espesor de muros; `annotation_reference_bounds_m` a partir de nodos de cotas y, para la única cota v7 compilada, el punto de colocación. El reporte no une anotaciones al perímetro arquitectónico. `unresolved` declara extremos de arco de puerta y colocación/render de cotas no compiladas. El alcance excluye texto, flechas, glifos, símbolos complejos y bounds de píxeles renderizados; no es una detección general de colisiones. Tests distinguen bounds de un fixture v1 con cota no compilada, el v7 exterior y el símbolo de puerta sin extremos de arco resueltos.

El fixture exterior v7 conservado del corte53 produjo en L0 arquitectura `[0,4]×[-0.1,0.1]` m y referencias de anotación `[2,4.5]×[-0.1,0.1]` m. L2 final `target/mcp-isolated/20260924-024655-face-1cb6de03/report.json`, SHA-256 `C56AD388DF6A63B725B1723CEEB71BC8AF451176BEF8C9324ABB420F3E43CB8F`, incluye esos bounds, captura limpia SHA-256 `DA98B6A6F887706DACB39DD9A6B72D9467E6FC1C0CEE75ACB12FF5AB16320301`, medida 0.20→0.25 m, guardados de manifiesto estable y GUI salida. Los DWG conservan hashes exactos del corte53: antes `D0BEDCE59CFBC9FDD734045E4B56DF9A6341040DDDB4FE7FFD5C83F300288C30`, después `A9A21E942EE4A348B32522B089C6278B2E8F0FC8644698C3A881BD6AA82EEF91`. AutoCAD L4-11 final en `target/mcp-external/20260924-024712-autocad-0ef5ae85/report.json` y `target/mcp-external/20260924-024714-autocad-2a057e30/report.json` pasó ambos con medida/ref/estilo, AUDIT 0/0, INSUNITS6, salida0 y SHA intacto. Masterplan Python 42/42, automatización 31/31 y `git diff --check` pasaron.

M6.2 queda **partial L0/L2**: separa huellas de fuente con casos explícitos, pero no puede calificar legibilidad, sobreposición real de texto ni bounds de entidades CAD tras render. Los DWG privados, captura interior histórica y `upstream` siguen intactos. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el quincuagésimo cuarto corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-separated-bounds-qa`; conserva L2/L4, capturas y preflight interior. Prosigue M6.2 con extents reales de puerta/arco, anotación, bloque/HATCH y perfiles de escala antes de promover colisiones; M6.5 necesita regiones CAD y revisión visual L5. M3 aún requiere telemetría real Luna/supervisor y comparación de recortes; M4/M5 requieren cotas de eje/orientación, puertas/uniones más complejas, estilo/escala y edición externa. Las seis imágenes inéditas reservadas no se han adjuntado para M7: congela hashes/oracle al recibirlas, tres repeticiones/caso y revisión humana. M8 merge/push solo al fork tras gates completos. No abras el DWG privado ni publiques privados/upstream.

## Quincuagésimo quinto corte perfil legible de cota sintética — 2026-09-24, en progreso

El corte 54 `codex/mcp-separated-bounds-qa` quedó publicado solo en el fork con SHA local/remoto `ab845570abe88fb3758961bb592aca56de86a3e8` y árbol limpio. Desde `codex/mcp-dimension-legibility-profile`, el fixture paralelo `fixtures/synthetic-wall-face-dimension-readable-v7.planspec.json` conserva el muro, la cota exterior y sus bounds del corte54; cambia únicamente estilo a `OCS_WALL_READABLE`, altura de texto0.07 m, flecha0.035 m y separación0.015 m, con escala/factor1. SHA-256 fixture `02B40356E31F5E710C024DEF374C71F4382F3D6EDBC50BB32641643664BB5490`. El hash de pasos difiere del perfil compacto mientras los cinco comandos geométricos y los bounds de fuente son idénticos; test Python lo verifica. El harness permite este nombre exacto y la sonda AutoCAD fija su hash antes de comparar.

L2 `target/mcp-isolated/20260924-024926-face-cc7c0a49/report.json`, SHA-256 `7EDE4B2F586D49E95495AC1A983736DF6657F53CCFAF45084012A0E5295181D9`, pasó con GUI aislada salida, medida0.20 inicial/reabierta y 0.25 tras editar la cara, dos save_verified con manifiesto estable. La captura fenced `dimension-before.png` SHA-256 `ED02F6E579ACD5CF5395F1152C64AED9252CFECAE2D061E22ADD12C2632E584F`, 1665×823, `drawing_only`, se inspeccionó: el texto se ve claramente mayor que en la captura exterior previa de DIMTXT0.035; la dimensión sigue fuera del muro. Esto es una observación de este viewport, no una prueba de legibilidad en impresión ni aceptación humana L5. DWG antes SHA-256 `BCCA9AC94C716EFCFF7997F2E4AF29263CFFB40EC48E46E0392E34E4E1F24CA4`, después `E22EF188ECFDC3790FBF293D9D4A593239A37F1A5942D0C053FD141AD1F3D30E`.

AutoCAD Core Console 2025 L4-11 pasó antes/después en `target/mcp-external/20260924-024951-autocad-f35a9855/report.json` y `target/mcp-external/20260924-024954-autocad-675865c9/report.json`: fixture hash válido, DXF42=0.20/0.25, MID refs a handles64/66, estilo `OCS_WALL_READABLE`, DIMTXT0.07, DIMASZ0.035, DIMGAP0.015, DIMSCALE/DIMLFAC1, AUDIT0/0, INSUNITS6, salida0 y SHA DWG intacto. Python masterplan 43/43, automatización 31/31 y `git diff --check` pasaron; no se modificó Rust ni se reejecutó su suite completa. M5.7/M6.5 queda **partial scoped L2/L4**: no hay PDF/plot a escala, varios tamaños de plano, fuentes comparadas ni revisión L5 del usuario. No se tocó el DWG privado ni `upstream`. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el quincuagésimo quinto corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-dimension-legibility-profile`. Conserva los tres estilos/capturas v7, la captura interior fallida y los reportes L4. Prosigue M6 bounds de arcos/texto/bloques y QA visual por regiones, M5 formatos de impresión/escala y fuentes con oráculo, M4 casos de cotas/muros adicionales y M3 telemetría de modelo real. Para M7 el usuario confirmó motor CAD y prometió seis imágenes inéditas autorizadas, pero todavía deben adjuntarse; al recibirlas congela hashes, clasificación 2/2/2 y oracle antes de ejecutar Luna, tres repeticiones por caso y revisión L5 humana. M8 integra/push únicamente al fork tras gates completos; no abras el DWG privado ni publiques privados/upstream.

## Quincuagésimo sexto corte bounds del giro de puerta — 2026-09-24, en progreso

El corte 55 `codex/mcp-dimension-legibility-profile` quedó publicado solo en el fork con SHA local/remoto `156426b1a44083a00758e840f2fb14a2d9228049` y árbol limpio. Desde `codex/mcp-door-swing-bounds`, `source_bounds` añade a la huella arquitectónica de puerta v4 bisagra, extremos de hoja y extremos cardinales interiores al cuarto de arco, derivados de muro dirigido, espesor, lado, bisagra, offset y ancho. La prueba adversarial rota el muro 45°, alarga la hoja a 2.4 m y obliga a incluir el máximo cardinal intermedio; el bbox no queda limitado a extremos del arco. El harness L2 incluye el resultado en su reporte. Una puerta v3 sin `swing` conserva `door_swing_without_v4_contract` en `unresolved`; no se infieren bounds de un contrato ausente.

L2 del fixture de puerta previo pasó en `target/mcp-isolated/20260924-025336-21aa919f/report.json`, SHA-256 `8B1295D70918808921EE556A9659443A3F65391681BADBBEC57CF112C3F727FA`: `source_bounds` arquitectura `[0,4]×[-0.1,3]` m, anotación nula y `unresolved=[]`; comandos SHA `FB5B575C2C627B1EF8056D89D4478AAA72F49F604925BC3E2FEACE619CF437E7`, manifiesto 13 LINE+1 ARC reabierto y GUI salida. El DWG sintético SHA-256 `0958285A0CF70F7B24883383724DB2F6FDB3D9E674B4FB5F236D4001A1E9FD27` coincide con el corte anterior. AutoCAD L4 en `target/mcp-external/20260924-025353-autocad-c95bc9f6/report.json`, SHA-256 `CA705E74FE075F81144A061D7D47E8A43A858D2C348391DBD20C68A19DD9B7A3`, confirmó 14 entidades, geometría fuente, AUDIT 0/0, salida0 y SHA de entrada intacto. Python masterplan 44/44, automatización 31/31 y `git diff --check` pasaron. No se cambió el binario Rust.

M6.2 queda **partial scoped L0/L2/L4**: calcula envolvente 2D de la fuente para este giro v4, sin texto, flechas, símbolo completo de otros tipos, bloques/HATCH ni extents renderizados. La habitación del fixture domina el bbox L2; el test girado comprueba el extremo que el fixture no revela. Este cambio no acredita colisiones visuales ni L5. El DWG privado, los reportes anteriores y `upstream` permanecen intactos. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el quincuagésimo sexto corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-door-swing-bounds`. Conserva los 56 cortes y sus fixtures/evidencias, especialmente la captura interior de cota fallida. Prosigue M6.2/M6.5 con extents de texto/flechas/bloques/HATCH y recortes por región con oráculo; M5 necesita perfil de impresión/escala y fuentes, M4 cotas/ejes/topologías adicionales, M3 telemetría del modelo real y descomposición de tiempos. Las seis imágenes inéditas autorizadas para M7 aún deben adjuntarse aquí: al recibirlas congela hashes, cohorte 2/2/2 y oracle antes de Luna, luego tres repeticiones por caso y revisión L5 humana. M8 requiere gates completos y merge/push solo al fork. No abras el DWG privado, no alteres ensayos históricos y no publiques archivos privados ni a `upstream`.

## Quincuagésimo séptimo corte intersecciones de círculos — 2026-09-24, en progreso

El corte 56 `codex/mcp-door-swing-bounds` quedó publicado únicamente en el fork con SHA local/remoto `f811b601bd11df1dd2a9606ddaaef36492928853` y árbol limpio. Desde `codex/mcp-circle-intersection-qa`, `analyze_geometry` versiona el reporte como `planspec-geometry-qa-2` y añade contactos de segmento LINE con CIRCLE (`secant`, `tangent`, `one_on_segment`) y pares de CIRCLE (`secant`, `tangent`). Usa coordenadas Decimal, discriminante y parámetros de segmento para excluir raíces fuera del tramo; los círculos concéntricos de distinto radio y pares distantes no generan contacto. IDs ordenados hacen el reporte independiente del orden de entrada. Un contacto lleva a `review_required`, no a rechazo automático, pues PlanSpec aún no expresa la intención topológica de cada línea/círculo.

La prueba sintética cubre secante, tangencia, una sola raíz en extremo de segmento, círculo distante, secante y tangente entre círculos, y permutación de nodos/círculos. Python masterplan 45/45, automatización 31/31 y `git diff --check` pasaron. Solo cambió el análisis previo a CAD: compilación y binario Rust quedaron intactos; este corte no reclama L2/L4 ni validación de entidades después de guardar. M6.1 sigue **partial L0**. Se conservaron todos los reportes históricos, el DWG privado y `upstream`. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el quincuagésimo séptimo corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-circle-intersection-qa`. Preserva los 57 cortes y evidencias. Completa M6 por categoría y compara entidades CAD/recortes regionales con oráculo; en M4/M5 amplía cotas, uniones, escala, estilos y simbología con fixtures sintéticos y motor externo. M3 requiere telemetría real de Luna y comparación controlada de recortes. Para M7 espera las seis imágenes inéditas autorizadas prometidas por el usuario; congela hashes/cohorte 2/2/2/oracle antes de L3, tres repeticiones por caso y revisión L5 humana. M8 merge/push solo al fork tras gates completos. Nunca abras/modifiques el DWG privado ni publiques privados o a `upstream`.

## Quincuagésimo octavo corte recortes regionales trazables — 2026-09-24, en progreso

El corte 57 `codex/mcp-circle-intersection-qa` quedó publicado solo en el fork con SHA local/remoto `dc81634e6ed8e2d60a2c6f64c463876353f4c71f` y árbol limpio. Desde `codex/mcp-region-artifact-review`, `region_evidence.py` admite únicamente reportes `passed` con ArtifactRef de PNG `drawing_only`, valida hash de fuente, documento y revisiones antes de derivar hasta 12 recortes por rectángulos de píxeles estrictos. El manifiesto enlaza hash del reporte padre, ArtifactRef padre, rectángulos e hijos y fija `human_review_status=pending`. `verify_regions` detecta cambio del reporte, PNG original o recortes; una segunda ejecución con el mismo nombre se rechaza sin sobrescribir. El CLI y su alcance se explican en `REGION-EVIDENCE.md`; requiere Pillow. No se solicita nueva captura ni se modifica el PNG/reporte originales.

La captura de puerta sintética L2 del corte56 (`target/mcp-isolated/20260924-025336-21aa919f/report.json`, SHA-256 `8B1295D70918808921EE556A9659443A3F65391681BADBBEC57CF112C3F727FA`) generó `regions/manifest.json`, SHA-256 `1219F31F38A6B10DD30CF8CF4CC7F50AFFA78DEE42170524BA9ECFB9E00787D6`; PNG padre SHA-256 `23058B933DEC9409D0BF263EBEAAE790E7E5AF0247EA5AA5D89432C43436D906` conservado. Dos recortes inspeccionados: `door-swing.png` SHA-256 `20588F8805409B308F0E214980107D8904A51948CCE4AB1657514DDE9321B1CF` y `opening-and-jambs.png` SHA-256 `7B68D802117779ADF83FD5BDB03225CDE56FCD284195758FE01333E79E68445D`. `verify_regions` pasó. Python masterplan 47/47, automatización 31/31 y `git diff --check` pasaron. No cambió Rust ni hubo nuevo DWG.

M6.5 permanece **partial scoped L1/L2**: los rectángulos se eligieron manualmente en píxeles de un único viewport, sin mapeo semántico CAD↔píxel ni oráculo, varios tamaños, revisión humana o aceptación L5. El DWG privado y reportes originales permanecieron intactos; no hubo envío a `upstream`. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el quincuagésimo octavo corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-region-artifact-review`. Preserva los 58 cortes y su evidencia, incluidos PNG originales y recortes. Continúa M6.5 con mapeo verificable CAD→viewport y revisión por zonas con oráculo; M6.3 necesita colisiones por categoría y M6.4 cotejo de entidades posguardado. M4/M5 aún requieren cotas/topologías/escala/fuentes, M3 enlace real Luna/supervisor. El usuario prometió seis imágenes inéditas para M7 pero debe adjuntarlas: congela hashes, cohorte 2/2/2 y oracle antes de L3, tres repeticiones por caso y L5 humano. M8 integra solo en el fork tras gates completos. No abras el DWG privado ni publiques privados/upstream.

## Quincuagésimo noveno corte linaje de píxeles regionales — 2026-09-24, en progreso

El corte 58 `codex/mcp-region-artifact-review` quedó publicado únicamente en el fork con SHA local/remoto `359ea9dabb62901d63b95f3ce5945db73cb9e74d` y árbol limpio. Auditoría del verificador encontró que comprobar el hash de padre e hijo no demostraba que los píxeles del hijo provinieran del rectángulo declarado: un manifiesto e hijo sustituidos juntos podían pasar las comprobaciones anteriores. Desde `codex/mcp-region-pixel-lineage`, `verify_regions` vuelve a abrir el PNG padre y cada hijo, valida status/overlay, cardinalidad, slug, rectángulo, dimensiones, hash y revisión, y compara todos los píxeles RGBA del hijo con el crop indicado del padre. Una prueba sustituye el PNG hijo por otro del mismo tamaño y actualiza su ArtifactRef con hash válido; ahora se rechaza por divergencia de píxeles. El manifiesto y PNG originales del corte58 se verificaron sin cambio.

Python masterplan 48/48, automatización 31/31, `verify_regions` sobre `target/mcp-isolated/20260924-025336-21aa919f/regions/manifest.json` y `git diff --check` pasaron. M6.5 sigue **partial scoped L1/L2**: linaje de píxeles correcto para este viewport, pero rectángulos manuales, sin mapeo CAD→píxel ni oráculo/L5. DWG privado, run histórico y `upstream` intactos. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el quincuagésimo noveno corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-region-pixel-lineage`. Conserva los 59 cortes y sus artefactos originales; verifica crops por píxel y no solo por hash. Prosigue M6.3/M6.4/M6.5 con colisiones por categoría, cotejo CAD y mapeo de regiones; amplía M4/M5 por contratos, M3 con medición real Luna/supervisor. Para M7 faltan las seis imágenes inéditas autorizadas prometidas: al recibirlas congela hashes, cohorte 2/2/2 y oráculo antes de L3, ejecuta tres repeticiones por caso y solicita L5 humana. M8 integra solo al fork tras gates completos. No abras el DWG privado ni publiques privados/upstream.

## Sexagésimo corte cota vinculada de muro vertical — 2026-09-24, en progreso

El corte 59 `codex/mcp-region-pixel-lineage` quedó publicado solo en el fork con SHA local/remoto `b7ac61fae9c0cebfe29843cca45eeee491139234` y árbol limpio. Desde `codex/mcp-vertical-face-dimension`, PlanSpec v7 permite también un muro vertical ascendente único sin huecos/uniones ni geometría extra, con referencias `left/right` en la estación media, cota lineal horizontal y estilo métrico explícito. El fixture nuevo `synthetic-wall-face-dimension-vertical-v7.planspec.json`, SHA-256 `3F495D44A2F268F5C09BB7EE66B174A560BAF5714114305760A78D9C85731039`, compila cuatro LINE y `DIMLINEAR -0.1,2 0.1,2 0,4.5`; línea y=4.5 exterior al extremo del muro y=4. Un offset interior se bloquea en L0. `source_bounds` mantiene arquitectura y anotación separadas. Los fixtures horizontales previos conservan su salida.

L2 `target/mcp-isolated/20260924-031151-face-fbe4211d/report.json`, SHA-256 `2B65F3558BB0B0A1A71184D0081BAB18906CBEB4E786E626F8268984C12FF64F`, pasó: binario SHA-256 `BB4A7C8C97BEA93A48DF633B6F75143421EA5F738392E0CC3502EFDC84A506F8`, comandos SHA `A7069D50908C94D98B0A21D1DCD5404F165023AB94107160A09E686BC747060A`, medida 0.20 inicial/reabierta y 0.25 tras mover la primera cara x=-0.1→-0.15, dos guardados con manifiesto 11 estable y GUI salida. DWG antes SHA-256 `148937C36D08417C64B3D102A434B8319C72AEA491719517252E570CB0810938`, después `3459418091FC7FF357A0383873F8AB6834A582C3DEAA40C7625CFB514E3DDFA6`. Captura `dimension-before.png` fenced `drawing_only` SHA-256 `422188DFF22D026CA37DAFACB6C54EF167DCB23A7F076EF88CFD84B8B6CF711D`, 1665×823, inspeccionada: el cap superior del muro está por debajo de la línea de cota.

La primera regla L4 de colocación exigió igualdad del punto DXF10 completo. Pasó antes de editar y **falló de forma legítima** después: DXF10 desplazó x=0.1→0.075 mientras y=4.5 se conservó al actualizar la asociación. El reporte rechazado `target/mcp-external/20260924-031406-autocad-40f16bba/report.json` se preserva. La regla corregida coteja solo la coordenada perpendicular que fija la línea de cota y la rotación; medida y referencias se cotejan por separado. AutoCAD 2025 L4-11 final pasó antes y después en `target/mcp-external/20260924-031443-autocad-b21aec79/report.json` (SHA-256 `153D17112E9EF4645366041B3BE3324D2AA75752324438CF704FC669BD31FD66`) y `target/mcp-external/20260924-031443-autocad-964202b0/report.json` (SHA-256 `36E7E9A062BD83A34CF5A9BFA99C761B33197E74377569A569173440D593917C`): DXF42 0.20/0.25, dos MID refs handles 64/66, estilo, línea y=4.5, rotación0, AUDIT0/0, INSUNITS6, salida0 y SHA de entrada intacto. La regresión horizontal final `target/mcp-external/20260924-031443-autocad-82f47c4d/report.json` también pasó con línea x=4.5 y rotación π/2. Python masterplan 49/49, automatización 31/31 y `git diff --check` pasaron; Rust no cambió.

M4/M5 quedan **partial scoped L0/L2/L4**. El ensayo de asociación mueve solo una cara: demuestra que la cota sigue la referencia, pero deja el contorno del muro editado abierto y no acredita una operación semántica de cambio de espesor. Faltan cotas oblicuas/ejes, uniones, escala/impresión, legibilidad por región, y edición controlada en motor externo. No se abrió el DWG privado ni se alteró ningún ensayo histórico; no hubo envío a `upstream`. Verificar SHA remoto al publicar.

### Prompt de continuidad desde el sexagésimo corte

Verifica `AGENTS.md`, Git/remotos, estado y SHA local/remoto de `codex/mcp-vertical-face-dimension`. Conserva el primer L4 de colocación rechazado y las capturas/DWG previos. El siguiente paso M4/M5 debe probar una edición semántica atómica de espesor que mueva cara y caps sin abrir el contorno, verificando la cota y topología en L2/L4; después ampliar ejes/oblicuas y escala de anotación. Prosigue M3 telemetría Luna/supervisor y M6 QA por categorías/regiones. Para M7 faltan las seis imágenes inéditas autorizadas prometidas: congela hashes, cohorte 2/2/2 y oráculo antes de L3, tres repeticiones por caso y L5 humana. M8 integra únicamente al fork tras gates completos. No abras el DWG privado ni publiques privados/upstream.

## Sexagésimo primer corte transacción de espesor con contorno cerrado — 2026-09-24, en progreso

El corte 60 `codex/mcp-vertical-face-dimension` quedó publicado solo en el fork con SHA local/remoto `90c8006b0fb9c3692b3e46f06969c6f783f24807` y árbol limpio. En `codex/mcp-wall-thickness-transaction` se añadió `edit_wall_thickness` como operación directa MCP con cuatro handles LINE ordenados, documento/revisión y espesor esperado/nuevo. El backend valida capas desbloqueadas, mismo layer/owner, plano z=0, rectángulo cerrado horizontal o vertical y espesor vigente antes de modificar. Cambia ambas caras y los dos caps alrededor del eje en un solo evento GUI y una entrada undo. Si `update_entity` devuelve error, intenta revertir los bordes ya escritos y devuelve fallo; una reversión fallida se informa de forma explícita. Se excluyó de los pasos batch para mantener el límite transaccional. El cliente usa `request_id` y consulta `operation` para recuperar el resultado; una repetición del mismo `request_id` devolvió el resultado previo sin aumentar la revisión. Alcance: rectángulo simple de cuatro LINE, sin huecos, joins ni muro inclinado; no se reclama aislamiento frente a fallos del proceso durante la actualización interna.

El primer ensayo L2 `target/mcp-isolated/20260924-033335-wall-edit-2ee7855b/report.json` falló por lectura incorrecta de la forma JSON de `query` en el arnés; quedó preservado y la GUI aislada PID15780 fue detenida tras comprobar su ejecutable. Los primeros pases L2/L4 (`033454` vertical, `033511` horizontal) permanecen como evidencia histórica. El pase final vertical `target/mcp-isolated/20260924-033734-wall-edit-7b3fd23b/report.json`, SHA-256 `35DD92FE5E239F6FA025B2012309AE2663B5EFB9783BB7DE15C6952813EF237E`, y horizontal `target/mcp-isolated/20260924-033827-wall-edit-4209e210/report.json`, SHA-256 `59A1BE97EFFE995C11B5B504755DE7E994661CDFDE01DD80B2D411C2A9C802E3`, pasaron con binario SHA-256 `C1C1298009E5DD2A1F52CD40B957F2ECD3C64E99AAB6C00621962AB3B1EFD360`. Ambos comprobaron contorno cerrado 0.20→0.25 m, DIM asociativa 0.25 m, reapertura DWG, operación recuperada, repetición idempotente y salida GUI. DWG vertical antes/después SHA-256 `F58203756CB4869240072A6AAA649D97CB9AEC365CD1F8CF49037C57E87D83D1`/`5C190C90EE494E48880A4207ED689DABC3E2215DA968EB8507445A9B7C6B171F`; horizontal `9ED7417D73D3143107F785E56D55D023768FA56DEF24C93A1953FC3027CAFCBD`/`76866AF4F0A690ACD96C0431A6899878E782346913AFC6CC2FADA2AF9B6785C4`.

AutoCAD Core Console 2025 L4-11 pasó los cuatro DWG finales en `target/mcp-external/20260924-033908-autocad-980dfa1e/report.json`, `20260924-033916-autocad-b93ef48b`, `20260924-033926-autocad-39d26944` y `20260924-033931-autocad-3d4d0255`: `audit_and_census_passed`, geometría 4/4, refs MID 2/2, medida, estilo, colocación, INSUNITS6, fuente/hash de fixture, Model=5 y SHA de entrada intacto. Una copia con el handle del segundo borde sustituido por el primero fue rechazada `semantic_mismatch` con 1 error geométrico y 0 de cota en `target/mcp-external/20260924-033952-autocad-624e0027/report.json`, SHA-256 `A0D529D4DD6B4179B42749945351DCEA01D5D56D43EEF3CA54E27AB0618437CD`. El DWG original y los reportes anteriores no se modificaron.

Rust completo: 1681 passed, 24 ignored, 0 failed; Python PlanSpec 49/49 y automatización 31/31; `git diff --check` limpio. Queda **partial scoped L1/L2/L4** para edición rectangular simple. M3 requiere telemetría real Luna/supervisor; M4/M5, ejes/oblicuas, joins, escala e impresión; M6, oráculos por categoría y mapa CAD→píxel; M7, seis imágenes inéditas prometidas pero aún no adjuntas, cohorte congelada, tres repeticiones y L5 humana. M8 no está aceptado. No se abrió el DWG privado ni se envió nada a `upstream`. Verificar SHA local/remoto tras publicar.

### Prompt de continuidad desde el sexagésimo primer corte

Verifica `AGENTS.md`, Git/remotos, SHA local/remoto y árbol limpio de `codex/mcp-wall-thickness-transaction`. Preserva el L2 fallido, primeros pases, L2/L4 finales y negativo de handle. Continúa M4/M5 con cotas de eje/oblicuas y perfil de impresión mediante contratos versionados, y M6 con oráculos geométricos y regiones trazables. M3 aún necesita medición real Luna/supervisor. Solicita las seis imágenes inéditas autorizadas que el usuario prometió adjuntar; antes de L3 congela hashes, cohorte 2/2/2 y oráculo, luego ejecuta tres repeticiones por caso y revisión L5 humana. M8 solo tras completar gates, con merge/push únicamente al fork. No abras/modifiques el DWG privado, no reescribas evidencia histórica ni publiques archivos privados/upstream.

## Sexagésimo segundo corte paridad exacta de bordes tras reapertura — 2026-09-24, en progreso

El corte 61 `codex/mcp-wall-thickness-transaction` quedó publicado solo en el fork con SHA local/remoto `ffa883748dc588b73d263adc9683beb28da4cfe4` y árbol limpio. Desde `codex/mcp-wall-reopen-parity`, el arnés L2 compara las 24 coordenadas de los cuatro LINE identificados por handle, a 1e-6 m, antes y después de cada guardado/reapertura DWG; conserva el chequeo de contorno cerrado, espesor, cota asociativa, `request_id` repetido sin nueva revisión y salida GUI. Los reportes L2 vertical `target/mcp-isolated/20260924-034330-wall-edit-8cdb7084/report.json` SHA-256 `E00F4B6BB099281145BED912764960366022643D4F892AE466F945F4043EBE27` y horizontal `target/mcp-isolated/20260924-034330-wall-edit-3dd7b4d2/report.json` SHA-256 `9FB743AF68267B0E9E54C6C445D07FC63CEC0AB1AAD6142230CD5D57F421B9E8` pasaron.

AutoCAD L4-11 reabrió ambos DWG antes/después y confirmó en los cuatro casos `audit_and_census_passed`, `matched_scoped` y 4/4 segmentos geométricos: `target/mcp-external/20260924-034402-autocad-cddf10a9/report.json`, `20260924-034406-autocad-0c3e3c4d`, `20260924-034409-autocad-be1165c1` y `20260924-034413-autocad-e2aa6600`. El backlog M6.4 se actualizó a **partial L1/L2/L4**. Se preservaron los reportes y DWG del corte 61. Este corte no cambió Rust ni el oráculo AutoCAD: exige paridad interna de los cuatro LINE de este muro rectangular y conserva pendientes otras entidades/topologías, edición externa y evaluación visual L5. No se abrió el DWG privado ni se envió a `upstream`. Verificar SHA local/remoto tras publicar.

### Prompt de continuidad desde el sexagésimo segundo corte

Verifica `AGENTS.md`, Git/remotos, SHA local/remoto y árbol limpio de `codex/mcp-wall-reopen-parity`. Conserva cortes 60–62, L2/L4/negativos y sus hashes. Amplía M6.4 por entidades/topologías con oráculos independientes, M6.3 por colisiones semánticas y M6.5 por regiones CAD→píxel; M4/M5 necesitan cotas de eje/oblicuas y escala/impresión. M3 requiere telemetría real Luna/supervisor. Cuando el usuario adjunte las seis imágenes inéditas autorizadas para M7, congela hashes, cohorte 2/2/2 y oráculo antes de L3; tres repeticiones/caso y revisión L5 humana. M8 solo tras gates completos y publicación/merge al fork; no abras/modifiques el DWG privado ni publiques privados/upstream.

## Sexagésimo tercer corte cruce de hoja abierta con contorno — 2026-09-24, en progreso

El corte 62 `codex/mcp-wall-reopen-parity` quedó publicado solo en el fork con SHA local/remoto `e24c42a389a055cd342c0d53250dcf50a43ea808` y árbol limpio. Desde `codex/mcp-door-leaf-clearance-qa`, PlanSpec añade `door_clearance_qa` v1 para v4–v7: calcula la hoja abierta desde bisagra, lado, ancho y eje del muro declarados; detecta cruces interiores estrictos con líneas fuente, distingue categoría `contour` de `unclassified`, y ordena el reporte por ID. Los cruces con contorno incorporan `door_open_leaf_crosses_contour` a `quality_blockers` y desactivan `executable`; una línea sin clase queda en `review_required` sin inferir que sea obstáculo. No cambia los comandos CAD ni su SHA del fixture base.

Prueba sintética sobre `synthetic-door-swing.planspec.json` intacto, SHA-256 `47AC031F90BB53C2290450107D32A37FDBD14F8A02B397FD7E1792B37866F98C`: base `clear` y ejecutable; línea adicional x=0.5..1.5, y=0.5 cruza la hoja abierta x=1, y=0.1..1 y se reporta `unclassified`; un contorno norte desplazado a y=0.5 se reporta `contour`, bloquea emisión y conserva la variante solo en memoria de test. PlanSpec 50/50, automatización 31/31 y `git diff --check` pasaron. No se ejecutó CAD porque este corte modifica solo QA previo y su caso adversarial queda bloqueado. M6.3 sigue **partial L0**: no analiza el barrido del arco, contactos en extremos, volumen 3D, muebles tipados ni múltiples símbolos CAD. No se abrió el DWG privado, no se alteró evidencia histórica ni se envió a `upstream`. Verificar SHA local/remoto tras publicar.

### Prompt de continuidad desde el sexagésimo tercer corte

Verifica `AGENTS.md`, Git/remotos, SHA local/remoto y árbol limpio de `codex/mcp-door-leaf-clearance-qa`; conserva L2/L4/negativos anteriores. Extiende M6.3 a barrido de puerta, contactos y obstáculos tipados mediante contrato versionado antes de aceptar colisiones generales; para M6.5 falta mapa CAD→píxel y L5. M4/M5 aún requieren cotas de eje/oblicuas, joins y perfil de impresión; M3 necesita telemetría Luna/supervisor. Las seis imágenes inéditas de M7 deben ser adjuntadas; congela hashes/cohorte 2/2/2/oráculo antes de L3, tres repeticiones/caso y revisión humana. M8 solo tras gates completos y merge/push al fork. Nunca abras/modifiques el DWG privado ni publiques privados/upstream.

## Sexagésimo cuarto corte sector barrido de puerta — 2026-09-24, en progreso

El corte 63 `codex/mcp-door-leaf-clearance-qa` quedó publicado solo en el fork con SHA local/remoto `88ffac5e10723d5fdb7e03d142dca0fa28294bab` y árbol limpio. En `codex/mcp-door-sweep-qa`, `door_clearance_qa` v2 añade la intersección del segmento fuente con el interior del sector circular de 90° barrido por la hoja. Transforma cada línea al sistema bisagra/eje de cierre/eje de apertura, recorta su parámetro contra las dos semirrectas positivas y minimiza distancia radial en el tramo recortado con Decimal; excluye tangencia y contacto puntual. Conserva `strict_crossings` de la hoja abierta, añade `sweep_intersections` y bloquea `executable` solo si el sector cruza una línea declarada de contorno. Una línea `unclassified` produce revisión sin afirmar obstrucción física. El esquema QA sube a v2; comandos CAD y fixture base no se alteran.

La prueba sintética cubre base clara, cruce de la hoja abierta, línea enteramente dentro del sector que evita la hoja, línea exterior tangente, cruce del contorno y puerta con bisagra final/lado derecho. PlanSpec 50/50, automatización 31/31 y `git diff --check` pasaron. Es **partial L0**: solo líneas 2D, sector ideal sin espesor de hoja ni altura, categorías `contour`/`unclassified`, sin muebles/obstáculos tipados, múltiples puertas ejecutables ni L2/L4 de escenarios colisionados. No se abrió el DWG privado ni se alteró evidencia histórica o `upstream`. Verificar SHA local/remoto tras publicar.

### Prompt de continuidad desde el sexagésimo cuarto corte

Verifica `AGENTS.md`, Git/remotos, SHA local/remoto y árbol limpio de `codex/mcp-door-sweep-qa`. Conserva v1/v2 de QA y los cortes L2/L4 anteriores. Para M6.3 versiona obstáculos con tipo, elevación y fuente antes de bloquear muebles o puertas; valida barrido/colisiones de más de una apertura con oráculo. Para M6.5, mapea CAD→píxel y obtiene revisión humana; M4/M5 requieren ejes/oblicuas, joins y perfil de impresión; M3, telemetría real Luna/supervisor. Al recibir las seis imágenes inéditas autorizadas para M7, congela hashes, cohorte 2/2/2 y oráculo antes de L3, ejecuta tres repeticiones/caso y L5 humana. M8 solo tras gates completos y merge/push al fork. No abras/modifiques el DWG privado ni publiques privados/upstream.

## Sexagésimo quinto corte obstáculos tipados y altura de hoja — 2026-09-24, en progreso

El corte 64 `codex/mcp-door-sweep-qa` quedó publicado solo en el fork con SHA local/remoto `cf2f7e72a4da7590f6e35803941d4808bf53e879` y árbol limpio. Desde `codex/mcp-typed-obstacles-v8`, PlanSpec v8 agrega `obstacles` con ID, tipo (`fixed_partition`, `furniture`, `annotation`), rectángulo XY local al origen, base/altura z y procedencia. Las puertas v8 exigen `leaf_height_m` 1.5–4 m. El validador rechaza claves faltantes, IDs duplicados, huellas sin área, base negativa, altura no positiva y tipo desconocido. Los obstáculos son contexto QA y no generan comandos CAD. Contrato y límites: `OBSTACLE-QA-V8.md`.

`door_clearance_qa` v3 coteja la huella rectangular con el sector barrido y exige solape vertical con la hoja. Un objeto físico medido produce `blocking`; fuente inferida/desconocida, `review_blocked`; ambos impiden `executable`. Una anotación es `nonphysical` y no bloquea; un objeto cuya base está a la altura de la hoja o superior no cruza el volumen. La confianza se conserva como evidencia, sin umbral improvisado. Fixture `synthetic-door-obstacle-v8.planspec.json` SHA-256 `A891B4FE992D9010B8DD76EF3FE06DC77B45F12F3DAEEAA572EBBE27B235BA8A`, esquema v8 SHA-256 `9F33BC7C4F1BE46E645EEEEF5BEDB292D6D09AE27D98219438BF9EC94F70027F`. L0 `target/m6-typed-obstacles/20260924-040025-f9a47cf3/report.json` SHA-256 `34093BCC784D84DFC6854440834EC01951980C3641802A5EEC06C66D70F537D1` bloqueó mobiliario dentro del barrido; el hash de comandos `fb5b575c2c627b1ef8056d89d4478aaa72f49f604925bc3e2feace619cf437e7` queda independiente de la huella QA.

Pruebas sintéticas cubren mobiliario medido, inferido, anotación, objeto sobre hoja, rectángulo que envuelve el sector y datos inválidos; PlanSpec 51/51, automatización 31/31, ambos JSON parseables y `git diff --check` pasaron. Esto es **partial L0**: huellas rectangulares y altura simplificada, sin mallas/curvas, espesor de hoja, varias puertas compiladas, CAD L2/L4 ni revisión visual. No se abrió el DWG privado ni se alteraron ensayos históricos o `upstream`. Verificar SHA local/remoto tras publicar.

### Prompt de continuidad desde el sexagésimo quinto corte

Verifica `AGENTS.md`, Git/remotos, SHA local/remoto y árbol limpio de `codex/mcp-typed-obstacles-v8`. Conserva PlanSpec v4–v7, fixture histórico de puerta y reportes L2/L4; v8 solo añade QA de obstáculos, sin dibujarlos. Valida v8 con otro motor de esquema si se incorpora, extiende M6.3 a varias puertas y huellas/alturas complejas con oráculo independiente; M6.5 necesita mapeo CAD→píxel y L5. M4/M5 requieren ejes/oblicuas, joins y perfil de impresión; M3, telemetría real Luna/supervisor. Al adjuntarse las seis imágenes inéditas autorizadas de M7, congela hashes, cohorte 2/2/2 y oráculo antes de L3, tres repeticiones/caso y revisión humana. M8 solo tras gates completos y merge/push en el fork. No abras/modifiques el DWG privado ni publiques privados/upstream.

## Sexagésimo sexto corte dos puertas en un muro v8 — 2026-09-24, en progreso

El corte 65 `codex/mcp-typed-obstacles-v8` quedó publicado únicamente en el fork con SHA local/remoto `65db60b8ceced4653683f5be0593995879b81cac` y árbol limpio. En `codex/mcp-two-door-wall-v8`, un muro recto v8 con dos puertas explícitas separadas compila 12 LINE de spans/caps/jambas y dos símbolos LINE+ARC, además de las cuatro LINE de contorno del fixture: 20 entidades con IDs→handles. Los intervalos se ordenan por offset/ID, el hash de comandos no depende del orden de entrada y el validador rechaza solape. QA de barrido se ejecuta por puerta: un obstáculo sintético colocado solo en la segunda bloqueó `executable` con ID `door-east`. Fixture `synthetic-two-door-wall-v8.planspec.json` SHA-256 `7E4B149F7AFC1465960C07FFD4A466DEC3A84FBB03E7CE087138E91A1D0CC157`.

El primer L2 `target/mcp-isolated/20260924-040603-f3762be4/report.json` pasó, pero AutoCAD L4 `target/mcp-external/20260924-040631-autocad-c89a2d05/report.json` rechazó 19/20: el arco `door-east__swing_arc` quedó con centro/radio y ángulos fuera de 1e-6 por el punto intermedio ARC redondeado a seis decimales. Se conservan ambos. V8 ahora emite ese punto a nueve decimales; v4 conserva seis y su comando histórico. El L2 nuevo `target/mcp-isolated/20260924-040735-b7eca5aa/report.json`, SHA-256 `2B84AF8C64B43D05952A0D0849FC1335F8899B20EABD1D4B4497EF41147C888D`, pasó con GUI salida, 20 handles, manifiesto interno 18 LINE+2 ARC y DWG SHA-256 `616D300DA3B51FE563C7294E9F772025D1DFC30E954444000261B8CD8425810E`; hash de comandos `16e385887a846f5d384bf1708008e5216b1cadca9f0e8acc8e408bc7808820f7`.

Un segundo L4 sobre el DWG nuevo `target/mcp-external/20260924-040753-autocad-3685f743/report.json` rechazó 19/20 pese a centro/radio correctos: DXF50 del segundo arco era aproximadamente -2.17e-10 rad, equivalente a 0, y el comparador no envolvía correctamente 2π. Se preserva. Se corrigió el comparador con distancia angular periódica y la misma tolerancia 1e-6. L4 final `target/mcp-external/20260924-040824-autocad-5dea6ae9/report.json`, SHA-256 `1D3D552F953398D0CCAEBA04208C09093E746347C6DC2364697E31632156467F`, pasó AUDIT/censo y geometría 20/20, unidad6, hash DWG intacto. Regresión de una puerta v4 `target/mcp-external/20260924-040848-autocad-ea065368/report.json` pasó 14/14. El DWG anterior de seis decimales continúa rechazado con el comparador corregido (`target/mcp-external/20260924-040906-autocad-6cae09ce/report.json`), confirmando que el defecto de precisión no fue ocultado. Copia sintética con handle del segundo arco sustituido por el primero fue rechazada `semantic_mismatch` en `target/mcp-external/20260924-041010-autocad-79bb8e90/report.json`.

PlanSpec 52/52, automatización 31/31, JSON parseable y `git diff --check` pasaron. M4/M6.3 quedan **partial scoped L0/L2/L4** para dos puertas separadas del mismo muro; no hay puertas en muros unidos, interacción de barridos entre muros, CAD de obstáculo, evaluación de colisión 3D real ni L5. No se abrió el DWG privado ni se modificaron reportes/DWG históricos o `upstream`. Verificar SHA local/remoto tras publicar.

### Prompt de continuidad desde el sexagésimo sexto corte

Verifica `AGENTS.md`, Git/remotos, SHA local/remoto y árbol limpio de `codex/mcp-two-door-wall-v8`. Preserva los dos L4 rechazados, el L4 final 20/20, la regresión 14/14 y el negativo de handle. Continúa M4/M6.3 con múltiples muros/puertas y obstáculos tipados con oráculo, sin extrapolar el caso de un muro. M6.5 requiere mapeo CAD→píxel y L5; M4/M5 aún necesitan ejes/oblicuas y perfil de impresión, M3 telemetría real Luna/supervisor. Al adjuntarse las seis imágenes inéditas autorizadas de M7, congela hashes, cohorte 2/2/2 y oráculo antes de L3, repite tres veces/caso y pide revisión humana. M8 solo tras gates completos y merge/push al fork. No abras/modifiques el DWG privado ni publiques privados/upstream.

## Sexagésimo séptimo corte proyección CAD→píxel fenced — 2026-09-24, en progreso

El corte 66 `codex/mcp-two-door-wall-v8` quedó publicado solo en el fork con SHA local/remoto `8224e090815bb659739adffaa7ab9a8513af9c99` y árbol limpio. Desde `codex/mcp-fenced-cad-pixel-map`, `ocs_capture` valida hasta 32 anclas CAD xyz con IDs únicos y proyecta cada una usando la cámara del viewport y el mismo recorte/redimensionamiento del PNG tras la barrera de render. Devuelve `viewport-rte-pixels-1`, píxeles y marca `inside`; el cliente rechaza IDs o coordenadas faltantes, revisión distinta, scope incorrecto, overlays o ausencia de barrera antes de guardar el PNG. La cuota de una captura permite pasar anclas sin repetir una captura incierta.

El L2 sintético de dos puertas `target/mcp-isolated/20260924-042327-357da0b0/report.json`, SHA-256 `9FDAB9EEA0664B9F1D00ABDA4DD8C5B2EF49F72DF13FE8B68E3DBA94F28A5576`, pasó con binario SHA-256 `D7840C59EE19861BB58C5B33DCB9C762EE8BE1569C4E081F81031FF572BB4E0A`. Las ocho anclas CAD de bisagra, hoja cerrada, medio de arco y hoja abierta de ambas puertas quedaron dentro de 1024×506 px y cada una encontró trazo visible a radio 3 px (picos RGB 132–176 frente a umbral 80). Documento2, geometría30 y cámara1 coincidieron con el PNG y frame renderizado; GUI salió. El primer L2 de esta rama `target/mcp-isolated/20260924-042207-e63c03af/report.json` pasó la proyección previa al oráculo de píxel y se conserva. `projected_door_regions` derivó dos recortes del reporte final; `target/mcp-isolated/20260924-042327-357da0b0/projected-doors/manifest.json` SHA-256 `3BB3F8F06BECDC889624E76D6923A3C3396DA7052554188043F44AE6C4B1D58C` enlaza hash del padre, ArtifactRefs e identidad; el verificador cotejó los píxeles exactos de ambos recortes. El primero fue inspeccionado visualmente. PlanSpec/evidencia 53/53, automatización 32/32, Rust lib 1681 passed/24 ignored/0 failed, build Rust y `git diff --check` pasaron; publicación del corte pendiente al cierre de esta entrada.

Alcance **partial scoped L1/L2** para un fixture de dos puertas y ocho anclas: el oráculo RGB distingue trazo de fondo solo en esta vista; no comprueba textos, impresión, calibración imagen fuente→CAD, cohortes reservadas ni revisión humana L5. M4/M5 todavía requieren cotas de eje/oblicuas, joins y perfil de impresión; M3 medición real Luna/supervisor. Las seis imágenes inéditas autorizadas para M7 y el motor CAD independiente están confirmados por el usuario, pero las imágenes aún no se adjuntan; M7 L3/L5 y M8 quedan abiertos. No se abrió/modificó el DWG privado, no se alteraron fallos históricos ni se publicó a `upstream`.

### Prompt de continuidad desde el sexagésimo séptimo corte

Verifica `AGENTS.md`, Git/remotos, SHA local/remoto, árbol y suite Rust de `codex/mcp-fenced-cad-pixel-map`. Preserva L2 de proyección inicial y final, PNG, los dos recortes y manifiesto. Generaliza M6.5 a entidades/planos distintos con oráculos de legibilidad y revisión L5; extiende M6.3 a múltiples muros y topologías sin extrapolar este caso. Completa M4/M5 cotas de eje/oblicuas, joins y perfil de impresión con L2/L4; M3 requiere telemetría real Luna/supervisor. Cuando el usuario adjunte las seis imágenes inéditas, congela hash/cohorte 2/2/2/oráculo antes de L3 y ejecuta tres repeticiones por caso, AutoCAD externo y L5 humana. M8 solo tras gates completos, merge/push únicamente al fork. No abras/modifiques DWG privado ni publiques privados/upstream.

## Sexagésimo octavo corte cota de tramo interior de eje v8 — 2026-09-24, en progreso

El corte 67 `codex/mcp-fenced-cad-pixel-map` se publicó solo en el fork con SHA local/remoto `af44b560ebddec2aba644bc5c8862b10773170db` y árbol limpio. Desde `codex/mcp-axis-length-dimension-v8`, PlanSpec v8 compila una cota `DIMLINEAR` de tramo interior de eje para un muro recto horizontal o vertical positivo, sin hueco/unión ni otras entidades fuente. Los nodos y bindings deben corresponder a dos estaciones estrictamente interiores, crecientes y de tipo `axis`; el fixture usa 0.5→3.5 m y valor 3.00 m. Offset positivo sitúa la cota a la izquierda del eje: +y o -x; si queda dentro de medio espesor, `quality_blockers` impide ejecutar. Se incluyen fuente/bounds de la línea de cota sin texto/flechas. El contrato y límites están en `AXIS-SPAN-DIMENSION-V8.md`; no se modificó v7.

El primer L2 `target/mcp-isolated/20260924-043528-ddbabd1b/report.json` SHA-256 `B7414DF401E5377C1A7162A52A4037BFFB39B1578847A1916BCA3EED93AD5719` quedó **partial**: después de reabrir el DWG verificado, el arnés intentó guardar esa segunda pestaña sobre `synthetic-session.dwg`, bloqueado por la pestaña original. La GUI aislada PID15332 se detuvo tras comprobar ejecutable y argumento `--new-instance`; se preservan DWG/reporte. El arnés ahora guarda la vista reabierta en un path distinto. L2 horizontal final `target/mcp-isolated/20260924-043604-9c4ab394/report.json` SHA-256 `C70219E0BCF1553BDF4AB323D31C2A4A23828D4FDA9687A80B24DB4C39515864`, y vertical `target/mcp-isolated/20260924-043621-45c0d700/report.json` SHA-256 `F7BCD0C686935E97DBA78169BB8CA3DA5A2686FBD4D33A0D5B2B3F36CD62E3AE` pasaron: 5 entidades, DIMENSION 3.00 m antes/después de reapertura, save_verified, captura viewport, GUI salida. DWG SHA-256 horizontal `AE24F05B08429873A0B08BBBA1027A18A2F8713400B86AA355ECA6E4374D2810` y vertical `33E959BDD9BCB4BAAD283EECB5ED23CCF259097B1D988F06DE2A1ACF5DDA66A0`.

El primer intento AutoCAD `target/mcp-external/20260924-043800-autocad-5ecd5b67` dejó censo/log pero no reporte porque una expresión PowerShell del nuevo comparador sumaba sobre `Object[]`; se preserva. Corregida la expresión, la sonda se endureció para cotejar el handle de reporte con el mapa PlanSpec, hash del fixture, valor y estilo de origen. Los L4 finales horizontal `target/mcp-external/20260924-044015-autocad-c94dd0d2/report.json` SHA-256 `BCAD3DA03B3EF694A3C7959455E49525522C70BBE9D85347F4D371B55376C6F5` y vertical `target/mcp-external/20260924-044024-autocad-2fc9fa06/report.json` SHA-256 `8831825B306C36F96A1401A6FDB2E01DE3838976A0398570F6F3A1B38011CE97` pasaron `audit_and_census_passed`/`matched_scoped`: 1/1 DXF42, DXF13/14/posición/rotación, seis propiedades de estilo, cuatro bordes, Model5, INSUNITS6, AUDIT0/0, salida0 y SHA de entrada intacto. Copia sintética con handle de cota reemplazado por borde fue rechazada `semantic_mismatch` en `target/mcp-external/20260924-044033-autocad-5af3b57c/report.json` SHA-256 `D0E7947D575A5A83F935414DC09B55B1CEDF1CE35A8F95A717DF52A5A5C18DD0`; cuatro bordes seguían correctos. Una regresión L4 del fixture v7 de cara vertical también pasó. PlanSpec/evidencia 54/54, automatización 32/32 y `git diff --check` pasaron; publicación se verificará antes del cierre.

Estado **partial scoped L0/L2/L4**: el binding de eje es un contrato PlanSpec; no se comprobó asociación nativa de la cota con bordes ni edición posterior. Faltan cotas de longitud total a extremos, muro oblicuo/invertido, varias cotas, impresión/escala y L5. M3 requiere telemetría real Luna/supervisor, M6 QA general y M7 las seis imágenes inéditas autorizadas aún no adjuntas. No se abrió/modificó DWG privado, no se reescribió evidencia histórica ni se envió nada a `upstream`.

### Prompt de continuidad desde el sexagésimo octavo corte

Verifica `AGENTS.md`, Git/remotos, SHA local/remoto y árbol de `codex/mcp-axis-length-dimension-v8`; preserva el L2 partial con bloqueo de archivo, el L4 sin reporte por excepción, dos L2/L4 finales, negativo de handle y regresión v7. Continúa M4/M5 con referencia a extremos mediante contrato versionado, cota oblicua y edición asociativa real, más perfil de impresión/escala; no interpretes la cota de tramo interior como longitud total de muro. Amplía M6 por topologías y L5, y M3 con telemetría real. Cuando lleguen las seis imágenes inéditas, congela hashes, cohorte 2/2/2 y oráculo antes de L3; tres repeticiones por caso, comparación externa y revisión humana. M8 tras gates completos, publicación/merge solo al fork. DWG privado y ensayos históricos intactos; nada a upstream.

## Sexagésimo noveno corte extremos de eje y cota alineada v9 — 2026-09-24, en progreso

El corte 68 `codex/mcp-axis-length-dimension-v8` quedó publicado solo en el fork con SHA local/remoto `4f097855f8e478772c197119c341d2103cd8d4e9` y árbol limpio. Desde `codex/mcp-axis-endpoint-aligned-v9`, PlanSpec v9 hereda v8 y permite `station_m=0/length` exclusivamente en bindings `axis`, conservando estaciones interiores para caras y v8. Un solo muro recto sin hueco/unión ni otras entidades compila DIMLINEAR para eje horizontal/vertical positivo y DIMALIGNED para muro oblicuo; exige estaciones crecientes, nodos enlazados y línea a más de medio espesor hacia la izquierda del eje. El fixture horizontal completo `synthetic-wall-axis-endpoints-v9.planspec.json` SHA-256 `70CA486BAC277D2A394018D9318AB2D1B78376BD7052A8C3C23EB116A2F9B54A` emite `DIMLINEAR 0,0 4,0 2,0.5`; el oblicuo 3–4–5 `synthetic-wall-aligned-endpoints-v9.planspec.json` SHA-256 `AEC89F338604928A88ABD07C5C3B87CA98B3F1825021C0E3C1F649DF04C8E2DF` emite `DIMALIGNED 0,0 3,4 1.1,2.3`. Un muro oblicuo invertido compiló en L0 con normal izquierda opuesta. El grafo marca `direct_bound_aligned` solo para el caso único v9; no declara solver de cadenas alineadas. Contrato: `AXIS-ENDPOINT-ALIGNED-V9.md`.

L2 horizontal `target/mcp-isolated/20260924-044713-d3a44581/report.json` SHA-256 `27A901FF1DD7BFA47A1546DD66EFBF779AB55D751E3FBD653FF5EC5695C7AAAD` y oblicuo `target/mcp-isolated/20260924-044728-0bd34340/report.json` SHA-256 `97BA6D3B6B484F1BD7F95A4B35C214B262B209A43CE6E45C0C861925AC76BBB2` pasaron con cinco entidades, medida 4.00/5.00 m antes/después de reapertura, captura fenced y GUI salida. DWG SHA-256 horizontal `BBFEC981527337BCB1269D7884B8FAFF1E61152901F025F8BEA06484418DF3CD`, oblicuo `824590924731F7EE3A57ABFCF42E8F4184431791B5A26CC3CED255F7FF056CE3`. La captura oblicua se inspeccionó visualmente; el texto es pequeño y no acredita L5.

AutoCAD L4 final horizontal `target/mcp-external/20260924-045053-autocad-5ad24c89/report.json` SHA-256 `C60EED8DB97757008A23F407738D7453275FDA5DE467668E4528CB69685FE0A8`, oblicuo `target/mcp-external/20260924-045045-autocad-243d137d/report.json` SHA-256 `35ED0863FE1A3470D7FC9A72EDA496210CEA28F1964F694CACDB4CAFA3B269F3`: ambos `audit_and_census_passed`/`matched_scoped`, Model5, 1/1 DXF42, DXF13/14, desplazamiento de línea, seis propiedades de estilo, cuatro bordes, INSUNITS6, AUDIT0/0, salida0 y SHA intacto. Para DIMALIGNED, DXF50 observado fue 0: la sonda registra `orientation_basis=dxf_13_14` y verifica el sentido por los puntos más el desplazamiento normal, sin interpretar DXF50 como ángulo. Un handle adulterado en copia produjo `semantic_mismatch` en `target/mcp-external/20260924-045213-autocad-c37f6304/report.json` SHA-256 `F69CA930D467AB17D615266E921533F54DB7DB382CF285A02B5510E1350B0414` con DWG y bordes intactos.

La primera regresión L4 de v8 `target/mcp-external/20260924-045006-autocad-1dba8166/report.json` SHA-256 `0B51959A25355B5CF8EED25EE8776A2BC3CB0FEFF5A1CDE6089148F57432C15A` fue un falso rechazo: el reporte histórico no tenía `axis_dimension.kind`, exigido por la nueva sonda. Quedó preservada. La sonda exige `kind` para v9 y permite su ausencia histórica v8; la regresión final `target/mcp-external/20260924-045034-autocad-d0510f1c/report.json` SHA-256 `3C6F4CF7B330C278CAE474B32633EB8D3FFF002DF355455995476A05E45C3427` pasó. PlanSpec/evidencia 55/55, automatización 32/32 y `git diff --check` pasaron. La prueba independiente de JSON Schema no se ejecutó: `jsonschema` no está disponible en los dos runtimes Python consultados. La publicación del corte se verificará después del commit.

Estado **partial scoped L0/L2/L4**. Los vínculos de eje son de PlanSpec; no se probó asociación CAD nativa de la cota con bordes ni reacción a edición, y quedan varios muros/cotas, uniones, impresión, escala y L5. M3 necesita telemetría real Luna/supervisor; M7 requiere las seis imágenes inéditas autorizadas todavía no adjuntas, cohorte congelada, tres repeticiones y revisión humana. No se abrió/modificó el DWG privado, no se alteraron resultados históricos ni se publicó a upstream.

### Prompt de continuidad desde el sexagésimo noveno corte

Verifica `AGENTS.md`, Git/remotos, SHA local/remoto y árbol de `codex/mcp-axis-endpoint-aligned-v9`. Conserva los dos L2, L4 iniciales/finales, negativo v9 y falso rechazo de regresión v8. Sigue M4/M5 con asociación nativa de cota de eje a geometría editable, varias cotas/muros y perfil de impresión/escala, cada gate con L2/L4 y negativos; no infieras asociación desde binding PlanSpec. Amplía M6 QA espacial/visual y M3 telemetría real. Cuando el usuario adjunte las seis imágenes inéditas, congela hashes y cohorte 2/2/2 con oráculo previo, luego tres repeticiones/caso, interoperabilidad externa y L5 humana. M8 solo tras gates completos, con merge/push al fork. DWG privado, ensayos históricos y upstream intactos.

## Septuagésimo corte edición asociativa de longitud de muro — 2026-09-24

El corte 69 se publicó solo en el fork con SHA local/remoto `346aac532172d17a5581dd2f8b018b6aeb9858e6`. Se verificaron AGENTS, Git/remotos y evidencia antes de editar. El censo AutoCAD de ambos fixtures v9 reveló DIMREF nativo a los remates (osnap MID). En `codex/mcp-associative-axis-length-edit`, `edit_wall_length` mueve un remate de una pared rectangular aislada de cuatro LINE y una DIMENSION `Linear`/`Aligned` asociada. Guarda documento/revisión, handles, contexto de espacio modelo, longitud, espesor, geometría, medida y referencias; comprueba la medida nueva, revierte fallos y deja un deshacer. No entra en `batch`. Contrato: `ASSOCIATIVE-AXIS-LENGTH-EDIT.md`.

L2 finales: horizontal `target/mcp-isolated/20260924-051621-wall-length-20e706e9/report.json`, SHA-256 `C24659CAAF5E595E1EA6663C7CC98501455801EFD22093383371A52DB97088A9`, y oblicuo `target/mcp-isolated/20260924-051639-wall-length-3baccfa8/report.json`, SHA-256 `6275A17E57944BCC8C1A25E2FE4A54CBB03F42732846D4B6A7D60D323497341F`. Mismo binario SHA-256 `C474034809E7A84D2786E75C77C6A3FED42F8FCE2FACA680E73980CBC5719F34`. Pasaron 4→4.5 y 5→5.5 m, espesor 0.2 m, contorno, cota nativa, rechazo de estado obsoleto, reconciliación por `request_id`, reproducción sin mutación, reapertura y salida GUI. DWG antes/después: horizontal `BBFEC981527337BCB1269D7884B8FAFF1E61152901F025F8BEA06484418DF3CD` / `457AC903E9527EB8694F5378C4675DD8DAC73228C95AEF09134982B275E3D579`; oblicuo `824590924731F7EE3A57ABFCF42E8F4184431791B5A26CC3CED255F7FF056CE3` / `FAC9317AA19473D0003767367C0214D89C68C4BD737FB21C9280147770AA9D4A`.

Cuatro L4 pasaron `audit_and_census_passed`/`matched_scoped`: horizontal antes/después `target/mcp-external/20260924-051717-autocad-e0aa4cb5` y `20260924-051720-autocad-6add34ca`; oblicuo antes/después `20260924-051657-autocad-6587a07e` y `20260924-051700-autocad-bf45aa8e`. AutoCAD confirmó Model5, INSUNITS6, AUDIT0/0, DXF42, cuatro bordes por handle/coordenada, dos DIMREF MID y SHA de entrada intacto. Copia sintética con referencia de remate adulterada fue rechazada `semantic_mismatch` en `target/mcp-external/20260924-051732-autocad-e60ad50e/report.json`, SHA-256 `57B1C90EA04896B9D1376FC981977D59F21F7BEAC4060F7697434B0B1BC3CC06`.

Ensayos preservados: `20260924-050603-wall-length-3914ad85` falló por manejo del error obsoleto en el arnés; `20260924-050637-wall-length-d10ecd74` agotó cierre GUI por pestañas; `20260924-050712-wall-length-53cba004` reveló helper solo `Linear`; `20260924-051435-wall-length-e91710ab` reveló que el censo inicial incluía entidades internas del bloque de cota tras reapertura. Se acotó al propietario de espacio modelo. Una selección de L4 incluyó un directorio negativo sin `before-edit.dwg`; falló antes de abrir AutoCAD y se repitieron los dos L4 horizontales correctos. Fallos y originales intactos.

PlanSpec 55/55, automatización 32/32 y Rust lib 1684 passed/24 ignored/0 failed pasaron; `cargo build --bin OpenCADStudio` y `git diff --check` también. El commit de código `3ea3f92202df0025c726f68a5e3fa63c5091b3ba` quedó publicado en la rama `origin/codex/mcp-associative-axis-length-edit`; `git ls-remote` coincidió y el árbol quedó limpio antes de esta actualización documental. Alcance M4/M5 **partial scoped L0/L2/L4** para muro aislado; faltan múltiples muros/cotas, joins, huecos, impresión/escala y L5. M3 requiere telemetría real Luna/supervisor. El usuario confirmó motor CAD y seis imágenes inéditas autorizadas, aún sin adjuntar; M7 espera cohorte congelada y M8 sigue abierto. DWG privado y upstream intactos.

### Prompt de continuidad desde el septuagésimo corte

Verifica AGENTS, Git/remotos, SHA local/remoto y árbol de `codex/mcp-associative-axis-length-edit`; conserva todos los L2/L4 y fallos exploratorios. Completa M4/M5 con perfil de impresión/escala y múltiples muros/cotas bajo contratos versionados, negativos, L2 y L4. No amplíes `edit_wall_length` a topologías no soportadas sin dependencias explícitas. Continúa M3 telemetría real Luna/supervisor y M6 QA visual/espacial. Al adjuntarse las seis imágenes inéditas, congela hashes, cohorte 2/2/2 y oráculo antes de L3; ejecuta tres repeticiones/caso y solicita revisión humana L5. M8 tras gates completos y merge/push solo al fork. No abras/modifiques DWG privado ni publiques privados/upstream.

## Septuagésimo primer corte perfil PDF métrico A4 — 2026-09-24, en progreso

El corte 70 `codex/mcp-associative-axis-length-edit` quedó publicado solo en el fork con SHA local/remoto `82327fdb34c0f72147b902c3528ef69205f642bb` y árbol limpio. Se verificaron AGENTS, remotos y Git antes de abrir `codex/mcp-metric-plot-profile`. La inspección de la ruta de impresión halló que el modelo en metros necesita convertir 1:100 a 10 mm de papel por unidad CAD; el PDF directo desde Model no usa por sí mismo el page setup DWG. Por eso `metric_plot_pdf` es un perfil de exportación tipado y separado de la futura persistencia de page setup. Contrato: `METRIC-PLOT-PDF-V1.md`.

El backend exige Model, `INSUNITS=6`, destino absoluto `.pdf` inexistente y denominador entero 10–1000; comprueba que las extensiones quepan en A4 horizontal 297×210 mm con al menos 10 mm libres, fija escala `1000/N` mm por unidad y centra sin fit automático. El ledger MCP recupera la operación por `request_id` y no reescribe al reproducirla. El primer L2 `target/mcp-isolated/20260924-053249-metric-plot-941570f5/report.json` quedó fallido: Poppler mostró solo la LINE horizontal porque el tramo vertical x=4 estaba exactamente sobre el recorte máximo. Un segundo L2 `20260924-053349-metric-plot-f5570d66` confirmó por lectura MCP que ambas LINE existían y conservó la falla de render. Se añadió 0.5 mm de holgura de ventana a cada lado, sin cambiar escala ni margen de aceptación.

L2 final `target/mcp-isolated/20260924-053620-metric-plot-3b054680/report.json` SHA-256 `739FE37C3AC1279579E0F86FB25CDBFB3237C1CAD494D26380A20EB7D7B9856C` pasó con binario SHA-256 `D33321C70710B15524D5B15349A8CE1D702898B2C222608AF01E17C71D1E4BF0`. PDF SHA-256 `DA9F975FB64D11EEA10794D02677AEFF4D1EC49CEBF598D615FECEEDBF1A8015`, una página MediaBox 297.04×209.90 mm, salida GUI y PNG inspeccionado. La L sintética 4×1 m midió 157×39 px a 100 dpi, conforme a 40×10 mm dentro de ±3 px. Hash MCP/local, lectura de operación, replay sin reescritura y rechazo de nuevo ID sobre destino existente pasaron. Rust lib 1686 passed/24 ignored/0 failed, Python PlanSpec 55/55 y automatización 32/32, `cargo build --bin OpenCADStudio` y compilación Python pasaron. El commit de código `a31488b7d81c58276c24769c6df58c8479e84539` quedó publicado en `origin/codex/mcp-metric-plot-profile`: `git ls-remote` coincidió y el árbol estaba limpio antes de esta actualización documental.

Estado M5.7 **partial scoped L0/L2**: esta exportación PDF no acredita page setup guardado en DWG, CTB/plumas, grosores por capa, cotas/textos legibles al imprimir, otros papeles/escalas ni L5 humana. M3, M4/M5 de múltiples muros, M6 restante, M7 de las seis imágenes todavía no adjuntas y M8 siguen abiertos. El DWG privado, los ensayos históricos y `upstream` no se tocaron.

### Prompt de continuidad desde el septuagésimo primer corte

Verifica AGENTS, remotos, SHA local/remoto y árbol de `codex/mcp-metric-plot-profile`; conserva los dos L2 de recorte fallidos y el L2 final con PDF/PNG. Continúa M5.7 con page setup métrico persistido en DWG y L4 AutoCAD de sus campos, más cotejo de grosor/texto/cota en PDF a varias escalas; no extrapoles el fixture de dos LINE. Sigue M4/M6 para múltiples muros y topologías, y M3 con telemetría real. Cuando lleguen las seis imágenes inéditas, congela cohorte/hash/oráculo antes de L3, ejecuta tres repeticiones y revisión L5; cierra M8 solo tras todos los gates. Publica únicamente al fork y preserva DWG privado y ensayos originales.

## Septuagésimo segundo corte perfil métrico persistido en DWG — 2026-09-24

El corte 71 `codex/mcp-metric-plot-profile` quedó publicado solo en el fork con SHA local/remoto `4d4e8b2d1710ac52bbdea4c1924c7b8f1b422a9e` y árbol limpio. Se comprobaron AGENTS, Git/remotos y el checkpoint antes de crear `codex/mcp-metric-page-setup-dwg`. Contrato: `METRIC-PAGE-SETUP-DWG-V1.md`. `set_metric_page_setup` guarda A4 horizontal, unidades de papel mm, Model/INSUNITS6, escala 1:N (10–1000), Extents, centrado y grosores en el Layout Model con guardas de documento/revisión y un deshacer; `metric_page_setup` lee la metadata. `metric_plot_pdf` admite `require_page_setup=true` y detiene la exportación si el perfil persistido no coincide. La operación no entra en batch.

L2 final `target/mcp-isolated/20260924-055936-metric-plot-0a106ef7/report.json` SHA-256 `E599FD86571016847C7BD2DFE988042C76C59D0A12523C390A127BE6CD21A57C` pasó con binario SHA-256 `3F6B0A541C58E9A854C9EDA661C25BFD4129F7FE506C58ABB28C9BCE990F9FF2`. Tras guardar y reabrir DWG, la lectura MCP coincidió campo a campo con el perfil previo: A4 210×297 mm, numerador10, denominador1, factor254, rotación90, Extents y márgenes0. El DWG sintético SHA-256 `BA07C6347A33C11DEE7F88560E286C92BDB31B4327258AE1836BACA6F1E02000` contiene dos LINE. PDF SHA-256 `DA9F975FB64D11EEA10794D02677AEFF4D1EC49CEBF598D615FECEEDBF1A8015`; la huella a 100 dpi fue 157×39 px para 4×1 m a 1:100. El L2 también rechazó 1:50 contra el perfil, reconcilió/reprodujo `request_id` sin reescribir, rechazó destino duplicado y comprobó salida GUI.

L4 AutoCAD final `target/mcp-external/20260924-060020-autocad-78aebfd9/report.json` SHA-256 `E9058769331DDD29DAB5FDEAE2B9EF76711F3FD301485491295DB27D4C27A79A` pasó con 16/16 campos DXF del Layout Model, 2/2 LINE, INSUNITS6, Model2, AUDIT0/0, salida0 y DWG intacto. La sonda vinculó el hash del reporte L2. Copia sintética con solo `paper_size` del reporte adulterado: `target/mcp-external/20260924-060154-autocad-d6496429/report.json` SHA-256 `08C78BF407523218455DD87D9ED1032A5528FEA912C914595EDB9834D3C02D2E`, `semantic_mismatch` 15/16 y geometría 2/2; hash del DWG intacto. Regresión del DWG de longitud de muro: `target/mcp-external/20260924-060206-autocad-136ef19d/report.json` SHA-256 `8257DF79C8155226C4C58EA3734AD9842DE0C1E4E637B33060844223F3F3DB33`, `matched_scoped`, Model5 y AUDIT0/0. Los primeros L4 `055218`, `055409`, `055442` y el primer L2 `055136` se conservan: expusieron respectivamente reconocimiento de fuente, análisis del nombre de papel con paréntesis, campos vacíos DXF y validación anterior al endurecimiento final. No se reemplazaron.

Rust lib 1687 passed/24 ignored/0 failed, PlanSpec 55/55, automatización 32/32, compilación Python y `git diff --check` pasaron. Estado M5.7 **partial scoped L0/L2/L4**: metadata DWG y PDF simple verificados en este motor y fixture, sin prueba del diálogo de impresión, CTB/plumas, texto/cotas, otras escalas/medios ni L5. M3, topologías múltiples M4/M6, M7 con seis imágenes inéditas aún sin adjuntar y M8 siguen abiertos. El DWG privado, originales históricos y `upstream` permanecen intactos. Publicación/SHA final de esta rama: verificar tras commit y push.

### Prompt de continuidad desde el septuagésimo segundo corte

Verifica AGENTS, Git/remotos, SHA local/remoto y árbol de `codex/mcp-metric-page-setup-dwg`; preserva L2/L4 aprobados, negativos, regresión y fallos exploratorios. Sigue M5.7 con fixture sintético que incluya cota y texto, varios grosores, y comparación PDF/CAD independiente a dos escalas, con guardas y negativos. Continúa M4/M6 con varios muros, joins y QA visual/espacial sin extrapolar el caso aislado; M3 requiere telemetría real Luna/supervisor. Cuando se adjunten las seis imágenes inéditas autorizadas, congela hashes de cohorte y desarrollo y oráculo antes de L3; ejecuta tres repeticiones/caso y solicita L5 humana. M8 solo tras gates completos, merge/push al fork. No abras ni modifiques el DWG privado, ni publiques privados o `upstream`.

## Septuagésimo tercer corte contenido PDF a dos escalas — 2026-09-24

El corte 72 `codex/mcp-metric-page-setup-dwg` quedó publicado solo en el fork con SHA local/remoto `dc4370f21cad165003ad992c45445eed57923e00` y árbol limpio. Desde `codex/mcp-metric-content-print-qa` se añadió el arnés `mcp_metric_content_smoke.py` y la sonda AutoCAD L4-13. Contrato, comandos y límites: `METRIC-CONTENT-PRINT-QA-V1.md`. No se cambió el motor CAD en este corte.

L2 final `target/mcp-isolated/20260924-061533-metric-content-51d148bf/report.json` SHA-256 `0924AF3CBE6E16244EB8114FF24D4C9A1910F389BDC420BFF9811145D9DDA516` pasó: GUI aislada salió; DWG SHA-256 `128CDC288B5F29A081E01EBB2741EF632716E7034F9CF752812874D3F7129CFA` se reabrió con dos LINE, TEXT `TEST123` y DIMLINEAR 4 m. El estilo explícito `OCS_PRINT_TEST` fija altura de texto de cota 0.2 m, flecha 0.08 m, separación 0.03 m y escala/factor1. Se exportaron A4 1:100 (PDF SHA-256 `DBBA2531A2E74E6C19F325FBC8E18FFF3F73337D68148434D7D84C79D3FB429A`) y 1:50 (`8CB929F5D3754E5C9E61C26C82FA88BDDE7B2B81084CB9074D8A902A407EA9A9`) con page setup coincidente exigido. A 100 dpi el tramo de 4 m midió 158→316 px; siete glifos del texto midieron 9→18 px de alto y la cifra de cota 8→18 px. Los dos PNG se inspeccionaron. L2 acredita proporción física y presencia de glifos en esta fixture; `pypdf.extract_text()` devolvió vacío en ambos PDF.

L4 final `target/mcp-external/20260924-061549-autocad-dca7fd87/report.json` SHA-256 `73CE28C69621559F112EACD2D86E67A1FD5F05BA9CD65C49DEE0E955C501C04C` pasó `matched_scoped`: Model4, 2 LINE/1 TEXT/1 DIMENSION, handle/coordenadas, texto exacto/altura/inserción/estilo, DXF42=4, puntos de cota y cinco parámetros de estilo, INSUNITS6, AUDIT0/0, exit0, DWG intacto. El negativo de reporte con `TEST123` sustituido por `WRONG` fue `semantic_mismatch` en `target/mcp-external/20260924-061611-autocad-9af3cb48/report.json` SHA-256 `272C9379BD68B7A8FEA62A5E056127FE563B0600D713D211B11BDDEB1192E05A`; las otras tres entidades coincidieron. Regresiones L4-13 de page setup y edición de longitud en `20260924-061331-autocad-fc31bd3d` y `20260924-061334-autocad-30841d37` pasaron.

Exploraciones preservadas bajo `target/mcp-isolated`: `060658` falló 1:50 por extensión desproporcionada de la cota de estilo por defecto; `060722` produjo PDF pero tuvo cierre incierto y se conserva sin aceptación; `060818` y `060903` probaron estilos y raster antes del arnés final. El primer L4 de contenido `061126` rechazó porque el reporte exploratorio no usaba el contrato `verified_dwg`; no se reinterpretó como defecto CAD. PlanSpec 55/55, automatización 32/32, compilación Python y `git diff --check` pasaron; no cambió Rust y la suite Rust del corte 72 permanece como evidencia de esa base. M5.7 permanece **partial scoped L2/L4**: falta texto PDF buscable/accesible, CTB, grosores diferenciados, fuentes, cota/texto en plano complejo, varias máquinas, impresión real y L5 humana. El DWG privado, reportes originales y `upstream` no se tocaron. Publicación de esta rama: verificar tras commit y push.

### Prompt de continuidad desde el septuagésimo tercer corte

Verifica AGENTS, Git/remotos, SHA local/remoto y árbol de `codex/mcp-metric-content-print-qa`; preserva los intentos exploratorios, L2/L4 finales, negativo y regresiones. Completa M5.7 con grosores diferenciados, salida de texto seleccionable/buscable o estado explícito de capacidad, y oráculo visual humano para cotas/texto a escala; no extrapoles el fixture único. Continúa M4/M6 con múltiples muros/joins/colisiones y M3 con telemetría real Luna/supervisor. Cuando el usuario adjunte las seis imágenes inéditas autorizadas, congela hashes de desarrollo y cohorte 2/2/2 y oráculo antes de L3; ejecuta tres repeticiones/caso y solicita L5 humana. M8 solo tras todos los gates, merge/push al fork. No abras/modifiques DWG privado ni publiques privados o `upstream`.

## Septuagésimo cuarto corte grosores directos en dos escalas — 2026-09-24

El corte 73 `codex/mcp-metric-content-print-qa` quedó publicado solo en el fork con SHA local/remoto `0ac449927adc6994b0aea9854e8f8764aa566713` y árbol limpio. En `codex/mcp-metric-pen-width-qa`, la variante optativa `--pen-widths` del mismo arnés aplica `LineWeight::Value(13)` y `Value(70)` a las dos LINE del fixture antes de guardar. Contrato y límites: `METRIC-PEN-WIDTH-QA-V1.md`. La sonda AutoCAD L4-14 coteja DXF370 por handle; el motor CAD no cambió.

L2 `target/mcp-isolated/20260924-062326-metric-content-16eb0a1f/report.json` SHA-256 `BF4921DEDB7BC1E148D1239B0DB189FAC9C8FCEF894069BD854ECA67C5DFC0C5` pasó con GUI salida, cuatro entidades reabiertas y LINE de 0.13/0.70 mm conservadas. DWG SHA-256 `8ECF3B13FCC0380546215D2A9582AB2E54D28CE5D3F218D4105C9ED4392DD128`. PDF 1:100 SHA-256 `3E847FDB6A55645201037E31E9CF567EB53AE8AD47B28BD26BA70E4B8F699862`, 1:50 `043C4DB49014366956FA39B08B40CC8A8DE6D4E11ABF4EE5744602102023B9D1`: el trazo de 0.13 mm ocupó 1 px y el de 0.70 mm 3 px en ambos PNG a 100 dpi; geometría y texto conservaron la razón 2×. La cuantización impide deducir milímetros exactos solo del raster.

L4 AutoCAD `target/mcp-external/20260924-062410-autocad-589153b1/report.json` SHA-256 `8FE0B0A6EADB2CEBF4037EF2123B4380405009D0316594E0743AA5350304B7BC` pasó 6/6 comparaciones de contenido/pesos, Model4, INSUNITS6, AUDIT0/0 y DWG intacto. Copia de reporte con primer peso cambiado de 13 a 70: `target/mcp-external/20260924-062422-autocad-26ec80f2/report.json` SHA-256 `6B1057FC6558C9618F9158D34937CD1497846BE3F5B505F036536FFD6FB5C195`, `semantic_mismatch`. Regresiones L4-14 del contenido sin pesos y del page setup en `20260924-062436-autocad-d4243574` y `20260924-062439-autocad-f7b16003` pasaron. La primera exploración `20260924-062048-metric-content-567da628` falló cerrado: el enum Rust exige `{"Value":13}`, no entero 13. Se conserva.

PlanSpec 55/55, automatización 32/32, compilación Python y `git diff --check` pasaron; Rust no cambió. M5.7 sigue **partial scoped L2/L4**: este gate cubre dos grosores directos y no CTB/STB, ByLayer, color→pluma, todas las entidades, impresora, plano real o L5. M3, M4/M6 de topologías múltiples, M7 sin imágenes inéditas adjuntas y M8 siguen abiertos. DWG privado, ensayos previos y `upstream` intactos. Verificar SHA local/remoto después de publicar.

### Prompt de continuidad desde el septuagésimo cuarto corte

Verifica AGENTS, Git/remotos, SHA local/remoto y árbol de `codex/mcp-metric-pen-width-qa`. Conserva L2/L4, negativos, regresiones y fallo de enum; no reinterpretar 1/3 px como medida exacta. Sigue M5.7 con ByLayer y CTB de fixture sintético, más un oráculo independiente de PDF y texto buscable/accesible si se implementa; delimita impresión real y L5. Continúa M4/M6 con múltiples muros y uniones, M3 con telemetría real. Al recibir las seis imágenes inéditas autorizadas, congela hashes/cohorte 2/2/2 y oráculo antes de L3, ejecuta tres repeticiones y L5 humana. M8 solo tras gates completos con merge/push al fork. No abrir ni modificar el DWG privado, ni publicar privados o `upstream`.

## Septuagésimo quinto corte grosores ByLayer de dos capas — 2026-09-24

El corte 74 `codex/mcp-metric-pen-width-qa` quedó publicado solo en el fork con SHA local/remoto `83564df76ea8c7a69df22f7ebe8efed502114baa` y árbol limpio. Desde `codex/mcp-metric-bylayer-print-qa`, el arnés añade `--bylayer-weights` y la sonda L4-16 compara registros completos de capa. Contrato: `METRIC-BYLAYER-PRINT-QA-V1.md`. El motor CAD no cambió.

L2 final `target/mcp-isolated/20260924-062933-metric-content-7259a888/report.json` SHA-256 `52E89A47557FEDDE1B37A323EFDC12E7BB2536FD6FAC73F3CB4CB92B85446D27` pasó con GUI salida. A-THIN/A-THICK conservaron grosores 13/70 y sus LINE `ByLayer` tras reapertura. DWG SHA-256 `186B1FBC8A560CFDD0CF7167D122FC04BD0160663B841F14303AEB958A48D193`. Los PDF 1:100 y 1:50 tienen SHA-256 `3E847FDB6A55645201037E31E9CF567EB53AE8AD47B28BD26BA70E4B8F699862` y `043C4DB49014366956FA39B08B40CC8A8DE6D4E11ABF4EE5744602102023B9D1`, idénticos a los de grosores directos del corte 74. En ambos PNG a 100 dpi se midió trazo fino1 px y grueso3 px; texto/cota y escala geométrica conservaron el oráculo anterior.

L4 final `target/mcp-external/20260924-063257-autocad-41a427d2/report.json` SHA-256 `584A00613E2E58C6F571319931123C567C38CB8AACB97E065E4F60498BCC6D80` pasó 6/6 comparaciones, Model4, INSUNITS6, AUDIT0/0 y DWG intacto. AutoCAD `entget` del registro completo devolvió DXF370=13/70 para A-THIN/A-THICK; las LINE no tenían un DXF370 explícito y el L2 las leyó `ByLayer`. Copia con grosor reportado de A-THIN cambiado a 70 fue rechazada en `target/mcp-external/20260924-063311-autocad-e9bd3450/report.json` SHA-256 `206EEC76706AACE9ED98BF4AC25D3EC250928FE49A69318AADA1CA353A667B81`. Regresiones L4-16 de grosor directo, contenido sin pesos, page setup y longitud de muro pasaron en `20260924-063324`, `063327`, `063329` y `063331`.

Intentos exploratorios preservados: `20260924-062851` rechazó el peso al consultar `tblsearch`, que devolvió registro abreviado; `063021` verificó con `entget` que la capa sí tenía DXF370; `063058` y `063128` exploraron COM y no acreditaron peso de entidad; `063151` produjo censo incompleto por `vlax-ename->vla-object` nulo. La sonda final usa el registro de capa completo y falla cerrado ante discrepancias. PlanSpec 55/55, automatización 32/32, compilación Python y `git diff --check` pasaron; Rust no cambió. M5.7 permanece **partial scoped L2/L4**: falta CTB/STB, color→pluma, múltiples capas/entidades, impresora, texto buscable y L5. DWG privado, originales y `upstream` intactos. Verificar SHA local/remoto después de publicar.

### Prompt de continuidad desde el septuagésimo quinto corte

Verifica AGENTS, Git/remotos, SHA local/remoto y árbol de `codex/mcp-metric-bylayer-print-qa`. Conserva L2/L4, negativo, regresiones y falsos rechazos de lectura de capa; no atribuyas a AutoCAD una lectura COM que falló. Continúa M5.7 con CTB/color→pluma, transparencia, texto PDF buscable/accesible o estado de capacidad, y L5 humana; evita extrapolar la igualdad de dos PDF a todos los dibujos. Sigue M4/M6 con topologías múltiples y M3 con telemetría real. Al adjuntarse seis imágenes inéditas autorizadas, congela desarrollo/cohorte 2/2/2 y oráculo antes de L3, repite tres veces/caso y L5. M8 solo tras gates completos y publicación al fork. DWG privado, evidencia histórica y `upstream` intactos.

## Septuagésimo sexto corte CTB monocromático integrado — 2026-09-24

El corte 75 `codex/mcp-metric-bylayer-print-qa` quedó publicado solo en el fork con SHA local/remoto `67fe04d11aeaa19086106e25b269373b637873ed` y árbol limpio. Desde `codex/mcp-metric-monochrome-ctb`, `set_metric_page_setup` y `metric_plot_pdf` admiten únicamente `plot_style=none|monochrome.ctb`; la tabla monocromática procede del asset integrado y no de una ruta del usuario. El page setup guarda nombre y bandera de estilos, `metric_page_setup` los lee y `require_page_setup` exige coincidencia. Contrato: `METRIC-MONOCHROME-CTB-V1.md`. El perfil `none` mantiene el comportamiento de los cortes anteriores.

L2 final `target/mcp-isolated/20260924-064327-metric-ctb-238e1dc9/report.json` SHA-256 `192C702BC0ACE13AB7F48DF656443E50560FCC3FD1B5A3C9467CCE21FC5BF769` pasó con binario SHA-256 `0CA475A28486E846C8C7BBB37C973E0A4D61E3BB6BEA14A9C2C0D39A2152E414`, GUI salida y DWG sintético SHA-256 `38225E8C04EF97FD6F80F4647508A1F807E4E43A43566F45880D58192E4F18E1`. Tras reapertura, Model conservó `monochrome.ctb` y bandera de estilo; las LINE conservaron ACI1/3. PDF de control sin estilo SHA-256 `B26E3213E4F3C1270FCA2DD0CBDB96FB165E172F9A2C386522B610E833490EA6` mostró 158 px rojos y 40 verdes; el PDF con CTB SHA-256 `312EBD07724D5F2E6CEB9B3B9BCF62528C8872630A98748F45D874EEEADAEF97` volvió negros 197/198 de esos píxeles. Ambos PNG se inspeccionaron. Una ruta CTB arbitraria fue rechazada sin crear PDF.

L4 final `target/mcp-external/20260924-064345-autocad-4f1b0143/report.json` SHA-256 `B84242BCB81AD09F677EE29D833C802AAED0487D1C3CBDEE7B52435A85939503` pasó 19/19 cotejos: 17 campos del Layout Model, incluidos DXF7=`monochrome.ctb` y DXF70=1188, dos LINE/ACI1/3, INSUNITS6, Model2, AUDIT0/0, exit0, SHA DWG intacto. La copia con estilo esperado adulterado fue `semantic_mismatch` en `target/mcp-external/20260924-064358-autocad-5321f3e7/report.json` SHA-256 `2A309292FA4342081835A849E9639C78EB41A7B51DED29E347F93F7BB31AC29F`. Regresión L2 de page setup sin estilo `20260924-064406-metric-plot-88f55969` pasó con PDF SHA histórico; L4-17 de esa regresión y del fixture ByLayer pasaron en `20260924-064424-autocad-f8514cf4` y `064426-autocad-545c6e67`. El primer L4 CTB `064152` solo censó el DWG y quedó `unknown` porque aún no había comparador CTB; no se promueve.

Rust lib 1688 passed/24 ignored/0 failed, PlanSpec 55/55, automatización 32/32, `cargo build --bin OpenCADStudio`, compilación Python y `git diff --check` pasaron. M5.7 permanece **partial scoped L0/L2/L4**: se acreditan dos ACI con CTB integrado, no tablas externas/STB, mapeos personalizados, transparencias, impresora, diálogo o L5. M3, M4/M6 de topologías múltiples, M7 pendiente de seis imágenes inéditas y M8 siguen abiertos. DWG privado, reportes históricos y `upstream` intactos. Verificar publicación de esta rama tras commit/push.

### Prompt de continuidad desde el septuagésimo sexto corte

Verifica AGENTS, Git/remotos, SHA local/remoto y árbol de `codex/mcp-metric-monochrome-ctb`; preserva L2/L4/negativo/regresiones. Amplía M5.7 con plumas y transparencia de varias entidades solo bajo fixture/oráculo; evalúa texto PDF buscable y L5 humana, sin habilitar CTB arbitrarios sin validación. Continúa M4/M6 con múltiples muros/joins/colisiones y M3 con telemetría real Luna/supervisor. Al adjuntarse las seis imágenes inéditas autorizadas, congela hashes de desarrollo, cohorte 2/2/2 y oráculo antes de L3; ejecuta tres repeticiones/caso y L5. M8 después de gates completos y merge/push al fork. No abrir/modificar DWG privado ni publicar privados o `upstream`.

## Septuagésimo séptimo corte: puerta en dos muros ortogonales unidos — 2026-09-24

El corte 76 `codex/mcp-metric-monochrome-ctb` quedó publicado solo en el fork con SHA local/remoto `5945bd6934aa6355af3782e2fada2661ba30a517` y árbol limpio. En `codex/mcp-joined-wall-single-door` se añadió compilación determinista de una puerta en el primer muro de una unión ortogonal explícita. Contrato y límites en `JOINED-WALL-SINGLE-DOOR-V1.md`. El fixture versionado declara dos muros, un join y una puerta de 0.9 m. El compilador emite 13 LINE y 1 ARC, divide ambas caras del muro, conserva las jambas y rechaza la puerta cerca del join antes de CAD. Puertas en segundo muro o múltiples puertas quedan `unsupported`.

L2 final `target/mcp-isolated/20260924-065319-af355597/report.json` SHA-256 `BBBBD039E52AC445743DE5B3004B856CDF35DC423AC245EDE279194A3C589707` pasó con binario SHA-256 `0CA475A28486E846C8C7BBB37C973E0A4D61E3BB6BEA14A9C2C0D39A2152E414`, 14/14 entidades, IDs→handles, auditoría, DWG reabierto y GUI salida. DWG sintético SHA-256 `A0B9F152ECF60CBA81C21D830DF7D4D91D68AACB1727C0ADF209783E5E8C0ED1`. Captura PNG SHA-256 `88CEA3847E57103516DB53063316C8DB492B61D14365306EEAE1E287B1F28656` inspeccionada: se observa hueco, jambas, arco y unión L.

L4 AutoCAD final `target/mcp-external/20260924-065425-autocad-8c9605af/report.json` SHA-256 `4D65691093777AF96A2D5768C1B66BC032A2D5530FD48712FA62E6E21512D286` pasó `matched_scoped`: 14/14 cotejos independientes por fixture/handle, Model14 (13 LINE/1 ARC), INSUNITS6, AUDIT0/0, exit0 y DWG intacto. Negativo con handle de jamba adulterado en copia de reporte: `target/mcp-external/20260924-065519-autocad-457a578f/report.json` SHA-256 `767234677B7E3142811AF10CBE508748AC00CF77F4562DB8A2C73804F6BA50CD`, `mismatch` con una comparación fallida y DWG intacto. Regresión de unión sin puerta: `target/mcp-external/20260924-065502-autocad-f377acfb/report.json` SHA-256 `78BF28459195CA4DFA18FB16E06538574DC17CD300623FEFDF741A73CBC64993`, `matched_scoped` 8/8 y DWG intacto.

PlanSpec 56/56, automatización 32/32, compilación Python y `git diff --check` pasaron. Rust no cambió y permanece la suite 1688/24 ignorados del corte 76. M4/M6 sigue **partial scoped L0/L2/L4** para una unión y una puerta, sin múltiples joins ni oráculo L5. M3 requiere telemetría real de Luna/supervisor; M5.7 conserva límites de impresión; M7 espera las seis imágenes inéditas que el usuario confirmó que aportará; M8 sigue abierto. Ni el DWG privado, ni los ensayos previos, ni `upstream` se tocaron. Verificar SHA local/remoto tras commit/push de este corte.

### Prompt de continuidad desde el septuagésimo séptimo corte

Verifica AGENTS, Git/remotos, SHA local/remoto y árbol de `codex/mcp-joined-wall-single-door`; conserva L2, L4, negativo y regresión. Sigue M4/M6 con más de una unión, huecos y colisiones tipadas, primero con oráculo geométrico sintético y luego L2/L4/L5. No extrapoles el fixture L con una puerta a planos arbitrarios. Avanza M5.7 solo con oráculo de impresión reproducible y M3 con telemetría real Luna/supervisor. Cuando el usuario adjunte las seis imágenes inéditas autorizadas, congela hashes de desarrollo/cohorte 2/2/2 y oráculo antes de L3; ejecuta tres repeticiones por caso y solicita L5 humana. M8 exige gates completos antes de merge/push al fork. No abras ni modifiques el DWG privado ni publiques archivos privados o `upstream`.

## Septuagésimo octavo corte: puerta en cualquiera de los dos brazos — 2026-09-24

El corte 77 `codex/mcp-joined-wall-single-door` quedó publicado solo en el fork con SHA local/remoto `7fbe9bcdf8a07a1674cc1e67b0abe084a005187d` y árbol limpio. Desde `codex/mcp-joined-wall-door-either-arm`, el compilador admite la puerta sobre el segundo muro de la unión L, con sus propios tramos, jambas y giro. Fixture nuevo `synthetic-joined-door-second.planspec.json`: muro vertical, offset1.1 m, ancho0.9 m, apertura a la derecha. Un offset0.5 m se rechaza antes de CAD. El arnés L2 guarda el dibujo encuadrado en una ruta nueva para evitar reemplazar un DWG abierto. Contrato actualizado: `JOINED-WALL-SINGLE-DOOR-V1.md`.

El primer L2 `target/mcp-isolated/20260924-065930-d96f0f23/report.json` SHA-256 `B36754FD2B6D4893A68A9889954532E35CB5333505DC94DB5C1F94D0CD8A4408` quedó **partial**: el guardado posterior a `ZOOM EXTENTS` intentó reemplazar el archivo abierto y Windows devolvió error1175. El DWG verificado ya existía, pero no se promueve ese run. La GUI hija PID3412 fue identificada por sesión/proceso, guardada bajo nuevo nombre dentro del mismo target y cerrada con `QUIT`; su salida se confirmó. No se borró ni sobrescribió el ensayo.

L2 final `target/mcp-isolated/20260924-070045-b099aa9b/report.json` SHA-256 `F1E155A51EBD8264635CD6E449E4D846820186A5568D6DBA7ECF33C52407BFF3` pasó con 14 entidades, auditoría, DWG reabierto, captura fenced y GUI salida. DWG sintético SHA-256 `FC1DEF882242E409DA28069CCCD864FF6F8EF3EBD3F5EECD2BA1CB91B0ECCB63`; PNG SHA-256 `7ADC55A9072E24BA71421BBBA0AA7D5BC696873713FD4A36F79BC77CF39B40A2` inspeccionado visualmente: hueco vertical, jambas, hoja y arco a la derecha.

El primer L4 de este fixture `target/mcp-external/20260924-070101-autocad-857c2883/report.json` SHA-256 `49DA8929CFD90715702101AC567D6B37E926AAFC0DF938B1EBA72CF9D5CF3E0D` rechazó el arco porque el comparador esperaba ángulos DXF50/51 en el orden del giro horario. AutoCAD guarda el arco menor con orden angular antihorario. Se corrigió el oráculo según bisagra/lado, sin editar el DWG. L4 final `target/mcp-external/20260924-070126-autocad-524ecabe/report.json` SHA-256 `FDEE0CDC4D4AEEFAF29162A3502C2AF5BD33BA192221C6D02DE54E1A1D2123D2` pasó `matched_scoped` 14/14, Model14, INSUNITS6, AUDIT0/0, exit0 y DWG intacto. Negativo con handle de arco adulterado en copia: `target/mcp-external/20260924-070143-autocad-944e7cd5/report.json` SHA-256 `F2E70E51B6B8A0DA42952923687440C15C857E5B874675F729A9CC2428E304BF`, `mismatch` de una comparación con DWG intacto. Regresiones L4 del primer brazo `20260924-070155-autocad-1be80cf2` SHA-256 `637482BFA55B857AB1AD7CC59425D30B4251DD70F021C00F6C60C275F8171544` y unión sin puerta `20260924-070155-autocad-84e321ce` SHA-256 `96F35022F482AAFB6A9ED67A689CE206739287E3376A475BF51F517984EEEFFE` pasaron.

PlanSpec 56/56, automatización 32/32, compilación Python y `git diff --check` pasaron. Rust no cambió. M4/M6 sigue **partial scoped L0/L2/L4** para dos muros y una puerta en uno de los brazos; múltiples joins, varias aberturas, cotas de muro unido, colisiones generales y L5 siguen abiertos. M3, M5.7, M7 (seis imágenes inéditas confirmadas pero no adjuntas) y M8 permanecen en sus estados anteriores. No se abrió el DWG privado ni se publicó en `upstream`. Verificar SHA local/remoto después de publicar.

### Prompt de continuidad desde el septuagésimo octavo corte

Verifica AGENTS, Git/remotos, SHA local/remoto y árbol de `codex/mcp-joined-wall-door-either-arm`. Preserva el L2 parcial por error1175, el L4 rechazado por oráculo horario, L2/L4 finales, negativo y regresiones. Extiende M4/M6 a tres muros con dos joins explícitos mediante un oráculo de unión geométrica independiente; mantén fallos cerrados y evita inferir joins. Continúa M5.7 con oráculo de impresión reproducible y M3 con telemetría real Luna/supervisor. Al recibir las seis imágenes inéditas autorizadas, congela hashes de desarrollo/cohorte 2/2/2 y oráculo antes de L3; repite tres veces/caso y pide L5. M8 solo tras todos los gates y merge/push al fork. Nunca abras/modifiques el DWG privado ni publiques privados o `upstream`.

## Septuagésimo noveno corte: cadena de tres muros y dos uniones — 2026-09-24

El corte 78 `codex/mcp-joined-wall-door-either-arm` quedó publicado solo en el fork con SHA local/remoto `9fdd722031b962f04d74bee86f82b48e50c30c80` y árbol limpio. En `codex/mcp-three-wall-two-join-union`, el compilador acepta tres muros alineados con XY, igual espesor/capa, dos joins explícitos que formen cadena y ninguna abertura. Traza una sola frontera de la unión de rectángulos; rechaza contacto/solape de muros sin join, ejes oblicuos, contorno ambiguo/múltiple y segmentos colapsados. Contrato: `THREE-WALL-UNION-V1.md`.

El fixture `synthetic-three-wall-chain.planspec.json` produjo 12 LINE de contorno cerrado, área 1.98 m², hash de comandos determinista en dos compilaciones. L2 final `target/mcp-isolated/20260924-070633-bf83354b/report.json` SHA-256 `F1DC9ADDA84E52E60F80DF2D70344C3EE4DEE680560CF0BAF65E4D5B094752B5` pasó con 12 handles, auditoría, DWG reabierto y GUI salida. DWG SHA-256 `D7ADE7A0C3F04523A6DAE3EF351618597401A990030212E73F934415FF4C479F`. Captura SHA-256 `1F6A520622FB960B7E51D971A6264F47F6EB8325196DE5CFD7A82427CE484D24` inspeccionada: contorno continuo sin líneas interiores de unión.

L4 AutoCAD `target/mcp-external/20260924-070717-autocad-ee8b46fc/report.json` SHA-256 `E41F3DEEB73647618CD8EA470A496F2360278C5E5B4F73B130B711D541122D6A` pasó `matched_scoped` 12/12 contra oráculo fijo independiente, Model12, INSUNITS6, AUDIT0/0, exit0 y DWG intacto. Negativo con handle de borde adulterado en copia: `target/mcp-external/20260924-070734-autocad-9fd48cf3/report.json` SHA-256 `BD2E4A6B87183F90938232EE9231742BD4505807AEECB970E44D5FAB56325DD9`, `mismatch` de una comparación, DWG intacto. Regresiones L4 del segundo brazo con puerta `20260924-070747-autocad-3115661c` SHA-256 `919926D6AFB42FD1B87B2089A9DE1B850ADA578193F4AC935975EEB9A6E52C25` y de unión sin puerta `20260924-070747-autocad-6497e65f` SHA-256 `313480E9EBA1332E22132A6F974D6E9CAAD5A31C32FDB8B2143613D2C3110BED` pasaron. PlanSpec 57/57, automatización 32/32, compilación Python y `git diff --check` pasaron; Rust no cambió.

M4/M6 permanece **partial scoped L0/L2/L4**: el algoritmo admite la clase acotada de tres muros, pero L4 solo certifica este fixture; T/X, ciclos, cotas, aberturas, oblicuos, otros oráculos y L5 quedan pendientes. M3 requiere Luna/supervisor real; M5.7 tiene sus límites documentados; M7 espera las seis imágenes inéditas confirmadas por el usuario y M8 sigue abierto. No se abrió el DWG privado ni se tocó `upstream`. Verificar SHA local/remoto tras commit/push.

### Prompt de continuidad desde el septuagésimo noveno corte

Verifica AGENTS, Git/remotos, SHA local/remoto y árbol de `codex/mcp-three-wall-two-join-union`. Preserva L2/L4 final, negativo, regresiones y todos los ensayos históricos. Generaliza el oráculo independiente a otra cadena de tres muros antes de ampliar más topologías; prueba rechazos de T/X y vueltas que contacten muros sin join. Avanza M5.7 y M3 solo con mediciones reales y oráculos reproducibles. Cuando el usuario adjunte seis imágenes inéditas autorizadas, congela hashes de desarrollo, cohorte 2/2/2 y oráculo antes de L3, ejecuta tres repeticiones/caso y revisión L5. M8 exige todos los gates antes de merge/push solo al fork. No abrir/modificar el DWG privado ni publicar privados o `upstream`.

## Octogésimo corte: segundo oráculo rotado para tres muros — 2026-09-24

El corte 79 `codex/mcp-three-wall-two-join-union` quedó publicado solo en el fork con SHA local/remoto `444f2202847e66785b1594aadb9b5441031b6a34` y árbol limpio. En `codex/mcp-three-wall-rotated-oracle`, un segundo fixture rota 90° los tres ejes y sus dos uniones, sin cambiar el compilador. La sonda AutoCAD tiene un oráculo independiente de 12 extremos para esta orientación. Contrato y backlog actualizados en `THREE-WALL-UNION-V1.md` y `03-FASES-Y-BACKLOG.md`.

L2 final `target/mcp-isolated/20260924-071036-83db0b94/report.json` SHA-256 `242EAFF4CBE7A5F66B1EEE9F64D13398DE94F0CD9914B29A50ABB4BAD4500EDE` pasó con 12 entidades, DWG reabierto, AUDIT y GUI salida. DWG SHA-256 `C24D9D018258DA64CF895791494BFDC434695A5D858B5B4DF80F3983A459D956`. Captura SHA-256 `F4AED29B155C57A3D4758A8286B4FA8EEB1070DB8826DF2FF9061ADCB22D9519` inspeccionada: cadena continua girada, sin borde interior de join. L4 `target/mcp-external/20260924-071107-autocad-8f9b9208/report.json` SHA-256 `2FCB4B0ED503E850DD73F858D9D82FE9845950DFAEDC66B3E42C06744FF37CDA` pasó `matched_scoped` 12/12, Model12, INSUNITS6, AUDIT0/0, exit0 y DWG intacto. Negativo con handle alterado `target/mcp-external/20260924-071120-autocad-5ab50a6f/report.json` SHA-256 `3DF994B12E23F59D5A154908626884260D55B477ECAC0021116E55D30319CB33` dio `mismatch` de una comparación y DWG intacto. Regresión de la primera cadena `target/mcp-external/20260924-071129-autocad-46fa4367/report.json` SHA-256 `E5F5F3CBEF675CA45BDA072768F9C47D9C7DAEDCC50F8C487C875316DC307051` pasó.

PlanSpec 57/57. La suite de automatización, compilación Python y `git diff --check` se ejecutan antes del commit. Estado M4/M6 **partial scoped L0/L2/L4** para dos orientaciones de una cadena; no se acreditan T/X, ciclos, aperturas, cotas en unidos o L5. M3 real y M7 siguen requiriendo respectivamente telemetría Luna/supervisor y seis imágenes inéditas autorizadas aún no adjuntas; M8 no puede aceptarse. No se abrió el DWG privado ni se tocó `upstream`. Verificar SHA local/remoto tras publicar.

### Prompt de continuidad desde el octogésimo corte

Verifica AGENTS, Git/remotos, SHA local/remoto y árbol de `codex/mcp-three-wall-rotated-oracle`. Conserva ambos oráculos L2/L4, negativos, regresiones y fallos históricos. Continúa M4/M6 con T/X y aperturas solo después de contrato/topología y oráculo independiente, o trabaja las tareas offline pendientes de M3/M5 sin afirmar datos Luna. Cuando el usuario adjunte las seis imágenes inéditas autorizadas, congela hashes de desarrollo/cohorte 2/2/2 y oráculo antes de L3; ejecuta tres repeticiones por caso y revisión L5. M8 después de todos los gates con merge/push solo al fork. No abras/modifiques el DWG privado ni publiques privados o `upstream`.

## Octogésimo primer corte: congelación de oráculos reservados M7 — 2026-09-24

El corte 80 `codex/mcp-three-wall-rotated-oracle` quedó publicado solo en el fork con SHA local/remoto `907760fd30c8c62991823edc3b81a7e4074abe3a` y árbol limpio. Se releyeron AGENTS, Git/remotos y checkpoint. En `codex/mcp-m7-oracle-freeze` se agregó `reserved_oracle.py` con CLI `freeze`/`verify` y contrato `M7-ORACLE-FREEZE-V1.md`. Un manifiesto nuevo de oráculos referencia el SHA exacto de la cohorte de seis imágenes y el SHA/tamaño de cada archivo de criterios privado; no publica imágenes, rutas, referencias del revisor ni criterios. Cada oráculo declara procedencia y criterios para métrica, topología, semántica y apariencia. `verify_ready` vuelve a verificar las seis imágenes, seis oráculos y el manifiesto antes de reportar preparación L0. Cambios posteriores de cualquier fuente fallan cerrado. El manifiesto se crea una sola vez; una corrección requiere otro corte.

Cinco pruebas nuevas usaron solamente seis PNG sintéticos generados: congelación y CLI, ausencia de rutas/contenido privado en salida, cambio de oráculo, cambio de imagen, cambio byte a byte de cohorte, falta de categoría o fuente incorrecta y falta de referencia de revisión. La suite masterplan pasó 62/62, automatización 32/32, compilación Python y `git diff --check` pasaron. No cambió Rust ni se ejecutó CAD/modelo en este corte. La declaración `human_verified` en un JSON **no prueba** una revisión humana; los criterios reales deberán revisarse antes de L3. El preflight todavía no está integrado de forma obligatoria al runner Luna, por lo que M7 sigue **partial L0**. Las seis imágenes inéditas autorizadas aún no se adjuntan; M3 real, M5 restante, M7 L3/L4/L5 y M8 siguen abiertos. DWG privado, ensayos históricos y `upstream` intactos. Verificar SHA local/remoto después de commit/push.

### Prompt de continuidad desde el octogésimo primer corte

Verifica AGENTS, Git/remotos, SHA local/remoto y árbol de `codex/mcp-m7-oracle-freeze`. Conserva las 62/32 pruebas y el historial. Integra `verify_ready` como gate obligatorio antes de iniciar cualquier repetición M7, con journal de intento/resultado que vincule `cohort_sha256`, `oracle_manifest_sha256`, modelo, binario, imagen y tres repeticiones por caso; nunca envíes el oráculo al generador. Sigue M3 telemetría real y M5.7 con evidencia reproducible. Cuando el usuario adjunte seis imágenes inéditas autorizadas, congela cohorte y criterios revisados antes del primer L3. M8 requiere gates técnicos, L3/L4/L5, revisión, PR/merge y verificación solo del fork. No abrir/modificar el DWG privado ni publicar privados o `upstream`.
